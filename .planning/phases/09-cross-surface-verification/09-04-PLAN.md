---
phase: 09-cross-surface-verification
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md
  - lib/data/projects.ts
  - app/projects/addvanced/addvanced-client.tsx
  - app/projects/echo/echo-client.tsx
  - app/projects/nagarro/nagarro-client.tsx
  - app/projects/rambis-ui/rambis-client.tsx
autonomous: true
requirements: [CRED-09]
tags: [credibility, audit, matrix]

must_haves:
  truths:
    - "09-CROSS-SURFACE-MATRIX.md exists with one row per project claim for all 7 case studies, columns visible copy · metadata · OG · JSON-LD, every cell one of agree / fixed→X / pulled / open / — , plus a per-project 'open for Randy' list (D-14)"
    - "Every claim changed, pulled or conformed by Plans 02/03 (titles, descriptions, og:type, og:image, dateCreated, FAQ removal, dead exports, $50M wording, Chameleon URL, placeholder links, addvanced breadcrumb name) appears as a matrix row so Randy can veto (D-18)"
    - "Waffle is recorded as verify-only: grid card, /projects/waffle page, metadata and OG image self-consistent, no unification applied (D-13)"
    - "Where visible page copy and a data-derived surface disagree on the same claim, the visible copy value is applied to the data (metadata/OG/JSON-LD conform) and the fix is a `fixed→X` row — no silent winner, no invented third value (D-18)"
  artifacts:
    - path: ".planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md"
      provides: "Per-claim × surface reconciliation for growit, ohplays, ledgeriq, addvanced, echo, nagarro, rambis-ui + Waffle verify-only + site-wide claims + dead schema + placeholder sweep + open items"
      min_lines: 150
  key_links:
    - from: "09-CROSS-SURFACE-MATRIX.md rows"
      to: "lib/data/projects.ts fields (name, subtitle, description, metrics, role, teamSize, timeline)"
      via: "each row cites the data field / file:line it was checked against"
      pattern: "lib/data/projects.ts"
    - from: "09-CROSS-SURFACE-MATRIX.md Part D/E/F"
      to: "09-03-SUMMARY.md findings"
      via: "copied verbatim (dead exports, $50M table, Chameleon chain, placeholder hits)"
      pattern: "FractionalCDOServiceStructuredData|chameleoncollective|example.com"
---

<objective>
Author the D-14 proof artifact — `09-CROSS-SURFACE-MATRIX.md` — by reading every claim on each of the 7 case-study surfaces (visible copy, metadata, OG image, JSON-LD) against `lib/data/projects.ts`, recording agree / fixed→X / pulled / open per cell, applying the D-18 conflict rule where the same claim differs (visible copy wins; conform the data field that feeds metadata/JSON-LD; log the row), and listing per-project items only Randy can close. Waffle gets a verify-only section (D-13). Plans 02/03 outcomes are folded in as rows.

Purpose: This is the artifact that closes CRED-09 the way v1.0's audit remediation did — a hiring manager reads the same story on the page that a recruiter sees in a link unfurl or page source, and every reconciliation is auditable.
Output: the matrix file; any D-18 data/copy conformance edits (each a row); no new authoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-02-SUMMARY.md
@.planning/phases/09-cross-surface-verification/09-03-SUMMARY.md
@.planning/DECK-COVERAGE-AUDIT.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@lib/data/projects.ts
@lib/metadata.ts
@CLAUDE.md

<interfaces>
<!-- Where each surface is read from, per project (post Plans 02/03). -->
| Project | Visible copy | Metadata (all derived: title `{name} | {subtitle}`, description = project.description) | OG image (og:image = projectOgImage) | JSON-LD (CreativeWork props + Breadcrumb from PROJECTS) |
|---|---|---|---|---|
| growit, ohplays, ledgeriq | app/projects/[slug]/project-detail-client.tsx (renders PROJECTS fields; only hard-coded numeric is a code comment "800%" at ~line 130) | projectMetadata(project) | growit hero-thumbnail.jpg (WebP data), ohplays ohplays-video-poster.png, ledgeriq → images[0] 1.jpg (thumbnail is .mp4) | projectCreativeWorkProps + projectBreadcrumbItems |
| addvanced | app/projects/addvanced/addvanced-client.tsx (1272 lines; imports PROJECTS; hard-coded tokens: "2 weeks (Sprint)", 50%, 60%, 70%, 800%, 86%+, 93+, 94%) | same | "/projects/addvanced/A0-Addvanced Splash Screen.png" (654×1414 portrait, space in filename) | same |
| echo | app/projects/echo/echo-client.tsx (470 lines; imports PROJECTS; hard-coded tokens: 100%, "40,000+") | same | /projects/echo/poster.png | same (dateCreated omitted — no year in timeline) |
| nagarro | app/projects/nagarro/nagarro-client.tsx (1118 lines; imports PROJECTS; hard-coded tokens: $50M+, 10,000+, 100+, 15+, 18,000+, 25%, "36 countries", 40%, 50%) | same | /projects/nagarro/nagarro-logo.png | same |
| rambis-ui | app/projects/rambis-ui/rambis-client.tsx (418 lines; renders rambisProject.description; hard-coded tokens: 100%, 40%, 50+) | same | /projects/rambis-ui/hero-thumbnail.jpg | same |
| waffle (verify-only, D-13) | app/projects/waffle/waffle-client.tsx (tokens: "2–4 minutes", "25+ more note-taking tools") + grid card | createPageMetadata in app/projects/waffle/page.tsx (title "Waffle | AI Interview Scorecard Generator", description identical to PROJECTS description) | /projects/waffle/opengraph.png (static — Read the PNG and transcribe any text claims) | none by design |
Grid surfaces shared by all: app/projects/projects-client.tsx (renders PROJECTS name/description/tags/status badges — no hard-coded numbers), app/projects/opengraph-image.tsx (grid OG renders "GrowIt · 240K+ Users · 4.8★ Rating" — growit metrics rows).

Data fields that count as claims (row sources): name, subtitle, description, longDescription (figures embedded), metrics[] (each entry), role, teamSize, timeline, status, isComposite/isLiveProduct badges, and named companies/partners/awards in copy (e.g., Alight/1871 for addvanced, Echo Global Logistics for echo, Nagarro, Chakra UI for rambis, Davey/Vega awards for growit).

Cell vocabulary (D-14): `agree` · `fixed→X` (X = the value now on that surface) · `pulled` (claim removed from that surface) · `open` (needs Randy) · `—` (surface does not carry this claim). Row verdict = worst cell.

Rows Plans 02/03 already determined (copy from their SUMMARYs; still one row each):
- every route: title fixed→"{name} | {subtitle}"; meta description fixed→project.description (was longDescription on [slug], hand-typed on standalone); og:type fixed→article ([slug] was website); og:image fixed→thumbnail (addvanced was A1-Home.png, echo img1.jpg, nagarro digital-accessibility-strategy.png); JSON-LD dateCreated fixed→4-digit year (was "Q1 2014"-style) / pulled for addvanced+echo; JSON-LD description fixed→project.description; CreativeWork+Breadcrumb added on echo/nagarro/rambis-ui (was none), Breadcrumb added on [slug]; addvanced breadcrumb name fixed→"Addvance" (was "Addvanced Career Tracker")
- FAQPage JSON-LD pulled from all [slug] pages (D-09: echo "AI design system", addvanced "financial advisors / SOC 2", metis orphan, growit on-topic but not visible on page)
- nagarro metadata claims pulled by derivation: "$50M+ in business impact", "36 countries", "15,000 to 18,000+", "100+ qualified leads" (visible copy still carries $50M+ / 36 countries / 100+; data description carries 18,000+ / 50%)
- Part D/E/F content from 09-03-SUMMARY (dead exports, $50M per-surface table, Chameleon chain, placeholder hits)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the matrix skeleton, [slug] trio rows, Waffle verify-only, and site-wide Parts D–F</name>
  <files>.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md</files>
  <read_first>
    - .planning/DECK-COVERAGE-AUDIT.md lines 1–160 (the table/verdict-definition format the matrix is modeled on — copy its header discipline: source file:line per row, verdict definitions up top)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md lines 55–70 (the remediation + verification-line style)
    - .planning/phases/09-cross-surface-verification/09-02-SUMMARY.md and 09-03-SUMMARY.md (per-route old→new titles; dead exports; $50M table; Chameleon chain; placeholder hits)
    - lib/data/projects.ts entries for growit, ohplays, ledgeriq, waffle (name/subtitle/description/longDescription/metrics/role/teamSize/timeline/status/isComposite/isLiveProduct)
    - lib/metadata.ts (projectMetadata / projectCreativeWorkProps — what metadata + JSON-LD now emit, so you can fill those columns from the data without rendering)
    - app/projects/[slug]/project-detail-client.tsx (grep for hard-coded numeric/company/award strings: `grep -noE '(\$?[0-9][0-9,.]*[%KM★+]+|[0-9]{2,3},[0-9]{3}\+?)'` — expected only the "800%" code comment; also grep "Composite" for the isComposite disclosure copy)
    - app/projects/projects-client.tsx (grid card fields rendered) and app/projects/opengraph-image.tsx lines 135–155 (grid OG "240K+ Users / 4.8★ Rating")
    - app/projects/waffle/page.tsx, app/projects/waffle/waffle-client.tsx, public/projects/waffle/opengraph.png (Read the image; transcribe text claims)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-09, D-13, D-14, D-15, D-16, D-17, D-18 and `<code_context>` "Findings already known (seed rows for the matrix)"
  </read_first>
  <action>
    Create `.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md` with this structure (headings exactly): title `# Phase 9 — Cross-Surface Matrix`; header block (Produced date, Requirement CRED-09, Method: "each claim read from lib/data/projects.ts and compared against the four surfaces; metadata + JSON-LD columns are derived by projectMetadata/projectCreativeWorkProps after Plan 02, so those cells cite the data field"); `## Verdict Definitions` (agree / fixed→X / pulled / open / — exactly as in the interfaces block); `## Surfaces` (the per-project surface table from the interfaces block); `## Part A — Per-project claim matrix` with a `### growit`, `### ohplays`, `### ledgeriq` subsection now (Task 2 adds the other four) — each a table `| Claim ID | Claim | Visible copy | Metadata | OG image | JSON-LD | Verdict | Note |` with Claim IDs like `GROWIT-M01` (metric), `-R01` (role/title/team/timeline), `-N01` (named partner/award), `-T01` (title/description/og). Minimum rows per project: title/subtitle, description, role, teamSize, timeline, status/badge (isComposite for ledgeriq — its "Composite Case Study" disclosure must be a row), every metrics[] entry, every named company/award in copy, plus the Plan-02 derived rows (title fixed→…, description fixed→project.description, og:type fixed→article, og:image, dateCreated fixed→year, FAQ pulled, Breadcrumb added). For growit also add the grid-OG rows (240K+ Users, 4.8★ Rating vs metrics — expected `agree`). For the OG image column: Read each thumbnail file; if it carries no legible text claims write `— (no text claims)`; if unreadable (growit hero-thumbnail.jpg is WebP data) say so. `## Part B — Waffle (verify-only, D-13)`: table over grid card · /projects/waffle page · metadata (createPageMetadata) · OG image (public/projects/waffle/opengraph.png transcription) for name, subtitle/tagline, description, "2–4 minutes", "25+ note-taking tools", Live Product badge, tech list — verdict per row; state explicitly "no helper, no CreativeWork/Breadcrumb added by design". `## Part C — Open for Randy` (per-project bullet lists; add growit/ohplays/ledgeriq now, others in Task 2; include the carried "as-of date for 4.8★ / 240K+" as an optional non-blocking item exactly as CONTEXT.md `<deferred>` words it, and for ledgeriq the note that its metrics were never source-verified per 05-CONTEXT deferred). `## Part D — Site-wide claims (D-15 $50M, D-16 Chameleon)`: paste the $50M per-surface table from 09-03-SUMMARY (career-wide rows verdict `agree`/`fixed→"$50M product value delivered"`; Nagarro-specific row `agree` with note "only surface after derivation; Randy confirmed real for both readings, D-15") and the Chameleon curl chain + resulting URL row (`fixed→https://chameleoncollective.com` or as recorded). `## Part E — FAQ disposition & dead schema removed (D-09, D-10)`: table `| File | Export | Claims it carried | Importers | Disposition |` with project-faq.tsx (entire file, `pulled`) and the four structured-data.tsx exports from 09-03-SUMMARY, plus the recoverability note. `## Part F — Placeholder sweep (D-17)`: table `| Pattern | Hits | File:line | Disposition |` for example.com / lorem / placeholder-as-copy / TODO-copy markers from 09-03-SUMMARY (the blog "Further Reading" links `pulled`; the email-test `placeholder=` attribute `excluded (HTML attribute)`). Leave `## Part G — Stale-claim grep & verification gate (D-19)` as a heading with the line "(filled by Plan 05)". Every row cites a source (`lib/data/projects.ts` field or `file:line`). Do not edit any code in this task.
  </action>
  <verify>
    <automated>f=.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md; test -f $f && for h in "## Verdict Definitions" "## Surfaces" "## Part A" "### growit" "### ohplays" "### ledgeriq" "## Part B" "## Part C" "## Part D" "## Part E" "## Part F" "## Part G"; do grep -q "$h" $f || { echo "missing $h"; exit 1; }; done && [ "$(grep -c '^| ' $f)" -ge 60 ] && grep -q "240K+" $f && grep -q "4.8★" $f && grep -q "Composite" $f && grep -q "project-faq.tsx" $f && grep -q "FractionalCDOServiceStructuredData" $f && grep -q "chameleoncollective" $f && grep -q "example.com" $f</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md` succeeds
    - Headings present (grep -q each): `## Verdict Definitions`, `## Surfaces`, `## Part A`, `### growit`, `### ohplays`, `### ledgeriq`, `## Part B`, `## Part C`, `## Part D`, `## Part E`, `## Part F`, `## Part G`
    - `grep -c '^| ' MATRIX` ≥ 60 (table rows incl. headers) after this task
    - `grep -c 'GROWIT-M\|OHPLAYS-M\|LEDGERIQ-M' MATRIX` ≥ 19 (6 growit + 7 ohplays + 6 ledgeriq metrics rows)
    - `grep -q "fixed→article" MATRIX` and `grep -q "pulled" MATRIX` and `grep -q "agree" MATRIX` succeed
    - `grep -q "Waffle" MATRIX` and `grep -q "createPageMetadata" MATRIX` succeed (Part B)
    - `grep -q "as-of date" MATRIX` succeeds (Part C carried item)
    - `grep -q '\$50M product value delivered' MATRIX` and `grep -q '\$50M+ in business impact' MATRIX` succeed (Part D)
    - `grep -q "FAQStructuredData" MATRIX` and `grep -q "OrganizationStructuredData" MATRIX` succeed (Part E)
    - `git diff --quiet HEAD -- lib app components` exits 0 (no code changes in this task)
  </acceptance_criteria>
  <done>Matrix skeleton with verdict definitions, [slug] trio rows, Waffle verify-only, and site-wide Parts D–F exists; every row cites a source.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the 4 bespoke projects — rows, D-18 conformance fixes, open items</name>
  <files>.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md, lib/data/projects.ts, app/projects/addvanced/addvanced-client.tsx, app/projects/echo/echo-client.tsx, app/projects/nagarro/nagarro-client.tsx, app/projects/rambis-ui/rambis-client.tsx</files>
  <read_first>
    - .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md (Task 1 output — append to Part A / Part C)
    - lib/data/projects.ts entries for addvanced, echo, nagarro-design-leadership, rambis-ui (all claim-bearing fields incl. processStory.outcome / stakeholderQuotes for echo)
    - app/projects/addvanced/addvanced-client.tsx — read the hero (lines ~110–160), metadata bar, metrics grid, and every line returned by `grep -nE '(\$?[0-9][0-9,.]*[%KM★+]+|[0-9]{2,3},[0-9]{3}\+?|[0-9]+ (weeks|members|participants|testers))' app/projects/addvanced/addvanced-client.tsx` (expected: "2 weeks (Sprint)", 50%, 60%, 70% (a CSS gradient stop — ignore), 800%, 86%+, 93+, 94%)
    - app/projects/echo/echo-client.tsx — hero (lines ~185–210), metrics, quotes, and every grep hit (expected: 100%, "40,000+" ~line 150 background paragraph)
    - app/projects/nagarro/nagarro-client.tsx — hero (lines ~560–585), metrics/initiatives (~460–495, ~860–870), and every grep hit (expected: $50M+, 10,000+, 100+, 15+, 18,000+, 25%, "36 countries", 40%, 50%)
    - app/projects/rambis-ui/rambis-client.tsx — hero (lines ~28–40), stats (~300–310), and every grep hit (expected: 100%, 40%, 50+)
    - .planning/DECK-COVERAGE-AUDIT.md rows for addvanced (ADDV-*) — reference only, advisory
    - .planning/STATE.md §Accumulated Context (Echo = process/design only: "$184.4 million", "16% revenue", "12% shipment volume", "1,000 beta downloads", "10,000+ active drivers" were removed — any reappearance is a `pulled` fix, not a row to keep)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-15, D-18 and `<specifics>`
  </read_first>
  <action>
    For each of addvanced, echo, nagarro, rambis-ui add a `### <slug>` subsection to Part A with the same table shape and Claim-ID scheme, minimum rows: title/subtitle vs page h1/tagline (addvanced h1 "AI-Enhanced Career Intelligence Platform" = subtitle; echo h1 "ECHO DRIVE"; rambis h1 "Rambis UI: A Modern Design System"; nagarro hero — record as `agree` when the same entity is named without contradiction, `fixed→…` only if a value conflicts), description (meta = project.description; visible hero paragraph — same claims?), role, teamSize, timeline (addvanced "2-week sprint" vs "2 weeks (Sprint)"; echo "Alpha → Beta → Launch"; nagarro "Mar 2022 - Oct 2022" vs "8 months"), every metrics[] entry vs the client's rendered metric (value AND label), every hard-coded numeric token from the greps above (each is a row: state whether the data carries it — e.g. nagarro 36 countries / $50M+ / 15+ articles are visible-copy-only after derivation → `—` in metadata/JSON-LD, verdict `agree` if no surface contradicts, with note "visible-only claim"), named partners/companies (Alight Innovation Lab / 1871 for addvanced; Echo Global Logistics; Nagarro; Chakra UI fork for rambis), and the Plan-02 derived rows (old title → new, og:image old → thumbnail, CreativeWork/Breadcrumb added, addvanced breadcrumb name fixed→Addvance, echo/addvanced dateCreated pulled). Apply D-18 only where the SAME claim carries DIFFERENT values across surfaces and neither is obviously stale: the visible page copy value wins — conform the `lib/data/projects.ts` field (which feeds metadata/JSON-LD/grid) or, if the conflict is inside the client file itself against a data-rendered value on the same page, conform the hard-coded client string to the data value only when the data value is the Phase-8-reviewed one and say so; record `fixed→X` with before/after and file:line; never invent a third value; if unsure which is stale, mark `open` and add it to Part C. Any Echo business figure that reappears anywhere is `pulled` (CRED-08). Do NOT rewrite prose, add claims, or change any metric Randy has not already stated. Update Part C with each project's "open for Randy" list (include: addvanced OG image is a 654×1414 portrait splash with a space in the filename — thumbnail choice is Randy's, `open`; nagarro OG is the logo PNG — `open` if he wants a richer image; anything marked `open` in rows). If any code file was edited, run `npm run lint`, `npx tsc --noEmit`, `npx jest __tests__/seo` and the relevant project test dir (`npx jest __tests__/projects`); if none was edited, state "no D-18 code conformance needed" in the SUMMARY.
  </action>
  <verify>
    <automated>f=.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md; for h in "### addvanced" "### echo" "### nagarro" "### rambis-ui"; do grep -q "$h" $f || { echo "missing $h"; exit 1; }; done && [ "$(grep -c '^| ' $f)" -ge 130 ] && grep -q "36 countries" $f && grep -q "Addvance" $f && grep -q "40,000+" $f && grep -q "Chakra" $f && ! grep -q "184.4" lib/data/projects.ts app/projects/echo/echo-client.tsx && npx tsc --noEmit && npx jest __tests__/seo</automated>
  </verify>
  <acceptance_criteria>
    - Headings `### addvanced`, `### echo`, `### nagarro`, `### rambis-ui` present in Part A; `grep -c '^| ' MATRIX` ≥ 130
    - `grep -c 'ADDVANCED-M\|ECHO-M\|NAGARRO-M\|RAMBIS-M' MATRIX` ≥ 24 (7 + 3 + 8 + 6 metrics rows)
    - Rows exist for each seeded token: `grep -q "36 countries" MATRIX`, `grep -q "\$50M+" MATRIX`, `grep -q "40,000+" MATRIX`, `grep -q "800%" MATRIX`, `grep -q "86%+" MATRIX`, `grep -q "Chakra" MATRIX`, `grep -q "Alight" MATRIX`, `grep -q "Echo Global" MATRIX` all succeed
    - `grep -q "Addvanced Career Tracker" MATRIX` succeeds (the breadcrumb-name fix is a row) and `grep -q "fixed→Addvance" MATRIX` succeeds
    - Part C has a bullet list under each of the 7 project names (`grep -c '^### ' MATRIX` ≥ 14 counting Part A + Part C subsections, or Part C uses `**<slug>**` labels — either way `grep -c "open for Randy\|Open for Randy" MATRIX` ≥ 1 and each of the 7 slugs appears in Part C)
    - Every `fixed→` row in the four new subsections that changed code has a matching `git diff` hunk (executor lists them in SUMMARY); `grep -rn "184\.4\|16% revenue\|12% shipment\|1,000 beta\|10,000+ active drivers" app lib components` prints nothing
    - If code was edited: `npm run lint` exits 0, `npx tsc --noEmit` exits 0, `npx jest __tests__/seo` exits 0
    - No `open` cell lacks a matching Part C bullet (`grep -c "| open |" MATRIX` equals the number of Part C bullets tagged `(row …)` or the SUMMARY explains the delta)
  </acceptance_criteria>
  <done>All 7 projects reconciled row-by-row; D-18 conformance applied and logged where a real conflict existed; open items enumerated per project; gates clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer (executor) → lib/data/projects.ts | D-18 conformance edits change the single source that feeds visible copy, metadata, OG alt, and JSON-LD; a wrong "winner" propagates everywhere. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Repudiation (silent claim change) | lib/data/projects.ts / client copy edits | mitigate | Every edit is a `fixed→X` matrix row with before/after + file:line; `open` when unsure (D-18); no third values invented. |
| T-09-10 | Information Disclosure (NDA figures re-surfacing) | echo surfaces | mitigate | Grep for the five removed Echo figures is an acceptance criterion; any hit is `pulled` (CRED-08). |
| T-09-SC | Tampering (supply chain) | npm installs | accept | No new dependencies. |
</threat_model>

<verification>
- tsc note: at planning time `npx tsc --noEmit` reported errors ONLY under `.next/types/**` (stale build artifacts referencing deleted routes `/ledgeriq`, `/archive`; tsconfig includes `.next/types/**/*.ts`). If you see those, run `rm -rf .next/types` and re-run; only errors outside `.next/` count as failures. Never treat `npm run build` as a gate (CLAUDE.md).
- Matrix has all 7 `### <slug>` subsections + Parts B–F, ≥130 table rows, every changed/pulled claim from Plans 02/03 present as a row
- No Echo business figures anywhere in app/lib/components
- If code edited: lint/tsc/jest seo + projects suites clean
</verification>

<success_criteria>
- D-14 artifact (1) complete except Part G (Plan 05)
- D-13 Waffle verify-only recorded; D-15/D-16/D-17/D-09/D-10 outcomes recorded as rows
- D-18 applied transparently; Part C lists what only Randy can close
</success_criteria>

<output>
Create `.planning/phases/09-cross-surface-verification/09-04-SUMMARY.md` when done. List every `fixed→` row that touched code (file:line, before → after) and every `open` row.
</output>
