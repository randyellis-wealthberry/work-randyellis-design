---
phase: 11-self-consistency-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/about/opengraph-image.tsx
autonomous: true
requirements: [CRED-13]
decisions: [D-01, D-02]

must_haves:
  truths:
    - "The /about OG image states 4 Design Awards, agreeing with lib/data/retainer.ts:51, app/about/about-client.tsx:26, app/about/page.tsx:27 and lib/seo/json-ld.ts's four-entry award array (D-01)"
    - "The fix ships as its own commit, touching exactly one file, bundled with no other Phase 11 work (D-02)"
  artifacts:
    - path: "app/about/opengraph-image.tsx"
      provides: "/about OpenGraph image with a correct award count"
      contains: "Design Awards"
  key_links:
    - from: "app/about/opengraph-image.tsx"
      to: "lib/data/retainer.ts PROOF_EXHIBITS 'Design awards won'"
      via: "both state the same award count as a literal"
      pattern: "the awards value div text node equals 4"
---

<objective>
Fix the single self-contradiction in the site's award count: `app/about/opengraph-image.tsx`
renders `6` Design Awards while every other surface that states a count says 4.

Purpose: this is the third recurrence of the v1.0 CRED-01 defect. It has survived two milestone
audits. It is a pure bug — no design decision blocks it, no backing question is involved
(D-01), which is why D-02 requires it to ship as its own independently-shippable commit rather
than riding along with PRF-01 or CRED-12.

Output: `app/about/opengraph-image.tsx` states `4`. One file changed. Nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-self-consistency-proof/11-CONTEXT.md
@.planning/phases/11-self-consistency-proof/11-PATTERNS.md

**Scope guardrails (hard — from ROADMAP "Planning constraints"):**
- Do NOT remove or alter `2.5M+`, `$50M`, or `800+` anywhere. They are accurate and stay.
- Do NOT touch `__tests__/integration/home-page-argument.test.tsx` — correct as written.
- Do NOT touch `app/page.tsx`, `app/services/services-client.tsx`, `app/opengraph-image.tsx`,
  or `lib/data/projects.ts`.
- `npm run build` is NOT a validation gate — `next.config.js` sets `eslint.ignoreDuringBuilds`
  and `typescript.ignoreBuildErrors`. The gate is `npm run lint` → `npx tsc --noEmit` → `npm test`.
</context>

<interfaces>
<!-- The awards cell in app/about/opengraph-image.tsx. This is a hand-built next/og flex tree
     with no shared component and no imported data — every literal is inline, which is exactly
     how `6` survived while three other surfaces said 4. -->

The cell is a two-child column `<div>`: a value `<div>` styled
`fontSize: "36px", fontWeight: "700", color: "#60a5fa"` whose sole content is the bare text
node, immediately followed by a label `<div>` styled `fontSize: "14px", opacity: 0.8` whose
sole content is the text node `Design Awards`.

Confirmed by grep at plan time: the value text node is the ONLY line in the entire file
matching `^[[:space:]]*[0-9]+[[:space:]]*$` (a whitespace-only-plus-digits line). Every other
numeric literal in the file is quoted CSS (`"36px"`, `"14px"`, `0.8`). That makes the bare-line
grep a precise, collision-free check.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Correct the /about OG award count from 6 to 4</name>

  <read_first>
    - `app/about/opengraph-image.tsx` — read the file. Locate the awards cell by searching for
      the text node `Design Awards`; the value `<div>` is the sibling immediately ABOVE it in
      source order. Do not trust the line number `238` blindly — confirm the value text node
      you are about to edit is the one whose next sibling renders `Design Awards`.
    - `lib/data/retainer.ts` — the `PROOF_EXHIBITS` entry `{ value: "4", context: "Design awards won" }`
      is the count this surface must agree with.
    - `lib/seo/json-ld.ts` (the `award` array in `buildPersonSchema`) — four entries, the
      independent confirmation that the true count is 4.
  </read_first>

  <action>
    Change the bare text node inside the awards value `<div>` from `6` to `4`.

    Change nothing else in that cell: the label text stays `Design Awards`, `fontSize` stays
    `"36px"`, `fontWeight` stays `"700"`, `color` stays `"#60a5fa"`, and the label `<div>`'s
    `fontSize: "14px"` / `opacity: 0.8` are untouched. No imports change. No other cell in the
    stats row is touched — the `2.5M+`, `$50M` and `800+` cells stay exactly as they are.

    This is a one-character literal edit. Do not refactor the cell to read from
    `PROOF_EXHIBITS`, do not extract a shared component, do not reorder cells. Consolidating
    the hand-typed literals is tracked separately as MI-4 and is explicitly deferred past v3.0.

    Per D-02 this plan is the standalone commit for CRED-13. Do not stage or include any file
    other than `app/about/opengraph-image.tsx`.
  </action>

  <verify>
    <automated>test -z "$(grep -nE '^[[:space:]]*6[[:space:]]*$' app/about/opengraph-image.tsx)" &amp;&amp; grep -nE '^[[:space:]]*4[[:space:]]*$' app/about/opengraph-image.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE '^[[:space:]]*6[[:space:]]*$' app/about/opengraph-image.tsx` returns nothing
      (exit status 1).
    - `grep -cE '^[[:space:]]*4[[:space:]]*$' app/about/opengraph-image.tsx` outputs exactly `1`.
    - `grep -c 'Design Awards' app/about/opengraph-image.tsx` outputs exactly `1` — the label is
      unchanged and was not duplicated.
    - `grep -c '2.5M+' app/about/opengraph-image.tsx`, `grep -c '\$50M' app/about/opengraph-image.tsx`
      and `grep -c '800+' app/about/opengraph-image.tsx` each return the same value they returned
      before the edit — the other three figures are untouched.
    - `git diff --stat` lists exactly one file: `app/about/opengraph-image.tsx`, with `1
      insertion(+), 1 deletion(-)`.
    - `git status --porcelain package.json package-lock.json` outputs nothing — no dependency
      was added (T-11-SC).
  </acceptance_criteria>

  <done>
    The `/about` OG image renders `4` above the label `Design Awards`. No other line in the file
    changed. No other file changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the fix passes the real verification gate</name>

  <read_first>
    - `./CLAUDE.md` §"Verifying Changes" — establishes that `npm run build` is not a validation
      step and fixes the gate order.
    - `jest.config.js` — confirms which paths Jest runs, so a green run is interpretable.
  </read_first>

  <action>
    Run the project's verification gate in the mandated order and confirm the change is clean:
    `npm run lint`, then `npx tsc --noEmit`, then `npm test`.

    Do NOT run `npm run build` as proof of anything — `next.config.js` sets
    `eslint.ignoreDuringBuilds` and `typescript.ignoreBuildErrors`, so a green build says
    nothing about correctness. Also do not run a build while a dev server is running.

    Note the known-baseline Jest behaviour recorded in `./CLAUDE.md`: several suites are
    intentionally `describe.skip`, and `__tests__/performance/animation-load-testing.test.tsx`
    measures FPS inside jsdom and is flaky on slower machines. If a failure appears, confirm it
    is one of those baselines by checking whether it also fails with this plan's change stashed
    (`git stash` → re-run the single failing suite → `git stash pop`). Only a failure that is
    absent on the stashed tree is a regression caused by this plan.
  </action>

  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npx tsc --noEmit` exits 0 and prints no diagnostics.
    - `npm test` shows no failure in `__tests__/seo/`, `__tests__/integration/`, or
      `__tests__/about-professional-experience.test.tsx`.
    - Any `npm test` failure that does appear is reproduced on the stashed tree, proving it is
      a pre-existing baseline and not caused by this plan; the SUMMARY records which suite and
      the stash-comparison result.
  </acceptance_criteria>

  <done>
    Lint and typecheck are clean. Jest shows no new failures attributable to this change, with
    any pre-existing baseline failure explicitly identified by stash comparison.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → `/about/opengraph-image` | `next/og` edge-rendered route, publicly reachable, GET-only, accepts no query params, path params, headers or body from the caller |
| repo → build output | the edited literal is compiled into the edge function bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / Injection | `app/about/opengraph-image.tsx` render tree | accept | No user-controlled data reaches the render. Every value in the awards cell is a module-scope literal; the route reads no `request`, no `searchParams`, no headers. Replacing one static digit with another introduces no sink. Acceptance criterion "diff is 1 insertion / 1 deletion in one file" bounds the change to exactly that. |
| T-11-02 | Information Disclosure | `/about/opengraph-image` response | accept | The award count is already public on `/about`, in `app/about/page.tsx`'s `openGraph.description`, and in the Person JSON-LD. Correcting `6`→`4` discloses nothing that was not already published, and removes a false statement rather than adding a true one. |
| T-11-03 | Denial of Service | edge image generation | accept | Satori/`next/og` rendering is CPU-bound and the route is publicly reachable, but this is pre-existing route behaviour that predates the phase and is unchanged by a literal edit. Vercel edge platform limits apply. Not introduced here; no in-phase mitigation is warranted. |
| T-11-SC | Tampering | npm supply chain | mitigate | This plan installs zero packages. Enforced by the acceptance criterion `git status --porcelain package.json package-lock.json` outputs nothing. No `## Package Legitimacy Audit` is required because no package-manager install task exists. |
</threat_model>

<verification>
1. `grep -nE '^[[:space:]]*6[[:space:]]*$' app/about/opengraph-image.tsx` → no output.
2. `grep -nE '^[[:space:]]*4[[:space:]]*$' app/about/opengraph-image.tsx` → exactly one line, and
   the `Design Awards` label appears within the following 10 lines
   (`grep -A10 -nE '^[[:space:]]*4[[:space:]]*$' app/about/opengraph-image.tsx | grep -c 'Design Awards'` outputs `1`).
3. `git diff --stat` → one file, one insertion, one deletion.
4. `npm run lint` → exit 0; `npx tsc --noEmit` → exit 0; `npm test` → no new failures.
</verification>

<success_criteria>
- The `/about` OG image states `4` Design Awards (D-01).
- `2.5M+`, `$50M` and `800+` are unchanged on this and every other surface.
- The change is confined to `app/about/opengraph-image.tsx` — one file, one line — so it is an
  independently-shippable commit (D-02).
- `npm run lint` → `npx tsc --noEmit` → `npm test` all pass with no new failures.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-proof/11-01-SUMMARY.md` when done.
Record: the pre-edit and post-edit grep output, the `git diff --stat` line, and — if any Jest
suite failed — the stash-comparison result proving it is a pre-existing baseline.
</output>
