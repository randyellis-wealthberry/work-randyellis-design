---
phase: 11-self-consistency-proof
plan: 05
type: execute
wave: 4
depends_on: ["11-02", "11-04"]
files_modified: []
autonomous: false
requirements: [PRF-01]
decisions: [D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "A human has seen the rendered /about awards cell at mobile and desktop widths and confirmed it reads as four awards plus a judge credential, not five awards"
    - "The in-cell list markup — Claude's Discretion under D-05 — is approved by the person who locked the placement"
    - "`npm run lint` → `npx tsc --noEmit` → `npm test` all pass on the assembled phase (ROADMAP criterion 5)"
    - "No plan in this phase removed 2.5M+, $50M or 800+, touched home-page-argument.test.tsx, app/page.tsx, services-client.tsx, or lib/data/projects.ts"
  artifacts: []
  key_links:
    - from: "rendered /about proof band"
      to: "lib/seo/json-ld.ts buildPersonSchema().award"
      via: "a human reads both and confirms they name the same four awards"
      pattern: "visual confirmation, recorded in the SUMMARY"
---

<objective>
Close the phase with the two things automation cannot do: run the real verification gate against
the fully assembled phase, and have a human look at the rendered result.

Purpose: D-04 locked WHERE the awards go and D-06/D-07 locked what they must not look like, but
CONTEXT.md left the exact markup to Claude's Discretion. Nobody has seen it. The awards land in
a quarter-width cell of a four-column grid at `text-xs` — four award names with issuers plus a
judge credential in that space is the single real risk in this phase, and it is only visible in
a browser. Plans 11-01 through 11-04 each verified their own file; this plan verifies the
assembled result.

This plan is a gate, not an implementation. It creates and modifies no files. If the human
verification surfaces a problem, that becomes a gap-closure plan — it is not fixed inline here.

Output: a recorded gate result and a human approval (or a recorded list of issues).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-self-consistency-proof/11-CONTEXT.md
@.planning/phases/11-self-consistency-proof/11-01-SUMMARY.md
@.planning/phases/11-self-consistency-proof/11-02-SUMMARY.md
@.planning/phases/11-self-consistency-proof/11-03-SUMMARY.md
@.planning/phases/11-self-consistency-proof/11-04-SUMMARY.md

**Dev server note:** use `npm run dev:auto` or `npm run dev:direct`. Plain `npm run dev` waits
for interactive confirmation and will hang a non-interactive shell. Never run `npm run build`
while a dev server is running — it makes every route 500 until the dev server is restarted.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real verification gate against the assembled phase</name>

  <read_first>
    - `./CLAUDE.md` §"Verifying Changes", §"Testing" and §"Build Optimization" — the gate order,
      why `npm run build` proves nothing here, and the known-baseline Jest failures.
    - All four plan SUMMARYs for this phase — so you can cross-check the claimed scope against
      the actual diff.
  </read_first>

  <action>
    Run `npm run lint`, then `npx tsc --noEmit`, then `npm test`, in that order, against the
    fully assembled phase. This is ROADMAP success criterion 5.

    Do NOT run `npm run build`. `next.config.js` sets `eslint.ignoreDuringBuilds` and
    `typescript.ignoreBuildErrors`, so a green build is not evidence of anything. Recording a
    passing build as verification would be a false negative of exactly the kind this milestone
    exists to eliminate.

    Then audit the phase's cumulative diff against the hard scope constraints. Run the guardrail
    checks in the acceptance criteria and record each result verbatim. This is the last chance
    to catch a scope breach before the phase closes, and several of the forbidden files are
    Phase 12's territory — a stray edit to `app/page.tsx` here creates the exact cross-phase
    conflict the roadmap sequenced this phase to avoid.

    If a Jest suite fails, establish whether it is a pre-existing baseline by checking out the
    phase's base commit in a scratch worktree, or by running that single suite with the phase's
    changes stashed, and record the comparison. `./CLAUDE.md` names
    `__tests__/performance/animation-load-testing.test.tsx` as flaky (it measures FPS inside
    jsdom) and notes several intentionally-skipped suites.
  </action>

  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npx tsc --noEmit` exits 0 with no diagnostics.
    - `npm test` reports no new failures; `__tests__/seo/award-count-consistency.test.ts` and
      `__tests__/integration/home-page-argument.test.tsx` both pass. Any failure present is
      shown to reproduce on the phase's base commit and is recorded as a pre-existing baseline.
    - `git diff --name-only <phase-base>..HEAD` contains none of: `app/page.tsx`,
      `app/services/services-client.tsx`, `app/opengraph-image.tsx`, `lib/data/projects.ts`,
      `__tests__/integration/home-page-argument.test.tsx`.
    - `grep -rn "2.5M+" app components lib | wc -l`, `grep -rn '\$50M' app components lib | wc -l`
      and `grep -rn "800+" app components lib | wc -l` each return the same counts as at the
      phase base commit — no sitewide figure was removed anywhere.
    - `grep -rnE '^[[:space:]]*6[[:space:]]*$' app/about/opengraph-image.tsx` returns nothing.
    - `git status --porcelain package.json package-lock.json` outputs nothing — the phase added
      no dependency.
    - The set of files changed by the phase is exactly: `app/about/opengraph-image.tsx`,
      `app/about/about-client.tsx`, `__tests__/seo/award-count-consistency.test.ts`, plus
      `.planning/` documents and phase SUMMARYs.
  </acceptance_criteria>

  <done>
    Lint, typecheck and Jest are green with no new failures. The cumulative phase diff violates
    none of the hard scope constraints, and every guardrail check is recorded verbatim.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the rendered /about awards cell</name>

  <read_first>
    - `.planning/phases/11-self-consistency-proof/11-03-SUMMARY.md` — the exact award strings,
      lead-in and judge-credential wording that shipped, so you can describe to the reviewer
      what they should be seeing.
    - `.planning/CREDIBILITY-COPY.md` §1 — the provenance table, so any wording question can be
      answered against the source rather than from memory.
  </read_first>

  <what-built>
    - `app/about/opengraph-image.tsx` now states `4` Design Awards instead of `6` (CRED-13,
      D-01) — closing the third recurrence of the v1.0 CRED-01 defect.
    - `/about`'s `4 / Design awards` proof-band cell now names all four awards with issuer and
      category inside its description slot (PRF-01, D-04/D-05), with the Webby rendered as a
      structurally separate judge credential (D-07) and no decorative glyph anywhere (D-06).
    - The proof band still has all four cells; `2.5M+`, `$50M` and `800+` are untouched (D-03).
    - `.planning/PROJECT.md`, `MILESTONES.md`, the v2.0 audit, `STATE.md` and the research index
      state that `Unbacked` means "absent from the deck", not "unsupported" (REC-01, D-09).
    - `__tests__/seo/award-count-consistency.test.ts` pins the count across five surfaces and
      fails the sweep if a new award-mentioning file appears (CRED-12, D-11/D-12/D-13).
  </what-built>

  <how-to-verify>
    1. Start the dev server with `npm run dev:auto` (NOT plain `npm run dev` — it waits for
       interactive confirmation).
    2. Open `http://localhost:3000/about` and scroll to the proof band (the four-figure row
       under the impact section heading).
    3. **Cell count and figure.** Confirm there are still exactly four cells, and that the
       awards cell still shows the large figure `4` above the label `Design awards`.
    4. **The four awards.** Confirm all four are readable under that label, each naming its
       issuer and category: two from The Davey Awards (Mobile Apps/Social and Mobile
       Apps/Lifestyle) and two from Vega Digital Awards (Best User Interface App/Experience and
       Best Lifestyle App). Cross-check against `.planning/CREDIBILITY-COPY.md` §1's table.
    5. **The Webby.** Confirm the Webby line reads unambiguously as a judging credential and
       cannot be mistaken for a fifth award or a win. Count what a stranger would count: it must
       come to four.
    6. **No glyphs.** Confirm there is no medal emoji, trophy, icon or list bullet anywhere in
       the band. `CREDIBILITY-COPY.md` §1's draft used 🥈🥈🥉🥉 and the design voice rejects
       them — the number over a context line is the whole signature.
    7. **Narrow width.** Resize to roughly 375px wide (or use device emulation). The band
       collapses from four columns to two. Confirm the awards cell is still legible — no
       overflow, no text spilling outside the cell, no collision with the neighbouring cell, and
       the four items still scannable as four.
    8. **Wide width.** Resize to a desktop width where the band is four columns across. This is
       the tight case: the awards cell is roughly a quarter of the content width. Confirm the
       award names wrap acceptably and the cell does not become a wall of text that unbalances
       the row against the other three one-line cells.
    9. **Dark mode.** Toggle the theme. Confirm every new line of award text is readable against
       the dark background — the muted text must not disappear.
    10. **OG image.** Open `http://localhost:3000/about/opengraph-image` directly. Confirm the
        stats row reads `4` above `Design Awards`, and that `2.5M+`, `$50M` and `800+` are all
        still present and unchanged.

    Report anything that looks wrong, including subjective layout judgements — the exact markup
    was Claude's Discretion under D-05 and this is the only point at which that discretion gets
    reviewed. Step 8 is the most likely place to find a problem.
  </how-to-verify>

  <acceptance_criteria>
    - Steps 3 through 10 are each explicitly confirmed or explicitly reported as an issue; the
      SUMMARY records a line per step, not a blanket approval.
    - The reviewer confirms the award count a stranger would arrive at is four, not five.
    - The reviewer confirms no glyph, emoji, icon or bullet marker appears in the band.
    - The reviewer confirms legibility at both ~375px and full desktop width, in light and dark
      mode.
    - The reviewer confirms `/about/opengraph-image` shows `4` and that all three sitewide
      figures are still present on it.
    - Any issue is recorded verbatim in the SUMMARY with the step number that surfaced it, and
      routed to a gap-closure plan rather than fixed inline in this plan.
  </acceptance_criteria>

  <resume-signal>Type "approved" to close the phase, or describe the issues found and the step number that surfaced each.</resume-signal>

  <done>
    A human has viewed the rendered `/about` band at two widths in both themes and the OG image,
    and has either approved the result or recorded specific issues for gap closure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer's browser → `localhost:3000` | The only boundary exercised. A local dev server serving the reviewer's own repository — no production credentials, no remote data, no third-party origin. |
| (no code boundary) | This plan has an empty `files_modified`. It creates and edits nothing, so it introduces no new attack surface of its own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-41 | Information Disclosure | local dev server | accept | `npm run dev:auto` binds localhost and serves only the repository's own public marketing content. The app runs without `.env.local`; only the newsletter API needs `LOOPS_API_KEY`, and no step in this verification touches it. Nothing sensitive is exposed. |
| T-11-42 | Repudiation | the human approval itself | mitigate | A blanket "looks fine" would leave no record of what was actually checked, and this checkpoint is the ONLY review the D-05 markup discretion receives. Mitigated by requiring a per-step confirmation line in the SUMMARY rather than a single approval token, so a later auditor can see which of the ten steps were genuinely exercised. |
| T-11-43 | Tampering | scope creep during verification | mitigate | The strongest temptation at a visual checkpoint is to nudge the markup inline while looking at it — which would ship an unreviewed, untested change after the gate ran. Mitigated by the empty `files_modified`, and by requiring any issue found to be routed to a gap-closure plan rather than fixed in this plan. |
| T-11-44 | Denial of Service | local build tooling | mitigate | Running `npm run build` while a dev server is up makes every route return 500 until the dev server restarts, which would look like a phase defect and send the reviewer chasing a phantom. Mitigated by explicitly forbidding `npm run build` in Task 1 and pointing the reviewer at `npm run dev:auto`. |
| T-11-SC | Tampering | npm supply chain | mitigate | This plan installs zero packages and modifies zero files. Enforced by the Task 1 acceptance criterion that `git status --porcelain package.json package-lock.json` outputs nothing. No `## Package Legitimacy Audit` is required — there is no package-manager install task. |
</threat_model>

<verification>
1. `npm run lint` → exit 0; `npx tsc --noEmit` → exit 0; `npm test` → no new failures.
2. `__tests__/seo/award-count-consistency.test.ts` passes in the full run.
3. `__tests__/integration/home-page-argument.test.tsx` passes, unmodified.
4. The phase's cumulative changed-file list contains no forbidden path.
5. All three sitewide figure counts match the phase base commit.
6. A human has confirmed the ten verification steps and either approved or filed issues.
</verification>

<success_criteria>
- ROADMAP criterion 5 satisfied: `npm run lint` → `npx tsc --noEmit` → `npm test` all pass, with
  a build explicitly NOT used as evidence.
- No hard scope constraint was breached anywhere in the phase.
- The rendered `/about` awards cell is human-approved at two widths in both themes, or specific
  issues are recorded for gap closure.
- A stranger reading `/about` counts four awards, with the Webby legible as a judge credential.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-proof/11-05-SUMMARY.md` when done.
Record: the verbatim output of each guardrail check from Task 1; the per-step human verification
result from Task 2 (one line per step, 3 through 10); and either the approval or the issue list
with the step number that surfaced each.
</output>
