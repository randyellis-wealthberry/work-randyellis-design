---
phase: 11-self-consistency-proof
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - __tests__/seo/award-count-consistency.test.ts
autonomous: true
requirements: [CRED-12]
decisions: [D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "A regression test fails if any surface that states an award count disagrees with the others (D-11)"
    - "Every assertion is anchored to a data shape or a semantic phrase, never to a bare `4` — `4` collides with grid-cols-4, h-4 w-4 and sm:text-4xl inside the very files under test (D-12)"
    - "Adding a new file that mentions awards fails the test until its count is pinned, so the surface set cannot silently grow"
    - "The test never reads `.planning/`, so the audit record it was written to protect is out of its reach (D-13)"
    - "The test pins the final post-PRF-01 shape, having been written after it landed (D-14)"
    - "The Webby cannot be counted among the four on any surface (D-07)"
  artifacts:
    - path: "__tests__/seo/award-count-consistency.test.ts"
      provides: "Award-count consistency regression test across all five stating surfaces plus a surface-set sweep"
      contains: "collectSourceFiles"
      min_lines: 120
  key_links:
    - from: "__tests__/seo/award-count-consistency.test.ts"
      to: "lib/seo/json-ld.ts buildPersonSchema"
      via: "real named import, asserting award array length"
      pattern: "buildPersonSchema\\(\\)\\.award"
    - from: "__tests__/seo/award-count-consistency.test.ts"
      to: "lib/data/retainer.ts PROOF_EXHIBITS"
      via: "real named import, structural lookup by context field"
      pattern: "PROOF_EXHIBITS"
    - from: "__tests__/seo/award-count-consistency.test.ts"
      to: "app/, components/, lib/ source tree"
      via: "collectSourceFiles walker reused from no-legacy-schema.test.ts"
      pattern: "collectSourceFiles\\(\\["
---

<objective>
Pin the award count as consistent across every surface that states it, so a figure cannot drift
on one surface again.

Purpose: `6` survived on `/about`'s OG image through two milestone audits because the count is
hand-typed independently into five files with no shared source. There is no single source to
guard, so the test guards agreement between them instead.

D-11 inverts the original design: this test does not ban strings, it asserts consistency.
D-12 requires structural verification against the data shape — a bare `"4"` match is worthless
here because `grid-cols-4`, `sm:grid-cols-4`, `h-4 w-4` and `sm:text-4xl` all appear inside the
exact files under test. D-14 puts this plan last so it pins the final post-PRF-01 shape rather
than a shape that does not exist yet.

Output: `__tests__/seo/award-count-consistency.test.ts`, green, and demonstrated to go red when
any single surface drifts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-self-consistency-proof/11-CONTEXT.md
@.planning/phases/11-self-consistency-proof/11-PATTERNS.md
@.planning/phases/11-self-consistency-proof/11-03-SUMMARY.md

**Scope guardrails (hard):**
- This plan creates ONE new test file. It modifies no application code. If an assertion fails
  because a surface is genuinely wrong, that is a finding to report — not a licence to edit the
  surface. Plans 11-01 and 11-03 own those files and have already run.
- Do NOT touch `__tests__/integration/home-page-argument.test.tsx`. It asserts `2.5M+`, `$50M`
  and `800+` are present and is correct as written.
- Do NOT assert anything about `2.5M+`, `$50M` or `800+`. This test is about the award count
  only; those figures are already covered by `home-page-argument.test.tsx`.
- `tsconfig.json` excludes `__tests__/`, so `npx tsc --noEmit` does NOT typecheck this file.
  Type errors here surface only when Jest runs it. Do not treat a clean `tsc` as evidence the
  test compiles.

**Location choice (Claude's Discretion, resolved):** `__tests__/seo/award-count-consistency.test.ts`.
`__tests__/seo/` is where the `collectSourceFiles` precedent lives and where JSON-LD and
OG-image correctness are already tested (`json-ld.test.tsx`, `blog-post-json-ld.test.tsx`,
`sw-kill-switch.test.ts`). Its naming convention is kebab-case describing the pinned invariant.
No `__tests__/credibility/` directory exists; creating one for a single file adds a directory
without adding clarity.
</context>

<interfaces>
<!-- Verified at plan time. Confirm each against the live files via <read_first>. -->

**Importable, so assert structurally via real imports (strongest form of D-12):**
- `lib/seo/json-ld.ts` exports `buildPersonSchema()`, returning an object whose `award` field is
  a four-entry string array. Already imported by the passing suite
  `__tests__/seo/json-ld.test.tsx`, so the module is proven to load under Jest.
- `lib/data/retainer.ts` exports `PROOF_EXHIBITS: ReadonlyArray<{ value: string; context: string }>`.
  The awards row is `{ value: "4", context: "Design awards won" }`. Pure data module, no React.

**Not importable, so assert via anchored source read:**
- `app/about/about-client.tsx` — `achievements` is a local `const` inside a `"use client"`
  component and is not exported. Importing it would pull the whole client module graph (motion,
  accordion, CTASection) into a data-only test and make it fail for reasons unrelated to award
  counts. Read the source text and anchor on the object literal instead.
- `app/about/opengraph-image.tsx` — a `next/og` edge route with no exported data. Verified: the
  awards value is the ONLY line in the file matching `^\s*\d+\s*$`. Every other numeric literal
  is quoted CSS (`"36px"`, `"14px"`) or a decimal (`0.8`). That makes the bare-line match
  precise and collision-free.
- `app/about/page.tsx` — exports a `Metadata` object whose `openGraph.description` contains the
  phrase `4 awards won`. Importing it would pull in `AboutClient`; read the source and anchor
  on the phrase.

**The walker to reuse (D-13)** — `collectSourceFiles(dirs: string[]): string[]` in
`__tests__/seo/no-legacy-schema.test.ts`. It takes an explicit root list, walks recursively,
skips `node_modules`, `.next`, `out`, `.git` and `dist`, and collects `.ts`/`.tsx`/`.mdx` files
excluding `.d.ts`. Every existing caller passes exactly the three roots `app`, `components`,
`lib` joined onto `process.cwd()`. Because `.planning/` and root `*.md` are never roots, the
audit record is outside its reach by construction, not by a skip-list entry.

Repo convention: SEO test files are self-contained and each defines its own walker inline. No
shared test-helper module exists. Copy the function rather than trying to import it.

**The complete set of files under `app/`, `components/`, `lib/` matching `/award/i`** — verified
by grep at plan time, exactly seven:

| Path | States a count? |
|------|-----------------|
| `app/about/about-client.tsx` | yes — `value: "4"` plus the four named awards added by plan 11-03 |
| `app/about/opengraph-image.tsx` | yes — the bare value text node above the `Design Awards` label |
| `app/about/page.tsx` | yes — `4 awards won` inside `openGraph.description` |
| `lib/data/retainer.ts` | yes — the `Design awards won` `PROOF_EXHIBITS` row |
| `lib/seo/json-ld.ts` | yes — a four-entry `award` array |
| `components/core/animated-number-basic.tsx` | no — dead code, zero imports anywhere in the repo, renders nothing, so it cannot disagree with anything |
| `components/ui/reading-progress.tsx` | no — `Award` is a `lucide-react` icon import used for a reading-progress milestone label, not a design-award count |

`app/opengraph-image.tsx` (the ROOT OG image) is deliberately absent from this list: it has a
three-cell stats row (`2.5M+`, `$50M`, `800+`) and no awards cell at all, confirmed by full-file
read. This matters for how CRED-12 is worded in the source documents — `REQUIREMENTS.md` says
"both OG generators" and ROADMAP criterion 3 says "all four surfaces", but the root OG generator
states no award count, so there is nothing there to pin. Asserting a count on a silent surface
would assert a claim the surface does not make. The sweep covers it correctly instead: the root
OG does not currently match `/award/i`, so adding an awards cell to it would make it appear in
the swept set, fail the allow-list assertion, and force whoever added it to pin its count.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the award-count consistency test</name>

  <read_first>
    - `__tests__/seo/no-legacy-schema.test.ts` — read the FULL file. Take `collectSourceFiles`
      from it verbatim (D-13) and model the "read every file, collect violations, assert the
      violations array is empty" call-site shape from its Test 2 block.
    - `app/about/about-client.tsx` — the post-PRF-01 `achievements` array. Read the actual
      shipped field names, ordering and award strings; plan 11-03 chose the exact wording under
      Claude's Discretion, so it may differ from any draft. Its SUMMARY records what shipped.
    - `.planning/phases/11-self-consistency-proof/11-03-SUMMARY.md` — the exact `awards` strings,
      `description` lead-in and `judgeCredential` wording that plan 11-03 shipped.
    - `app/about/opengraph-image.tsx` — the post-11-01 awards cell.
    - `app/about/page.tsx` — the `openGraph.description` metadata string.
    - `lib/data/retainer.ts` and `lib/seo/json-ld.ts` — the two importable surfaces.
    - `jest.config.js` — confirms `moduleNameMapper` maps `@/` to the repo root, so
      `@/lib/seo/json-ld` resolves.
  </read_first>

  <action>
    Create `__tests__/seo/award-count-consistency.test.ts` with a file-header doc comment
    explaining what invariant it pins and why bare-string matching is forbidden here (D-12),
    matching the header style of `no-legacy-schema.test.ts`.

    Import `fs` and `path`, plus the two real data imports: `buildPersonSchema` from
    `@/lib/seo/json-ld` and `PROOF_EXHIBITS` from `@/lib/data/retainer`. Do not import
    `@testing-library/react` — nothing renders here.

    Declare a single module-level constant `EXPECTED_AWARD_COUNT = 4`. Every surface assertion
    compares against this one constant rather than against a hard-coded literal per test. That
    is the mechanism that makes the suite a *consistency* test rather than five independent
    count tests (D-11): if Randy wins a fifth award, exactly one constant changes and the suite
    then names every surface that has not been updated.

    Copy `collectSourceFiles` in verbatim, with its doc comment, below the describes — same
    placement as in the precedent file.

    Write four describe blocks:

    **Block 1 — importable surfaces, asserted structurally.** Assert `buildPersonSchema().award`
    is an array whose length equals `EXPECTED_AWARD_COUNT`; that every entry is a non-empty
    string; that exactly two entries match `/Davey Awards/` and exactly two match
    `/Vega Digital Awards/`; and that NO entry matches `/webby/i` (D-07 — the judge credential
    must never be counted among the awards). Then assert `PROOF_EXHIBITS` contains exactly one
    row whose `context` is `"Design awards won"`, found by field lookup and not by array index,
    and that its `value` equals `String(EXPECTED_AWARD_COUNT)`.

    **Block 2 — source-text surfaces, asserted with shape-anchored extraction.** For each, read
    the file with `fs.readFileSync` at a `path.join(process.cwd(), …)` literal path.

    For `app/about/about-client.tsx`: locate the index of `label: "Design awards"` and assert it
    is found. Slice backwards to the nearest preceding `{` using `lastIndexOf` — this isolates
    the award object without depending on indentation, which Prettier controls. Assert that
    slice matches `/value:\s*"4"/`. Then locate `awards: [` searching FORWARD from the label
    index, slice to the next `]`, and count the quoted string literals inside that slice with a
    `/"[^"]*"/g` match; assert the count equals `EXPECTED_AWARD_COUNT`. Assert that slice does
    NOT match `/webby/i`. Finally assert the file's `/webby/i` match exists but its index falls
    OUTSIDE the `awards` array slice — that is the structural proof the judge credential is not
    a fifth list item (D-07).

    For `app/about/opengraph-image.tsx`: split on newlines, filter to lines matching
    `/^\s*\d+\s*$/`, assert exactly one such line exists and that its trimmed value equals
    `String(EXPECTED_AWARD_COUNT)`. Then assert a line containing `Design Awards` appears within
    the following 12 lines, proving the number you matched is the awards figure and not some
    unrelated bare digit that appeared later.

    For `app/about/page.tsx`: collect every match of `/(\d+)\s+awards?\s+won/gi`, assert at least
    one match exists, and assert every captured group equals `String(EXPECTED_AWARD_COUNT)`.
    Anchoring on the phrase rather than the digit is what keeps this from colliding with
    unrelated numbers in the metadata block.

    **Block 3 — surface-set sweep, using the walker (D-13).** Call `collectSourceFiles` with the
    three roots `app`, `components`, `lib` joined onto `process.cwd()`. Read each file and keep
    those whose content matches `/award/i`. Convert to repo-relative POSIX paths, sort, and
    assert deep equality against the seven-entry allow-list in the `<interfaces>` table above.
    Write the allow-list as an array with an inline comment on each of the two non-counting
    entries explaining why it is listed (dead code with zero imports; lucide icon import).
    Give the assertion a failure message that tells the author what to do: a new file mentioning
    awards must either state 4 and be added to this test's surface assertions, or be added to
    the allow-list with a comment justifying why it states no count. That message is the whole
    value of this block — it is what stops the surface set growing silently.

    **Block 4 — cross-surface agreement.** One test that binds the three independently-derived
    counts together in a single assertion: the length of `buildPersonSchema().award`, the
    numeric parse of the `PROOF_EXHIBITS` `Design awards won` value, and the count of string
    literals in `about-client.tsx`'s `awards` array must all be equal. This is the assertion
    that most directly encodes D-11 — it fails when the surfaces disagree with EACH OTHER, not
    merely when one disagrees with a constant.

    Never write an assertion that greps a bare `4`, `"4"`, or `/4/` against whole file contents.
    Every numeric assertion must be anchored to a field name, a phrase, an array literal, or a
    whole-line match (D-12).
  </action>

  <verify>
    <automated>npx jest __tests__/seo/award-count-consistency.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx jest __tests__/seo/award-count-consistency.test.ts` passes with zero failures and
      zero skipped tests.
    - `grep -c "collectSourceFiles" __tests__/seo/award-count-consistency.test.ts` returns at
      least `2` — the walker is defined and called (D-13).
    - `grep -c "EXPECTED_AWARD_COUNT" __tests__/seo/award-count-consistency.test.ts` returns at
      least `6` — the shared constant is genuinely shared across surfaces, not decorative.
    - `grep -c "\.planning" __tests__/seo/award-count-consistency.test.ts` returns `0` — the test
      never names the audit record as a root or a path (D-13).
    - `grep -c "process.env\|process.argv" __tests__/seo/award-count-consistency.test.ts` returns
      `0` — every filesystem root is a `process.cwd()` literal, never externally supplied
      (T-11-31).
    - `grep -ci "webby" __tests__/seo/award-count-consistency.test.ts` returns at least `2` —
      the D-07 judge-credential exclusion is asserted on both the JSON-LD array and the
      about-client awards literal.
    - `grep -c "buildPersonSchema" __tests__/seo/award-count-consistency.test.ts` and
      `grep -c "PROOF_EXHIBITS" __tests__/seo/award-count-consistency.test.ts` each return at
      least `2` — both importable surfaces are asserted via real imports, not regex.
    - `grep -nE 'toContain\("4"\)|includes\("4"\)|/4/' __tests__/seo/award-count-consistency.test.ts`
      returns nothing — no bare-`4` matching survived (D-12).
    - `grep -c "2.5M\|800+\|50M" __tests__/seo/award-count-consistency.test.ts` returns `0` —
      this test makes no claim about the three sitewide figures.
    - `git status --porcelain` lists exactly one path,
      `__tests__/seo/award-count-consistency.test.ts`, and it is untracked/new.
  </acceptance_criteria>

  <done>
    `__tests__/seo/award-count-consistency.test.ts` exists and passes. It asserts five stating
    surfaces plus a seven-file surface-set sweep, uses real imports where the data is exported
    and shape-anchored extraction where it is not, shares one count constant across all of them,
    and never matches a bare `4`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the test actually fails when a surface drifts</name>

  <read_first>
    - `__tests__/seo/award-count-consistency.test.ts` — the file you just wrote.
    - `lib/data/retainer.ts` — the mutation target. Confirm it is unmodified relative to HEAD
      before touching it; no plan in this phase modifies it, so `git checkout --` will restore
      it exactly.
  </read_first>

  <action>
    A consistency test that cannot go red is decoration. Prove each detection path independently
    by mutating one surface at a time, running only this suite, confirming it fails, and
    reverting before the next mutation. Never run two mutations at once — a single failure would
    not tell you which path caught it.

    Before starting, confirm `git status --porcelain lib/data/retainer.ts` and
    `git status --porcelain app/about/opengraph-image.tsx` both output nothing, so
    `git checkout --` restores the exact HEAD content rather than discarding real work.

    Run four mutations, reverting with `git checkout -- <path>` after each and confirming
    `git status --porcelain <path>` is empty again before the next:

    1. In `lib/data/retainer.ts`, change the `Design awards won` row's `value` from `"4"` to
       `"5"`. Expect the retainer assertion AND the cross-surface agreement test to fail.
    2. In `app/about/opengraph-image.tsx`, change the awards value text node from `4` back to
       `6`. Expect the OG assertion to fail. This is the exact defect CRED-13 fixed and the
       single most important path to prove.
    3. In `app/about/about-client.tsx`, delete one entry from the `awards` array. Expect the
       about-client literal-count assertion AND the cross-surface agreement test to fail.
    4. In `app/about/about-client.tsx`, move the Webby judge text INTO the `awards` array as a
       fifth entry. Expect both the count assertion and the `/webby/i` exclusion to fail
       (D-07).

    Record, per mutation, which named test(s) failed. If a mutation does NOT produce a failure,
    that detection path is broken — fix the test in this task, do not record the mutation as
    inconclusive.

    After all four, confirm the working tree is clean of mutations and run the suite once more
    to confirm it is green again. Do not use `git stash` for this — an explicit
    `git checkout -- <path>` per file is unambiguous and leaves nothing on a stash stack to be
    forgotten.
  </action>

  <verify>
    <automated>npx jest __tests__/seo/award-count-consistency.test.ts &amp;&amp; test -z "$(git status --porcelain lib/data/retainer.ts app/about/opengraph-image.tsx app/about/about-client.tsx)"</automated>
  </verify>

  <acceptance_criteria>
    - Each of the four mutations produced at least one FAILING named test, and the SUMMARY
      records the mutation and the exact failing test name for each.
    - Mutation 2 (OG `4`→`6`) specifically fails the OG-image assertion — the CRED-13 defect is
      demonstrably pinned.
    - Mutation 4 (Webby as a fifth `awards` entry) fails the `/webby/i` exclusion assertion —
      D-07 is demonstrably pinned.
    - After all mutations, `git status --porcelain lib/data/retainer.ts app/about/opengraph-image.tsx app/about/about-client.tsx`
      outputs nothing.
    - `git stash list` shows no new stash entry created by this task.
    - `npx jest __tests__/seo/award-count-consistency.test.ts` passes on the restored tree.
    - `git status --porcelain` lists exactly one path,
      `__tests__/seo/award-count-consistency.test.ts`.
  </acceptance_criteria>

  <done>
    All four detection paths are demonstrated to go red under a targeted mutation and green
    again after revert. The working tree contains exactly one new file and no leftover mutation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → local filesystem | The test walks and reads the repository with `fs.readdirSync` / `fs.readFileSync`. This is the only real boundary in the plan and the only one worth mitigating. |
| (no runtime boundary) | The file lives under `__tests__/`, is never imported by application code, is not in the Next.js build graph, and is never served. It reaches no user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-31 | Tampering / path traversal | `collectSourceFiles` filesystem walk | mitigate | An fs-walking test that accepted an externally-supplied root could read outside the repository. Mitigated three ways: every root is a literal `path.join(process.cwd(), "app" \| "components" \| "lib")` with no `process.env` or `process.argv` input (enforced by an acceptance-criterion grep returning `0`); the walker recurses only on `entry.isDirectory()` from `withFileTypes: true`, which reports `false` for symlinks, so a symlinked directory pointing outside the repo is not traversed; and it filters to `.ts`/`.tsx`/`.mdx` while skipping `node_modules`, `.next`, `out`, `.git` and `dist`. |
| T-11-32 | Information Disclosure | the audit record in `.planning/` | mitigate | The document set REC-01 just corrected must not be scanned, or a future "fix the violations" pass could be pointed at the historical record and erase it. `.planning/` is excluded by construction — it is never passed as a root (D-13) — and reinforced by the acceptance criterion that the string `.planning` does not appear anywhere in the test file. |
| T-11-33 | Tampering | mutation-testing in Task 2 | mitigate | Task 2 deliberately edits three production files to prove the test detects drift. Leaving a mutation behind would ship a false award count — the exact defect this phase exists to close. Mitigated by requiring one mutation at a time, an explicit `git checkout -- <path>` revert per file rather than `git stash`, a pre-check that each target is clean at HEAD before mutating, and a final acceptance criterion that `git status --porcelain` on all three targets outputs nothing and no stash entry was created. |
| T-11-34 | Denial of Service | CI runtime | accept | The walk covers `app`, `components` and `lib` only and reads each file once — the same cost `no-legacy-schema.test.ts` already pays four times over on every run. No meaningful additional CI cost. |
| T-11-SC | Tampering | npm supply chain | mitigate | This plan installs zero packages. It uses only Node built-ins (`fs`, `path`) and Jest globals already configured in `jest.config.js`. Enforced by `git status --porcelain package.json package-lock.json` outputting nothing. No `## Package Legitimacy Audit` is required — there is no package-manager install task. |
</threat_model>

<verification>
1. `npx jest __tests__/seo/award-count-consistency.test.ts` → passes, no skipped tests.
2. `npm test` → the new suite runs as part of the full run with no new failures elsewhere.
3. Four mutation runs each produced a named failing test; all four reverted clean.
4. `grep -c "\.planning" __tests__/seo/award-count-consistency.test.ts` → `0`.
5. `grep -c "process.env\|process.argv" __tests__/seo/award-count-consistency.test.ts` → `0`.
6. `git status --porcelain` → exactly one path, the new test file.
7. Note: `npx tsc --noEmit` does NOT cover this file — `tsconfig.json` excludes `__tests__/`.
   Jest is the only gate that compiles it, which is why Task 1's verify runs Jest directly.
</verification>

<success_criteria>
- A regression test pins award-count consistency across every surface that states a count
  (ROADMAP criterion 3, D-11).
- Every assertion is structural — anchored to an imported value, a field name, a phrase, an
  array literal, or a whole-line match. No bare `4` matching anywhere (D-12).
- The `collectSourceFiles` walker from `no-legacy-schema.test.ts` is reused, and `.planning/`
  is outside its roots (D-13).
- The surface set cannot grow silently: a new file mentioning awards fails the sweep with an
  instructive message.
- The Webby is structurally excluded from the award count on both the JSON-LD and the
  about-client surface (D-07).
- The test was written after PRF-01 landed and pins the shipped shape (D-14).
- All four detection paths are demonstrated to go red under mutation.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-proof/11-04-SUMMARY.md` when done.
Record: the final surface set and the assertion method used for each; the seven-entry allow-list
as shipped; the four mutation results with the exact failing test name for each; and explicit
confirmation that `app/opengraph-image.tsx` (root OG) states no award count and is covered by
the sweep rather than by a count assertion — this is the honest reconciliation of
`REQUIREMENTS.md`'s "both OG generators" and ROADMAP criterion 3's "all four surfaces" wording
against what the code actually does.
</output>
