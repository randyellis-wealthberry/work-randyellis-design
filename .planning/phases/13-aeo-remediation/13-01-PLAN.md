---
phase: 13-aeo-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/llms.txt/route.ts
  - app/rss.xml/route.ts
  - app/layout.tsx
  - __tests__/seo/llms-txt.test.ts
  - __tests__/seo/rss-feed.test.ts
autonomous: true
requirements: [T-04, T-06]
tags: [aeo, llms-txt, rss, tdd]

must_haves:
  truths:
    - "GET /llms.txt returns 200 text/plain Markdown: H1 identity line, blockquote summary, Services / Case Studies / Writing / About sections with absolute links derived from lib data, not hand-typed copies"
    - "GET /rss.xml returns valid RSS 2.0 with one <item> per blog article (title, link, guid, pubDate, description), dates RFC-822"
    - "Root layout <head> carries <link rel='alternate' type='application/rss+xml'>"
  artifacts:
    - path: "app/llms.txt/route.ts"
      provides: "llms.txt route handler generated from PROJECTS + blog data + constants"
    - path: "app/rss.xml/route.ts"
      provides: "RSS 2.0 feed from blog article data"
    - path: "__tests__/seo/llms-txt.test.ts"
      provides: "asserts identity line, all 9 project links, all 4 post links"
    - path: "__tests__/seo/rss-feed.test.ts"
      provides: "asserts channel fields + one item per post + RFC-822 pubDate"
---

<objective>
Give the AI crawlers robots.ts already welcomes (D-21) a machine map (llms.txt)
and a publication feed (RSS 2.0 + rel=alternate). Audit T-04/T-06: both were
absent repo-wide.

Content is generated from `lib/data/projects.ts`, blog data, and
`lib/constants.ts` so it cannot drift from the site the way hand-typed copies
would (the Phase-13 date-drift lesson, T-03).
</objective>

<tasks>
1. `app/llms.txt/route.ts` — static route handler (`export const dynamic =
   "force-static"`), text/plain. Structure per llmstxt.org: `# Randy Ellis`,
   blockquote positioning summary, then Services, Case Studies (9 projects,
   one line each: name — one-line description — absolute URL), Writing (4
   posts), About/Contact (booking URL, sameAs profiles).
2. `app/rss.xml/route.ts` — static route handler, application/rss+xml.
   Channel: title/link/description/language/lastBuildDate. Items from blog
   article data (post-13-03 single source; use whatever `getBlogArticles()`
   exposes at execution time).
3. `app/layout.tsx` — add `alternates.types` RSS entry (or `<link>` in head)
   pointing at /rss.xml.
4. Tests first (tdd_mode): llms-txt + rss-feed suites calling the route
   handlers' GET directly.
</tasks>

<verification>
npm run lint && npx tsc --noEmit && npm test -- __tests__/seo/
curl of /llms.txt and /rss.xml on the preview deploy at phase gate (13-05).
</verification>
