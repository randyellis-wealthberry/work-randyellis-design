---
phase: 13-aeo-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/core/accordion.tsx
  - app/projects/rambis-ui/rambis-client.tsx
  - lib/seo/json-ld.ts
  - app/page.tsx
  - __tests__/seo/accordion-ssr.test.tsx
autonomous: true
requirements: [T-01, T-02]
tags: [aeo, accordion, faq-schema, ssr, tdd]

must_haves:
  truths:
    - "Collapsed items in components/core/accordion.tsx keep children mounted (height-collapsed, aria-hidden/inert), so home FAQ answers and /blog archive post links are present in server HTML (T-01)"
    - "The Rambis UI Radix accordion renders collapsed content via forceMount + CSS collapse (T-01)"
    - "Homepage emits a FAQPage JSON-LD node whose Question/acceptedAnswer text matches the visible FAQS array verbatim — same source constant, no second copy (T-02)"
    - "The stale comment at app/page.tsx:59-61 claiming FAQ structured data exists is replaced by one that is true"
    - "Visible At Zero holds: no content re-hidden behind JS; prefers-reduced-motion unaffected"
  artifacts:
    - path: "components/core/accordion.tsx"
      provides: "AccordionItem content always mounted; motion animates height 0<->auto; overflow hidden; aria-hidden when collapsed"
    - path: "lib/seo/json-ld.ts"
      provides: "buildFaqPageSchema(faqs) — D-08 amended (see STATE.md 2026-08-29): FAQPage permitted because the Q&A is server-rendered visible content"
    - path: "__tests__/seo/accordion-ssr.test.tsx"
      provides: "renderToString asserts collapsed accordion children are in the markup for core accordion; FAQPage schema matches FAQS"
---

<objective>
Collapsed accordions currently unmount their children
(`<AnimatePresence>{isExpanded && …}` at components/core/accordion.tsx:127) —
the homepage's ~600-word FAQ and the /blog archive's per-post links never reach
crawlers. Mount always, animate height instead. Then add the FAQPage schema the
visible Q&A now earns.

Note the /blog archive fix comes free: blog-archive-accordion.tsx composes the
core accordion, so no edit to it is needed — verify, don't modify.
</objective>

<tasks>
1. Test first: SSR-render (react-dom/server renderToString) an accordion with a
   collapsed item; assert child text present in markup with aria-hidden.
2. components/core/accordion.tsx — keep children mounted; motion.div
   animate={{height: isExpanded ? "auto" : 0}} with overflow:hidden,
   aria-hidden={!isExpanded}; preserve existing variants/transition props.
3. rambis-client.tsx — Radix Accordion.Content forceMount + data-state CSS
   collapse (check components/ui/accordion.tsx for where the animation lives).
4. lib/seo/json-ld.ts — buildFaqPageSchema; mount in app/page.tsx (or the
   server page after 13-04's split — whichever exists at execution time),
   fed from the same FAQS constant the accordion renders. Fix stale comment.
5. Verify /blog archive links appear in SSR output via the same test approach.
</tasks>

<verification>
npm run lint && npx tsc --noEmit && npm test
Raw-HTML curl checks land in 13-05.
</verification>
