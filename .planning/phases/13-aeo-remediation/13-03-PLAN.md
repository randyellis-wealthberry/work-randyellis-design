---
phase: 13-aeo-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils/blog-data.ts
  - app/blog/claude-obsidian-workflows/page.mdx
  - app/blog/create-professional-videos-claude-code-guide/page.mdx
  - app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx
  - app/blog/profits-not-pixels/page.mdx
  - __tests__/seo/blog-date-consistency.test.ts
autonomous: true
requirements: [T-03, T-07, T-12]
tags: [seo, blog, metadata, dates, tdd]

must_haves:
  truths:
    - "Each post has exactly one authoritative publication date; blog-data.ts agrees with the MDX on all 4 (today 3 of 4 disagree; MDX byline/JSON-LD dates win per roadmap constraint)"
    - "create-professional-videos' internal 2024-12-20 vs 2024-12-15 self-disagreement resolved"
    - "All 4 posts build metadata via createArticleMetadata() (lib/metadata.ts:127, currently dead code): per-post OG title/description/url/publishedTime/image + twitter card (T-07)"
    - "The one post with no openGraph block (exploring-the-intersection…) emits og:type=article with dates"
    - "A regression test walks all 4 posts and asserts blog-data.ts date === MDX openGraph.publishedTime === BlogPostJsonLd datePublished"
  artifacts:
    - path: "__tests__/seo/blog-date-consistency.test.ts"
      provides: "cross-source date agreement pinned for every post, structurally (imports the modules, no string grep)"
---

<objective>
Three sources state each post's date (mock listing array → sitemap+archive;
MDX metadata → OG; BlogPostJsonLd props → schema) and they disagree on 3 of 4
posts — worst case 9 months apart on profits-not-pixels. Converge on the MDX
dates, adopt the existing-but-unused createArticleMetadata() helper so OG gaps
close in the same pass, and pin agreement with a test so a fifth post can't
reintroduce drift.

Junk keys in MDX metadata exports (date/author/tags/slug at top level) are
silently dropped by Next — move what matters into real Metadata fields, delete
the rest.
</objective>

<tasks>
1. Test first: blog-date-consistency suite (drives all later edits red→green).
2. Correct blog-data.ts publishedDate to match MDX for the 3 divergent posts;
   resolve create-professional-videos' internal split (pick the byline date).
   Update the "Mock blog articles data" comment to name blog-data.ts's actual
   remaining role (listing copy + sort), note dates are test-pinned to MDX.
3. Rewrite each MDX metadata export to use createArticleMetadata() with the
   post's real image where one exists (each already passes imageUrl to
   BlogPostJsonLd — reuse it).
4. Confirm sitemap picks up corrected dates (it reads getBlogArticles()).
</tasks>

<verification>
npm run lint && npx tsc --noEmit && npm test
</verification>
