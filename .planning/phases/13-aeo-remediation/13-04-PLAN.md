---
phase: 13-aeo-remediation
plan: 04
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - app/page.tsx
  - app/home-client.tsx
  - lib/seo/json-ld.ts
  - lib/metadata.ts
  - app/projects/waffle/page.tsx
  - app/about/about-client.tsx
  - components/seo/related-content.tsx
  - app/blog/layout.tsx
  - __tests__/seo/sitemap.test.ts
  - __tests__/integration/home-page-argument.test.tsx
  - SEO_IMPLEMENTATION.md
  - SEO_OPTIMIZATION_REPORT.md
autonomous: true
requirements: [T-08, T-09, T-10, T-13, T-14, T-16]
tags: [seo, metadata, entity, cleanup]

must_haves:
  truths:
    - "app/page.tsx is a server component exporting homepage metadata (title/description/canonical/OG/twitter) and rendering app/home-client.tsx; the client content moved verbatim (T-09)"
    - "home-page-argument.test.tsx green with only its import updated"
    - "Person JSON-LD carries email: hello@randyellis.design; resume PDF linked from /about (T-08)"
    - "/projects/waffle uses projectMetadata + emits CreativeWork JSON-LD like the other 8 (T-10)"
    - "projectMetadata omits openGraph.images entirely when no raster image resolves, so the file-convention OG fallback applies; skills' .svg is treated as no-image (T-10)"
    - "WebSite.potentialAction removed (T-13)"
    - "related-content.tsx + its commented import deleted; getMetadataBase deleted; SEO_IMPLEMENTATION.md + SEO_OPTIMIZATION_REPORT.md removed (superseded by .planning/research/SEO-AEO-AUDIT.md); sitemap test asserts 9 static pages (T-14)"
    - "not-found.tsx no longer canonicals to / (T-16): set alternates.canonical null/undefined via its own metadata"
  key_links:
    - from: "app/page.tsx"
      to: "app/home-client.tsx"
      via: "import"
      pattern: "home-client"
---

<objective>
Close the entity and metadata loose ends: the homepage finally follows the
repo's own server-page + client-component convention and declares its own
metadata; the Person entity becomes contactable; Waffle stops being the one
project page without CreativeWork; empty OG arrays stop suppressing the
file-convention fallback; dead code and stale docs go.

Phase-12 note (recorded in STATE.md): FEATURED_SLUGS and the Selected-work
paragraph move to app/home-client.tsx.
</objective>

<tasks>
1. Split homepage: move content to app/home-client.tsx ("use client"), new
   server app/page.tsx with metadata (createPageMetadata, path "/") + FAQPage
   schema mount from 13-02 if it landed client-side. Update test import.
2. lib/seo/json-ld.ts — add email to Person.
3. app/about/about-client.tsx — link /randy-ellis-resume.pdf (download
   affordance near certifications/contact CTA; match existing styles).
4. Waffle: projectMetadata + CreativeWork/Breadcrumb JSON-LD (mirror
   echo/page.tsx). Keep its bespoke opengraph.png via project data or explicit
   override.
5. lib/metadata.ts — projectOgImage: only raster formats count; spread
   images into openGraph conditionally (never images: []). Delete
   getMetadataBase.
6. Delete WebSite.potentialAction, related-content.tsx (+ commented import in
   app/blog/layout.tsx), both stale root SEO docs. Fix sitemap.test.ts count.
7. not-found.tsx: export metadata with its own canonical handling + noindex.
</tasks>

<verification>
npm run lint && npx tsc --noEmit && npm test
</verification>
