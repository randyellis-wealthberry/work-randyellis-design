---
phase: 13-aeo-remediation
plan: 05
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified: []
autonomous: true
requirements: []
tags: [phase-gate, verification]

must_haves:
  truths:
    - "npm run lint, npx tsc --noEmit, npm test all pass (build is NOT a gate)"
    - "Production build succeeds and a local `next start` serves: /llms.txt (200, contains all 9 project URLs), /rss.xml (200, 4 items), / (FAQ answer text present in raw HTML, FAQPage JSON-LD present), /blog (4 /blog/<slug> hrefs in raw HTML), /projects/rambis-ui (accordion content in raw HTML)"
    - "ROADMAP.md progress row, STATE.md position, and 13-0X-SUMMARY.md files updated"
---

<objective>
Phase gate: the full verify chain plus raw-HTML proof of the four AEO success
criteria against a locally-served production build (curl, no JS), mirroring how
the audit measured the live site.
</objective>

<verification>
1. npm run lint
2. npx tsc --noEmit
3. npm test
4. npm run build && npx next start -p 3100 (background) → curl checks above → kill
5. Update .planning docs; mark phase complete
</verification>
