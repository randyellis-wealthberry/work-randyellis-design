---
phase: 04-waffle-product-page
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/data/types.ts
  - lib/data/projects.ts
  - app/projects/projects-client.tsx
  - __tests__/projects/waffle/waffle-grid-card.test.tsx
autonomous: true
requirements: [WAF-02, WAF-04]
user_setup: []

must_haves:
  truths:
    - "A visitor browsing /projects sees a Waffle card with a 'Live Product' badge that visually distinguishes it from the client case studies"
    - "Clicking the Waffle card routes to /projects/waffle (the bespoke page from 04-01), not the [slug] case-study template"
    - "The Live Product badge uses the amber accent (bg-amber-600 / dark:bg-amber-500 + text-zinc-950), contrast-safe in both themes"
    - "The badge only renders on projects flagged isLiveProduct — existing case-study cards are unaffected"
  artifacts:
    - path: "lib/data/types.ts"
      provides: "Optional isLiveProduct flag on the Project type"
      contains: "isLiveProduct"
    - path: "lib/data/projects.ts"
      provides: "Waffle PROJECTS entry with slug 'waffle', isLiveProduct: true, and screenshot thumbnail"
      contains: "isLiveProduct: true"
    - path: "app/projects/projects-client.tsx"
      provides: "Conditional Live Product badge overlay on the project card thumbnail"
      contains: "isLiveProduct"
    - path: "__tests__/projects/waffle/waffle-grid-card.test.tsx"
      provides: "Test asserting the Waffle entry shape and that the badge renders + card links to /projects/waffle"
  key_links:
    - from: "app/projects/projects-client.tsx"
      to: "lib/data/types.ts isLiveProduct flag"
      via: "conditional render guarded by project.isLiveProduct"
      pattern: "project\\.isLiveProduct"
    - from: "lib/data/projects.ts waffle entry (slug 'waffle')"
      to: "/projects/waffle route"
      via: "existing Link href={`/projects/${project.slug}`} + Next static-route precedence over [slug]"
      pattern: "slug: \"waffle\""
---

<objective>
Deliver the second vertical slice: the discovery entry point. A recruiter browsing the `/projects` grid finds Waffle as a badged "Live Product" card that routes to the bespoke `/projects/waffle` page built in 04-01 (not the `[slug]` case-study template).

Purpose: Surface Waffle where recruiters browse Randy's work, visually signalling it is a product he owns and runs — not a client case study.
Output: An `isLiveProduct` flag on the `Project` type, a `waffle` entry in `PROJECTS`, a conditional badge overlay in `projects-client.tsx`, and a test proving the badge + routing.
</objective>

## Phase Goal

**As a** hiring manager browsing Randy's projects, **I want to** spot Waffle in the projects grid marked as a live product and click straight into its showcase page, **so that** I immediately understand he ships and owns a real AI SaaS, not just client work.

> Story derived from `04-CONTEXT.md` D-05/D-06 and the ROADMAP Phase 4 goal. This slice depends on 04-01 (the `/projects/waffle` page must exist for the card to route to a real destination, and the entry references the screenshot asset copied in 04-01).

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-waffle-product-page/04-CONTEXT.md
@.planning/phases/04-waffle-product-page/04-UI-SPEC.md
@.planning/phases/04-waffle-product-page/04-PATTERNS.md

# Analog source files
@lib/data/types.ts
@app/projects/projects-client.tsx
@components/ui/global-article-grid.tsx

<interfaces>
<!-- Contracts the executor uses directly. -->

Project type (lib/data/types.ts) — required fields the waffle entry MUST satisfy:
```typescript
type Project = {
  id: string; name: string; subtitle?: string; slug: string;
  description: string; longDescription: string;   // both required, non-empty
  category: string; categories?: string[]; tags: string[];
  link: string; video: string; thumbnail?: string; images: string[];
  timeline: string; status: "completed" | "in-progress" | "concept";
  technologies: string[]; featured: boolean;
  // ...optional case-study fields (challenges/solutions/processStory) — OMIT for Waffle
};
```

Existing grid card render (app/projects/projects-client.tsx ~line 234) — the `<Card>` is already `relative`, so the badge overlay is inserted as a Card-level sibling of the `<Link>`:
```tsx
<Card className="group relative flex h-full flex-col ...">
  <Link href={`/projects/${project.slug}`} className="block">
    <ProjectThumbnail project={project} />
  </Link>
  {/* insert Live Product badge overlay here */}
```

Data-integrity guard (already passing; do NOT break): every PROJECTS entry needs `video || thumbnail` truthy, unique `id` and unique `slug`, and boolean `featured`. Waffle uses `video: ""` + a real `thumbnail`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing test for the Waffle grid card + badge + routing</name>
  <files>__tests__/projects/waffle/waffle-grid-card.test.tsx</files>
  <read_first>
    - __tests__/projects/mobile-filter-spacing.test.tsx (how ProjectsClient is rendered in jsdom + the motion/react mock — but do NOT mock @/lib/data/projects; use the real data so the real waffle badge renders)
    - __tests__/integration/data-integrity.test.tsx (media/uniqueness guards the new entry must satisfy)
    - app/projects/projects-client.tsx (card render + Link href pattern)
    - .planning/phases/04-waffle-product-page/04-UI-SPEC.md (Grid Card / Live Product Badge Contract)
  </read_first>
  <action>
    Create `__tests__/projects/waffle/waffle-grid-card.test.tsx`. It MUST fail on the first run (RED) because neither the `isLiveProduct` flag, the waffle entry, nor the badge render exist yet.

    Data-level assertions (import the real `PROJECTS` from `@/lib/data/projects`): there is exactly one entry with `slug === "waffle"`; that entry has `isLiveProduct === true`, `id === "waffle"`, `name === "Waffle"`, `thumbnail === "/projects/waffle/screenshot.png"`, a non-empty `longDescription`, and `featured` is a boolean.

    Render-level assertions (render `ProjectsClient` from `@/app/projects/projects-client` with the real data; apply the `motion/react` mock from the analog): the text "Live Product" appears at least once; a link whose `href` is `/projects/waffle` exists; and no non-live project renders a "Live Product" badge (assert exactly one "Live Product" occurrence).

    Regression guard assertion: the badge is conditional — assert that at least one existing non-waffle project (e.g. the entry with `slug === "echo"`) does NOT carry `isLiveProduct === true`.
  </action>
  <verify>
    <automated>npx jest __tests__/projects/waffle/waffle-grid-card.test.tsx 2>&1 | grep -Eq "fail|FAIL|Cannot find|isLiveProduct" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/projects/waffle/waffle-grid-card.test.tsx` exists and imports the real `PROJECTS` (not a mock) and `ProjectsClient`.
    - The test fails on first run (RED) — waffle entry / flag / badge absent.
    - Test asserts `slug === "waffle"` entry has `isLiveProduct === true` and `thumbnail === "/projects/waffle/screenshot.png"`.
    - Test asserts a link with `href="/projects/waffle"` and exactly one "Live Product" badge in the rendered grid.
  </acceptance_criteria>
  <done>The failing test encodes the badge, routing, and data-shape contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add isLiveProduct flag, the Waffle PROJECTS entry, and the badge overlay — GREEN</name>
  <files>lib/data/types.ts, lib/data/projects.ts, app/projects/projects-client.tsx</files>
  <read_first>
    - lib/data/types.ts (Project type — add the optional field next to `featured: boolean;`)
    - lib/data/projects.ts (existing entries, e.g. echo/nagarro, to mirror shape and placement)
    - app/projects/projects-client.tsx (card render block ~line 234 — insert the badge as a Card-level sibling of the Link)
    - components/ui/global-article-grid.tsx (the `absolute top-3 right-3 z-10` Featured-indicator positioning precedent)
    - .planning/phases/04-waffle-product-page/04-UI-SPEC.md (Grid Card / Live Product Badge Contract — exact badge classes)
    - .planning/phases/04-waffle-product-page/04-PATTERNS.md (lib/data/projects.ts entry shape + badge implementation excerpt)
  </read_first>
  <behavior>
    - After this task, `PROJECTS` contains a `waffle` entry with `isLiveProduct: true`, `slug: "waffle"`, `thumbnail: "/projects/waffle/screenshot.png"`.
    - The `/projects` grid renders a "Live Product" badge overlay only on the Waffle card; all other cards are visually unchanged.
    - The Waffle card's thumbnail/link routes to `/projects/waffle` via the existing `href={`/projects/${project.slug}`}` (Next resolves the static `app/projects/waffle/` route ahead of `[slug]`).
    - The Task 1 test passes; the full existing suite still passes (data-integrity, route-coverage, selected-projects unaffected).
  </behavior>
  <action>
    In `lib/data/types.ts`, add an optional field to the `Project` type immediately after `featured: boolean;`: `isLiveProduct?: boolean;` with a short comment noting it drives the "Live Product" grid badge (D-05). This is additive and optional — no existing entry needs changes.

    In `lib/data/projects.ts`, append a new `PROJECTS` entry mirroring the shape of existing entries with these concrete values: `id: "waffle"`, `name: "Waffle"`, `subtitle: "AI-Powered Interview Scorecard Generator"`, `slug: "waffle"`, `description:` the one-liner ("AI-powered interview scorecard generator — paste a job description, get weighted competencies, behavioral questions, and scoring rubrics in 2–4 minutes."), a non-empty `longDescription` drawn from the CONTEXT recruiter-proof framing (a real 1–3 sentence summary — not a placeholder), `category: "AI/ML"`, `categories: ["AI/ML", "Web Dev"]`, `tags: ["AI SaaS", "Interview Tech", "Generative UI", "Recruiting"]`, `link: "https://waffle.cards"`, `video: ""`, `thumbnail: "/projects/waffle/screenshot.png"`, `images: ["/projects/waffle/screenshot.png"]`, `timeline: "2025 – Present"`, `status: "completed"`, `technologies: ["Next.js 16", "AI SDK 6", "Claude", "Stripe", "Neon", "Prisma", "Clerk"]`, `featured: true`, `isLiveProduct: true`. OMIT the case-study-only fields (challenges/solutions/learnings/processStory/overview) — this is a product entry, not a case study (D-06/Deferred).

    In `app/projects/projects-client.tsx`, insert a conditional badge overlay as a sibling of the `<Link>` inside the existing `<Card className="group relative ...">` (the Card is already `relative`). Guard it with `{project.isLiveProduct && (...)}`. The overlay is `<div className="absolute top-3 right-3 z-10">` containing a `Badge` with `className="bg-amber-600 text-sm font-bold text-zinc-950 dark:bg-amber-500"` and text "Live Product". Never use white text on the amber fill (contrast rule). `Badge` is already imported in this file — no new import needed. Do not restructure any other card markup.

    Do NOT edit `app/sitemap.ts` — it already maps over `PROJECTS` and will include `/projects/waffle` automatically at featured priority. Do NOT add any `[slug]` routing special-case — the static `app/projects/waffle/` folder takes precedence.
  </action>
  <verify>
    <automated>npx jest __tests__/projects/waffle/waffle-grid-card.test.tsx && npm run lint && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/projects/waffle/waffle-grid-card.test.tsx` passes (GREEN).
    - Canonical CLAUDE.md verify order green after the targeted jest run: `npm run lint` clean; `npx tsc --noEmit` clean; full `npm test` passes with zero new failures (data-integrity, route-coverage, selected-projects, component-integration all still green).
    - `grep -c "isLiveProduct" lib/data/types.ts` ≥ 1 and `grep -c "isLiveProduct: true" lib/data/projects.ts` returns 1.
    - `grep -c "project.isLiveProduct" app/projects/projects-client.tsx` returns 1.
    - The badge markup pairs `bg-amber-600`/`dark:bg-amber-500` with `text-zinc-950` (grep) and contains no `text-white` on that element.
    - `grep -c 'slug: "waffle"' lib/data/projects.ts` returns 1; the entry's `thumbnail` is `/projects/waffle/screenshot.png`.
  </acceptance_criteria>
  <done>The Waffle card appears in the grid with a Live Product badge and routes to the bespoke page; no existing test regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Data + client render only; no untrusted input, no network calls, no new route surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering (client routing) | Card `Link href={`/projects/${project.slug}`}` | accept | `slug` is a static constant in `lib/data/projects.ts` (`"waffle"`), never user-derived; no injection path |
| T-04-05 | Denial of Service (data-driven render) | New PROJECTS entry iterated by grid/sitemap | accept | One additional static object; O(1) impact; sitemap already generic over PROJECTS |
| T-04-SC | Tampering (package installs) | npm/pip/cargo | N/A | No package installs — `Badge` already installed and imported |
| S / R / I / E | Spoofing / Repudiation / Info disclosure / Elevation | — | N/A | No auth, no secrets, no server compute, no privilege change |
</threat_model>

<verification>
- `npx jest __tests__/projects/waffle/waffle-grid-card.test.tsx` passes.
- `npm run lint` + `npx tsc --noEmit` clean.
- `npm test` full suite green (confirms the new PROJECTS entry did not break data-integrity/route-coverage/selected-projects assertions).
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion #3 met: `/projects` grid shows a Waffle card with a "Live Product" badge routing to `/projects/waffle` (not the `[slug]` template).
- WAF-02 satisfied; the badge-accent portion of WAF-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-waffle-product-page/04-02-SUMMARY.md` when done.
</output>
