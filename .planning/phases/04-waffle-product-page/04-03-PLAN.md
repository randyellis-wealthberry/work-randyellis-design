---
phase: 04-waffle-product-page
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [WAF-04]
user_setup: []

must_haves:
  truths:
    - "The green automated gate (lint + tsc + full test suite) passes before human review"
    - "/projects/waffle renders correctly in both light and dark mode with a legible amber accent and a visible Waffle logo mark"
    - "The Waffle grid card badge and the on-page CTAs work end-to-end (badge → page → external product), with the product screenshot free of PII/credentials"
  artifacts: []
  key_links:
    - from: "/projects grid Waffle card"
      to: "/projects/waffle"
      via: "human click-through verification"
      pattern: "manual"
    - from: "/projects/waffle CTAs"
      to: "waffle.cards"
      via: "human click-through in a new tab"
      pattern: "manual"
---

<objective>
Final human verification of the phase's visual and contrast requirements (ROADMAP success criterion #4: waffle-orange accent applied within the portfolio design system, contrast-safe in light + dark). All implementation is already automated and test-covered in 04-01/04-02; this plan runs the closing automated gate and then a focused human review of the things automated class-assertions cannot fully judge — dark-mode legibility, logo rendering, and screenshot provenance.

Purpose: Ship-confidence for a recruiter-facing marketing page; also closes the T-04-02 supply-chain mitigation (visual PII check on the copied screenshot).
Output: A human sign-off (or a punch-list of visual fixes to route back into a gap plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-waffle-product-page/04-UI-SPEC.md
@.planning/phases/04-waffle-product-page/04-01-PLAN.md
@.planning/phases/04-waffle-product-page/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the closing automated gate and start the dev server</name>
  <files>(none — verification only)</files>
  <read_first>
    - CLAUDE.md (Verifying Changes section — the lint → tsc → test order; and the agent note that `npm run dev` hangs, so use `npm run dev:auto` or `npm run dev:direct`)
  </read_first>
  <action>
    Run the project verification order and confirm all three are green: `npm run lint`, then `npx tsc --noEmit`, then `npm test`. If any fails, stop and report — do not proceed to the human checkpoint with a red gate. Then start the dev server in a way that does not block on interactive input: use `npm run dev:auto` (automated port cleanup) or `npm run dev:direct` — NEVER `npm run dev` (it waits for interactive confirmation and hangs in a non-interactive shell). Report the local URLs the reviewer should open: `http://localhost:3000/projects/waffle` and `http://localhost:3000/projects`.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0, `npx tsc --noEmit` exits 0, `npm test` reports 0 failed (skips allowed per the known baseline).
    - The dev server is reachable at `http://localhost:3000/projects/waffle` (HTTP 200).
  </acceptance_criteria>
  <done>Automated gate is green and the app is running for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual + contrast + provenance verification</name>
  <files>(none — human review only)</files>
  <read_first>
    - .planning/phases/04-waffle-product-page/04-UI-SPEC.md (Color contrast rule + Layout Details — what "correct" looks like)
  </read_first>
  <action>
    Blocking human-verify checkpoint. With the dev server running (from Task 1), the reviewer opens the two URLs and confirms the visual, contrast, CTA-behavior, screenshot-provenance, and grid-badge-routing items enumerated in `<how-to-verify>`. Do not mark the phase complete until the reviewer replies with the resume signal. If issues are reported, capture them as a punch-list for a follow-up gap plan rather than editing implementation inside this checkpoint task.
  </action>
  <what-built>
    The `/projects/waffle` product showcase page (hero → 6-feature grid → 3-step how-it-works → screenshot → closing CTA → back-nav) with a waffle-orange (amber) accent, and a "Live Product" badged Waffle card in the `/projects` grid that routes to it. Both CTAs link to waffle.cards and fire analytics. All of this is automated and test-covered — this step is a visual/functional confirmation only.
  </what-built>
  <how-to-verify>
    1. Open `http://localhost:3000/projects/waffle`. Confirm the section order: hero (with a "Live Product" badge, the Waffle logo mark, "Waffle" H1, the one-liner, and Randy's build-credit line) → "Key Features" (6 cards) → "How It Works" (3 numbered steps) → "See it in action" (screenshot) → "Ready to see it live?" CTA band → "← Back to Projects".
    2. Toggle dark mode (theme switch in the header) and confirm: the amber accent (buttons, badge, feature icon circles, step numbers) stays legible with dark text on amber (no white-on-amber, no washed-out orange); the Waffle logo mark is clearly visible against the dark background.
    3. In light mode, confirm the same amber elements read cleanly against the white background and no text is low-contrast.
    4. Click "View live product ↗" and "Try free" — each must open waffle.cards / the signup URL in a NEW tab (existing tab preserved).
    5. Inspect the product screenshot: confirm it contains NO personal data, real candidate names, emails, or credentials (supply-chain / privacy check). If it does, flag it — a cleaner capture is needed before ship.
    6. Open `http://localhost:3000/projects`, find the Waffle card, confirm the "Live Product" badge is present and visually distinct, then click the card and confirm it lands on `/projects/waffle` (not a generic case-study layout).
  </how-to-verify>
  <verify>
    <human-check>Reviewer confirms items 1–6 above pass in both light and dark mode.</human-check>
  </verify>
  <resume-signal>Type "approved" to sign off, or list the specific visual/functional issues to fix.</resume-signal>
  <done>Reviewer has signed off (or a punch-list of fixes has been recorded for a gap plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external repo → shipped asset | The copied product screenshot is reviewed here for PII/credential leakage before it goes live (closes T-04-02 from 04-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering / Information disclosure (supply chain — screenshot provenance) | `public/projects/waffle/screenshot.png` | mitigate | Human visual PII/credential check in this checkpoint (step 5); reject and re-capture if any personal data is visible |
| T-04-01 | Tampering (reverse tabnabbing) | CTA new-tab behavior | mitigate (verify) | Human confirms CTAs open a new tab and the origin tab is intact; `rel="noopener noreferrer"` already enforced by the 04-01 test |
| S / R / D / E | — | — | N/A | Verification-only plan; introduces no new attack surface |
</threat_model>

<verification>
- Automated gate green (Task 1).
- Human sign-off recorded (Task 2) covering light/dark contrast, logo visibility, CTA new-tab behavior, screenshot provenance, and grid badge → page routing.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion #4 fully confirmed: waffle-orange accent + logo within the portfolio design system, contrast-safe in light + dark, with lint + tsc + test green.
- WAF-04 visually verified; T-04-02 supply-chain mitigation closed.
</success_criteria>

<output>
Create `.planning/phases/04-waffle-product-page/04-03-SUMMARY.md` when done (record the sign-off or the punch-list).
</output>
