---
phase: 10-seo-remediation
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/sitemap.ts
  - __tests__/seo/sitemap.test.ts
autonomous: true
requirements: [SEO-05]
tags: [seo, sitemap, lastModified, tdd]

must_haves:
  truths:
    - "Every URL in sitemap.xml carries a valid lastModified date — static pages and project pages at build time, blog posts at their publication date (audit T-05, D-20)"
    - "The sitemap lists the footer-linked legal pages /privacy-policy and /terms-of-service (audit T-05 second bullet, recommended by audit + research)"
    - "No URL appears twice and every URL is absolute under WEBSITE_URL"
  artifacts:
    - path: "app/sitemap.ts"
      provides: "MetadataRoute.Sitemap with lastModified on all entries; static + legal + projects + blog"
      contains: "lastModified"
    - path: "__tests__/seo/sitemap.test.ts"
      provides: "Contract test over sitemap() output"
      min_lines: 30
  key_links:
    - from: "app/sitemap.ts"
      to: "lib/data/projects.ts PROJECTS"
      via: "projectPages map"
      pattern: "PROJECTS.map"
    - from: "app/sitemap.ts"
      to: "lib/utils/blog-data.ts getBlogArticles"
      via: "blogPages map"
      pattern: "getBlogArticles"
---

<objective>
Add `lastModified` to every sitemap entry (build-time for static + project URLs, publication date for blog) and include the two legal pages, proven by a contract test over `sitemap()`.

Purpose: Audit T-05 — freshness signals are missing for static and project URLs; D-20 locks adding `lastModified`. Research §Open Questions 1-2 recommend build-time dates for projects (no `updatedAt` field exists) and including privacy/terms (public, footer-linked, already indexed).

Output: `app/sitemap.ts` updated, `__tests__/seo/sitemap.test.ts` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/10-seo-remediation/10-RESEARCH.md
@.planning/research/SEO-AUDIT.md

<interfaces>
From app/sitemap.ts today: 5 static entries (`/`, `/projects`, `/about`, `/blog`, `/metis`) with changeFrequency+priority and NO lastModified; `projectPages = PROJECTS.map(...)` with no lastModified; `blogPages = getBlogArticles().map(post => ({ url, lastModified: new Date(post.publishedDate), changeFrequency: "monthly", priority: 0.6 }))`.

From lib/utils/blog-data.ts:
```typescript
export interface BlogArticle { slug: string; title: string; description: string; publishedDate: string; readTime: number; category: string; tags: string[]; views?: number; featured?: boolean; }
export function getBlogArticles(): BlogArticle[];   // 4 mock articles sorted newest first
```
Observation (log, do not fix here): `publishedDate` in blog-data.ts differs from the `metadata.date` declared in 3 of the 4 `app/blog/*/page.mdx` files (e.g. profits-not-pixels 2024-10-30 vs 2025-07-21). Sitemap keeps using blog-data.ts per current behavior; note the mismatch in the SUMMARY for Randy.

`PROJECTS` has 8 entries (incl. waffle); `Project` has no `updatedAt`/`dateModified` field → build time is the only honest content-agnostic date (research Open Question 2).

Legal pages exist: `app/privacy-policy/page.tsx`, `app/terms-of-service/page.tsx` (footer-linked, indexable).
</interfaces>
</context>

<feature>
  <name>Sitemap lastModified + legal pages</name>
  <files>app/sitemap.ts, __tests__/seo/sitemap.test.ts</files>
  <read_first>
    - app/sitemap.ts (current)
    - lib/utils/blog-data.ts lines 1-90 (BlogArticle, getBlogArticles)
    - lib/data/projects.ts (only `slug:` and `featured:` lines — `grep -n "slug:\|featured:" lib/data/projects.ts`)
    - __tests__/seo/project-metadata.test.ts lines 1-12 (import conventions)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-20; .planning/research/SEO-AUDIT.md T-05
  </read_first>
  <behavior>
    RED `__tests__/seo/sitemap.test.ts` (import default `sitemap` from "@/app/sitemap", `PROJECTS` from "@/lib/data/projects", `getBlogArticles` from "@/lib/utils/blog-data", `WEBSITE_URL` from "@/lib/constants"):
    - Test 1: every entry has `lastModified` that is a `Date` with `!Number.isNaN(d.getTime())` (currently fails for 5 static + 8 project entries)
    - Test 2: the set of static URLs includes exactly `${WEBSITE_URL}/`, `/projects`, `/about`, `/blog`, `/metis`, `/privacy-policy`, `/terms-of-service` (currently fails — legal pages missing)
    - Test 3: for every `PROJECTS` entry, an entry with url `${WEBSITE_URL}/projects/${slug}` exists with priority `0.8` when `featured` else `0.6` and changeFrequency `"monthly"`
    - Test 4: for every `getBlogArticles()` item, an entry exists with `lastModified.getTime() === new Date(post.publishedDate).getTime()`
    - Test 5: all urls start with `WEBSITE_URL`, none contain `localhost`, and there are no duplicate urls (`new Set(urls).size === urls.length`)
    - Test 6: all static and project entries share one build-time instant (every `lastModified.getTime()` among them is equal) and that instant is within 24h of `Date.now()` at test time
  </behavior>
  <implementation>
    RED commit: `test(10-04): add failing sitemap lastModified contract test`.

    GREEN in `app/sitemap.ts`: add `const BUILD_TIME = new Date();` at module scope (evaluated once at build/first request — a comment explains this is the honest freshness signal until projects carry content dates; D-20 + research Open Question 2). Add `lastModified: BUILD_TIME` to each of the 5 existing static entries; append two static entries `{ url: `${WEBSITE_URL}/privacy-policy`, lastModified: BUILD_TIME, changeFrequency: "yearly", priority: 0.3 }` and the same for `/terms-of-service` (audit T-05 second bullet). Add `lastModified: BUILD_TIME` to the `projectPages` map. Leave `blogPages` as is. Keep `MetadataRoute.Sitemap` typing and the `as const` on changeFrequency. Run the test — green. `npx tsc --noEmit` + `npm run lint`. Commit `feat(10-04): add lastModified to every sitemap entry and list legal pages`.
  </implementation>
  <verify>
    <automated>npx jest __tests__/seo/sitemap.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/seo/sitemap.test.ts` exits 0 with 6 passing tests
    - `grep -c "lastModified" app/sitemap.ts` ≥ 3 (static block, project map, blog map) and `grep -c "BUILD_TIME" app/sitemap.ts` ≥ 3
    - `grep -c "privacy-policy" app/sitemap.ts` == 1 and `grep -c "terms-of-service" app/sitemap.ts` == 1
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
    - git log shows the RED commit before the GREEN commit for this plan
    - SUMMARY logs the blog-data.ts vs page.mdx date mismatch (not fixed here)
  </acceptance_criteria>
</feature>

<verification>
- `npx jest __tests__/seo/sitemap.test.ts` exits 0 (6 tests)
- `grep -c "lastModified" app/sitemap.ts` ≥ 3 (static block, project map, blog map)
- `grep -c "privacy-policy\|terms-of-service" app/sitemap.ts` == 2
- `npx tsc --noEmit` and `npm run lint` exit 0
</verification>

<success_criteria>
- ROADMAP Phase 10 SC4 satisfied at code level (live `<lastmod>` proof in Plan 10-09)
- D-20 implemented; SUMMARY logs the blog-data.ts vs page.mdx date mismatch for Randy (not fixed here — outside the audit findings)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build → sitemap.xml | Static generation from trusted data files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Information Disclosure | sitemap URLs | accept | Only public routes listed; `/admin/*` and `/api/*` are never added (test 2/3/4 enumerate the allowed sets) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<output>
Create `.planning/phases/10-seo-remediation/10-04-SUMMARY.md` when done (include the blog date-mismatch observation).
</output>
