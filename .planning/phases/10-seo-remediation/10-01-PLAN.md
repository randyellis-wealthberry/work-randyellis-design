---
phase: 10-seo-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/robots.ts
  - app/sitemap.ts
  - app/test/codeblock/page.tsx
  - middleware.ts
autonomous: true
requirements:
  - SEO-02
  - SEO-03
  - SEO-05

must_haves:
  truths:
    - "Googlebot can fetch /_next/ resources to render client-side content"
    - "AI crawlers (ChatGPT, Claude, Perplexity) can index the site for answer-engine citations"
    - "Test routes are not crawlable by search engines"
    - "Sitemap includes lastModified dates for crawl efficiency"
  artifacts:
    - path: "app/robots.ts"
      provides: "Updated robots.txt rules"
      contains: "allow: '/'"
      excludes: "'/_next/'"
    - path: "app/sitemap.ts"
      provides: "Sitemap with lastModified"
      contains: "lastModified:"
    - path: "middleware.ts"
      provides: "Test route noindex enforcement"
      pattern: "startsWith\\('/test'\\)"
  key_links:
    - from: "app/robots.ts"
      to: "Googlebot"
      via: "/robots.txt fetch"
      pattern: "disallow.*(?!/_next/)"
    - from: "middleware.ts"
      to: "X-Robots-Tag header"
      via: "shouldBlockIndexing check"
      pattern: "pathname\\.startsWith\\('/test'\\)"
---

<objective>
Fix critical crawlability issues blocking search engine rendering and indexation.

Purpose: Remove robots.txt barriers preventing Googlebot from fetching JS/CSS needed to render client-side structured data, open the site to AI crawlers for answer-engine visibility (AEO/GEO), prevent test routes from diluting crawl budget, and add sitemap freshness signals.

Output: Updated crawl control files (robots.ts, middleware.ts, sitemap.ts), deleted test routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/10-seo-remediation/10-RESEARCH.md
@.planning/research/SEO-AUDIT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix robots.txt crawl barriers</name>
  <files>app/robots.ts</files>
  <read_first>
    - app/robots.ts (current disallow list and AI crawler blocks)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md (D-11, D-15)
    - .planning/research/SEO-AUDIT.md (T-02 finding)
  </read_first>
  <action>
Remove `'/_next/'` from the disallow array on line 10 (per D-15 — critical fix; all JSON-LD is currently client-rendered, so blocking /_next/ prevents Googlebot from fetching the JS bundles needed to render structured data).

Remove all AI crawler user-agent blocks (lines 12-31: GPTBot, CCBot, ChatGPT-User, anthropic-ai, Claude-Web) per D-11 — opening site to ChatGPT, Claude, Perplexity for AEO/GEO answer-engine citations. Trade-off accepted: content gets ingested into training data.

Keep existing disallows: `/private/`, `/admin/`, `/api/`, `/drafts/`.

The resulting rules array should have only one entry for userAgent "*" with allow "/" and the four preserved disallows.
  </action>
  <acceptance_criteria>
    - `app/robots.ts` contains `disallow: ["/private/", "/admin/", "/api/", "/drafts/"]` (4 entries only)
    - `app/robots.ts` does NOT contain `"/_next/"` in any disallow array
    - `app/robots.ts` rules array has length 1 (only the "*" user-agent entry remains)
    - Lines 12-31 (AI crawler blocks) are deleted
    - `npm run lint` passes
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>
robots.ts allows Googlebot to fetch /_next/ resources and permits all AI crawlers (GPTBot, CCBot, ChatGPT-User, anthropic-ai, Claude-Web) to index the site. Disallow list reduced from 5 to 4 entries (/_next/ removed). AI crawler rules section deleted entirely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Block test routes from indexation</name>
  <files>app/test/codeblock/page.tsx, middleware.ts</files>
  <read_first>
    - app/test/codeblock/page.tsx (demo route to delete)
    - middleware.ts (shouldBlockIndexing function, lines 92-99)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md (D-08, D-09, D-10)
    - .planning/research/SEO-AUDIT.md (T-03 finding)
  </read_first>
  <action>
Delete `app/test/codeblock/page.tsx` entirely per D-08 — it's an indexable demo route diluting crawl quality. AGENTS.md says "don't add stray routes"; this is an existing stray.

In `middleware.ts` line ~95, change `pathname.startsWith("/test-glow")` to `pathname.startsWith("/test")` — broader pattern covers both /test-glow (already blocked) and /test/* paths. This ensures X-Robots-Tag: noindex is applied to any remaining or future /test routes.

Audit for other dev-only routes per D-09 criteria: not linked from nav/sitemap, not in ROADMAP scope. Check `app/admin/email-test/page.tsx` and `app/admin/test-results-demo/page.tsx` — both already under /admin/ which is covered by existing shouldBlockIndexing check (line 92 `pathname.startsWith("/admin")`). No further changes needed.

Note: /test-glow/page.tsx not found in scan — either already deleted or never existed. The middleware check can be removed in a future cleanup if confirmed absent.
  </action>
  <acceptance_criteria>
    - `app/test/codeblock/page.tsx` does not exist
    - `middleware.ts` contains `pathname.startsWith("/test")` (not "/test-glow")
    - Grep shows no other page.tsx files under `/app` that match dev-only criteria (not in nav, not in sitemap, not in ROADMAP scope) outside /admin/ or /api/
    - `npm run lint` passes
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>
/test/codeblock route deleted. middleware.ts blocks all /test/* paths with X-Robots-Tag: noindex. No indexable test routes remain outside the protected /admin/ and /api/ prefixes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add lastModified to sitemap URLs</name>
  <files>app/sitemap.ts</files>
  <read_first>
    - app/sitemap.ts (current static/project/blog entries, blog already has lastModified on line 46)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md (D-14)
    - .planning/research/SEO-AUDIT.md (T-05 finding)
  </read_first>
  <action>
Add `lastModified: new Date()` (build time) to all entries in the `staticPages` array (lines 8-34: home, /projects, /about, /blog, /metis). This provides freshness signals to crawlers; build-time dates are acceptable for static content per research discretion.

Add `lastModified: new Date()` to all entries in `projectPages.map()` (line 37-41). Alternative approach (content-derived from `project.timeline?.end`) is deferred — start with build time per research recommendation; switch if Search Console shows stale "last crawled" timestamps.

Blog pages (lines 44-49) already have `lastModified: new Date(post.publishedDate)` — leave unchanged.

The final sitemap will have lastModified on 100% of URLs (static + projects + blog), not just blog posts.
  </action>
  <acceptance_criteria>
    - All 5 staticPages entries contain `lastModified: new Date()`
    - All projectPages entries contain `lastModified: new Date()`
    - Blog pages retain `lastModified: new Date(post.publishedDate)`
    - `npm run lint` passes
    - `npx tsc --noEmit` passes
    - Build succeeds with `npm run build` (metadata routes compile)
  </acceptance_criteria>
  <done>
Sitemap includes lastModified dates for all URLs (static pages at build time, projects at build time, blog posts at content publish date). Crawlers receive freshness signals to improve crawl efficiency and re-indexing priority.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search engine crawlers → site | Untrusted bots fetch robots.txt and sitemap.xml; middleware enforces noindex on sensitive paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | robots.ts allow rules | accept | robots.txt is public by design; sensitive paths remain in disallow list (/admin/, /api/, /private/, /drafts/) |
| T-10-02 | Denial of Service | sitemap.ts build-time generation | accept | Build-time lastModified calculation is cheap (Date constructor); no user input involved |
| T-10-03 | Tampering | AI crawler training data ingestion | accept | Per D-11, trade-off accepted for AEO/GEO visibility — content will be ingested into ChatGPT/Claude/Perplexity training corpora |
</threat_model>

<verification>
**Per-task checks:**
- Task 1: robots.txt generated at build contains no /_next/ block, no AI crawler blocks, 4 disallows only
- Task 2: /test/codeblock route 404s, middleware X-Robots-Tag applies to /test paths
- Task 3: sitemap.xml contains <lastmod> tags on all URLs

**Integration check:**
- Build succeeds: `npm run build`
- Lint clean: `npm run lint`
- Type-check clean: `npx tsc --noEmit`
- Dev server renders sitemap.xml at http://localhost:3000/sitemap.xml with lastModified
- Dev server renders robots.txt at http://localhost:3000/robots.txt without /_next/ or AI crawler blocks
</verification>

<success_criteria>
- Googlebot can fetch /_next/*.js and /_next/*.css (robots.txt allows it)
- AI crawlers (ChatGPT, Claude, Perplexity) are not blocked (no user-agent disallow rules for GPTBot/CCBot/ChatGPT-User/anthropic-ai/Claude-Web)
- /test/* routes return X-Robots-Tag: noindex (middleware enforces it)
- /test/codeblock returns 404 (route deleted)
- Sitemap contains lastModified for 100% of URLs (static, projects, blog)
- `npm run build && npm run lint && npx tsc --noEmit` all pass clean
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-01-SUMMARY.md` when done
</output>
