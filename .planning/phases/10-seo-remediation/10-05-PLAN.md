---
phase: 10-seo-remediation
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-03"]
files_modified:
  - app/layout.tsx
  - app/projects/[slug]/page.tsx
  - app/projects/addvanced/page.tsx
  - app/projects/echo/page.tsx
  - app/projects/nagarro/page.tsx
  - app/projects/rambis-ui/page.tsx
  - app/about/page.tsx
  - app/metis/page.tsx
  - components/ui/breadcrumb-nav.tsx
  - __tests__/seo/json-ld-wiring.test.tsx
autonomous: true
requirements: [SEO-04]
tags: [seo, json-ld, rsc, structured-data, project-routes]

must_haves:
  truths:
    - "The root layout server-renders Person + WebSite JSON-LD into <head> via <JsonLd> — no next/script, no client component (D-08, D-10)"
    - "All 7 case-study routes (growit/ohplays/ledgeriq via [slug]; addvanced, echo, nagarro, rambis-ui standalone) server-render CreativeWork + BreadcrumbList derived from their PROJECTS entry; [slug] no longer emits FAQPage (D-08, D-09, D-10; Phase 9 D-11/D-12 JSON-LD parity)"
    - "/about and /metis server-render BreadcrumbList via <JsonLd>; components/ui/breadcrumb-nav.tsx is a visual-only nav (no JSON-LD), so no page emits BreadcrumbList twice"
    - "No file under app/ or components/ui imports components/seo/structured-data or components/seo/project-faq except those owned by Plans 10-06 (app/projects/page.tsx) and 10-07 (blog) which migrate in parallel"
  artifacts:
    - path: "app/layout.tsx"
      provides: "<JsonLd data={buildPersonSchema()}/> and <JsonLd data={buildWebSiteSchema()}/> in <head>"
      contains: "buildPersonSchema()"
    - path: "app/projects/[slug]/page.tsx"
      provides: "CreativeWork + BreadcrumbList via JsonLd; no ProjectFAQStructuredData"
      contains: "buildCreativeWorkSchema("
    - path: "__tests__/seo/json-ld-wiring.test.tsx"
      provides: "Render test ([slug] growit + addvanced with client mocked) + source assertions for all 9 files"
      min_lines: 60
  key_links:
    - from: "app/projects/[slug]/page.tsx"
      to: "lib/seo/json-ld.ts buildCreativeWorkSchema / lib/metadata.ts projectBreadcrumbItems"
      via: "import + JsonLd render"
      pattern: "buildBreadcrumbSchema\\(projectBreadcrumbItems\\("
    - from: "app/layout.tsx"
      to: "components/seo/json-ld.tsx"
      via: "import { JsonLd }"
      pattern: "from \"@/components/seo/json-ld\""
---

<objective>
Wire the server-rendered JSON-LD layer into the root layout, all 7 case-study routes, /about and /metis, and strip the client-side schema emission from the breadcrumb nav — so Person/WebSite/CreativeWork/BreadcrumbList are present in the initial HTML and FAQPage is gone.

Purpose: Audit T-04 (all JSON-LD is client-rendered) and T-06 (entity chaos) — D-10 moves schema into RSC output; D-08/D-09 restrict it to the four entity types + navigational breadcrumbs. Because Plan 10-08 deletes `components/seo/structured-data.tsx`, every route that currently imports it must migrate here (or in the parallel Plans 10-06/10-07).

Phase 9 note: plans 09-02..09-05 have NOT executed. This plan delivers the JSON-LD half of Phase 9 D-11/D-12 (CreativeWork + Breadcrumb on all 7 routes) in the server-rendered form, but deliberately does NOT touch any `export const metadata` / `generateMetadata` — Phase 9 plan 09-02 still owns metadata unification via `projectMetadata()`.

Output: 9 source files migrated, 1 wiring test, lint/tsc/test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@.planning/phases/10-seo-remediation/10-03-SUMMARY.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Contracts from Plan 10-03 (already shipped in wave 1) -->
From lib/seo/json-ld.ts:
```typescript
export type JsonLdObject = Record<string, unknown>;
export const PERSON_ID: string; export const WEBSITE_ID: string;
export function buildPersonSchema(): JsonLdObject;
export function buildWebSiteSchema(): JsonLdObject;
export function buildCreativeWorkSchema(project: Project): JsonLdObject;
export function buildArticleSchema(input: {...}): JsonLdObject;
export function buildBreadcrumbSchema(items: ReadonlyArray<{ name: string; url: string }>): JsonLdObject;
export function serializeJsonLd(data: unknown): string;
```
From components/seo/json-ld.tsx (RSC):
```typescript
export function JsonLd({ data, id }: { data: JsonLdObject | JsonLdObject[]; id?: string }): JSX.Element;
```
From lib/metadata.ts (Phase 9): `projectBreadcrumbItems(project)` → `[{Home, WEBSITE_URL}, {Projects, /projects}, {project.name, /projects/slug}]`.
From lib/constants.ts: `WEBSITE_URL`.

Current importers of `@/components/seo/structured-data` (verified): app/layout.tsx (PersonStructuredData, WebsiteStructuredData), app/projects/[slug]/page.tsx (CreativeWorkStructuredData + `@/components/seo/project-faq` ProjectFAQStructuredData), app/projects/addvanced/page.tsx (BreadcrumbStructuredData with hand-typed items incl. name "Addvanced Career Tracker"), app/projects/page.tsx (Plan 10-06 owns), app/about/page.tsx, app/metis/page.tsx, app/blog/layout.tsx (Plan 10-07 owns), components/ui/breadcrumb-nav.tsx (client; imported by app/projects/[slug]/project-detail-client.tsx and app/blog/layout.tsx), 4× app/blog/*/page.mdx (Plan 10-07 owns).

Standalone pages today: echo/nagarro/rambis-ui emit NO JSON-LD; addvanced emits Breadcrumb only. rambis-ui already does `const rambisProject = PROJECTS.find((p) => p.id === "rambis-ui")!` at module scope — the pattern to replicate (CONVENTIONS.md: non-null assertion on known-good data). echo and rambis-ui wrap their client in `<div className="container mx-auto max-w-6xl px-4 py-16">` — keep.

app/layout.tsx after Plan 10-01: no PWA wiring; still imports `PersonStructuredData, WebsiteStructuredData` and renders them inside `<head>` under `{/* Structured Data */}`.

Rendering an RSC page in Jest: pages are plain (async) functions — `const el = await ProjectDetailPage({ params: Promise.resolve({ slug: "growit" }) }); renderToStaticMarkup(el)` works when the heavy client component is mocked: `jest.mock("@/app/projects/[slug]/project-detail-client", () => ({ __esModule: true, default: () => null }))`. Existing pattern for mocking heavy deps: __tests__/projects/mobile-filter-spacing.test.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout → server-rendered Person + WebSite; breadcrumb-nav → visual only</name>
  <files>app/layout.tsx, components/ui/breadcrumb-nav.tsx</files>
  <read_first>
    - app/layout.tsx (full, post-10-01 state — locate the `PersonStructuredData`/`WebsiteStructuredData` import and the `{/* Structured Data */}` block in `<head>`)
    - components/ui/breadcrumb-nav.tsx (full — `BreadcrumbStructuredData` import, `structuredDataItems` computation, `createAbsoluteUrl` import, the `<>` fragment wrapping the nav)
    - components/seo/json-ld.tsx and lib/seo/json-ld.ts exports (from Plan 10-03)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-08, D-10
  </read_first>
  <action>
    app/layout.tsx: replace `import { PersonStructuredData, WebsiteStructuredData } from "@/components/seo/structured-data";` with `import { JsonLd } from "@/components/seo/json-ld";` and `import { buildPersonSchema, buildWebSiteSchema } from "@/lib/seo/json-ld";`. In `<head>` replace `<PersonStructuredData />` + `<WebsiteStructuredData />` with `<JsonLd id="person-jsonld" data={buildPersonSchema()} />` and `<JsonLd id="website-jsonld" data={buildWebSiteSchema()} />` (keep the `{/* Structured Data */}` comment, append "— server-rendered (D-10); entity story per D-08"). Nothing else in the layout changes.

    components/ui/breadcrumb-nav.tsx: remove the `BreadcrumbStructuredData` import, the `structuredDataItems` block, the `<BreadcrumbStructuredData items={structuredDataItems} />` element, and — if now unused — the `createAbsoluteUrl` import (keep `trackBreadcrumbClick`, `cn`, `Link`, icons). Collapse the fragment to return just the `<nav aria-label="Breadcrumb" ...>` unless other siblings exist. Add a one-line comment at the top of the component: "Visual breadcrumb only — BreadcrumbList JSON-LD is server-rendered by each page via <JsonLd> (Phase 10 D-10)". This prevents duplicate BreadcrumbList on /projects/[slug] and blog posts once their pages emit the server version.

    `npx tsc --noEmit` and `npm run lint` clean. Commit `feat(10-05): server-render Person + WebSite JSON-LD in root layout; breadcrumb nav visual-only`.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/seo/json-ld"' app/layout.tsx && grep -q "buildPersonSchema()" app/layout.tsx && grep -q "buildWebSiteSchema()" app/layout.tsx && ! grep -q "structured-data" app/layout.tsx components/ui/breadcrumb-nav.tsx && ! grep -q "BreadcrumbStructuredData" components/ui/breadcrumb-nav.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PersonStructuredData\|WebsiteStructuredData\|components/seo/structured-data" app/layout.tsx` == 0
    - `grep -c "buildPersonSchema()" app/layout.tsx` == 1 and `grep -c "buildWebSiteSchema()" app/layout.tsx` == 1 and `grep -c "<JsonLd" app/layout.tsx` == 2
    - `grep -c "BreadcrumbStructuredData\|structured-data\|application/ld+json" components/ui/breadcrumb-nav.tsx` == 0
    - `head -1 components/ui/breadcrumb-nav.tsx` is still `"use client";` (visual nav remains a client component — it uses analytics click tracking)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Layout emits Person + WebSite from the server; the breadcrumb nav no longer emits any schema.</done>
</task>

<task type="auto">
  <name>Task 2: [slug] + 4 standalone project routes → server-rendered CreativeWork + BreadcrumbList; drop FAQ</name>
  <files>app/projects/[slug]/page.tsx, app/projects/addvanced/page.tsx, app/projects/echo/page.tsx, app/projects/nagarro/page.tsx, app/projects/rambis-ui/page.tsx</files>
  <read_first>
    - app/projects/[slug]/page.tsx (full — imports lines 1-6, `generateMetadata` (DO NOT MODIFY), the JSX block lines 91-115 with `<CreativeWorkStructuredData .../>` and `<ProjectFAQStructuredData .../>`)
    - app/projects/addvanced/page.tsx, app/projects/echo/page.tsx, app/projects/nagarro/page.tsx, app/projects/rambis-ui/page.tsx (full — each is ≤ 60 lines; note `export const metadata` must stay untouched)
    - lib/metadata.ts lines 268-300 (`projectCreativeWorkProps`, `projectBreadcrumbItems`)
    - lib/data/projects.ts `grep -n "slug:\|id:" lib/data/projects.ts` (confirm ids/slugs: addvanced, echo, nagarro, rambis-ui)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-09, D-11, D-12 (what Phase 9 intended for these routes) and .planning/phases/09-cross-surface-verification/09-02-PLAN.md task list (to avoid touching metadata — that plan still owns it)
  </read_first>
  <action>
    app/projects/[slug]/page.tsx: replace the `CreativeWorkStructuredData` and `ProjectFAQStructuredData` imports with `import { JsonLd } from "@/components/seo/json-ld";`, `import { buildCreativeWorkSchema, buildBreadcrumbSchema } from "@/lib/seo/json-ld";`, `import { projectBreadcrumbItems } from "@/lib/metadata";`. In the returned fragment replace the `<CreativeWorkStructuredData ...>` element and the `<ProjectFAQStructuredData projectSlug={project.slug} />` line with `<JsonLd id="creativework-jsonld" data={buildCreativeWorkSchema(project)} />` and `<JsonLd id="breadcrumb-jsonld" data={buildBreadcrumbSchema(projectBreadcrumbItems(project))} />` (D-09: FAQPage deleted — fabricated/off-topic content, no visible Q&A on page; D-08/D-10). Leave `generateMetadata`, `relatedProjects`, and `<ProjectDetailClient>` exactly as they are (Phase 9 09-02 owns `generateMetadata` → `projectMetadata()` refactor; do not pre-empt it). Remove now-unused imports.

    Each standalone page (addvanced, echo, nagarro, rambis-ui): add (or reuse, for rambis-ui) a module-scope `const <name>Project = PROJECTS.find((p) => p.slug === "<slug>")!;` (import `PROJECTS` from "@/lib/data/projects" where missing), import `JsonLd`, `buildCreativeWorkSchema`, `buildBreadcrumbSchema`, `projectBreadcrumbItems` as above, and render `<JsonLd id="creativework-jsonld" data={buildCreativeWorkSchema(<name>Project)} />` + `<JsonLd id="breadcrumb-jsonld" data={buildBreadcrumbSchema(projectBreadcrumbItems(<name>Project))} />` as the first children of the page's returned fragment/container (before the client component). For addvanced: delete the `BreadcrumbStructuredData` import, the hand-typed `breadcrumbItems` array (its "Addvanced Career Tracker" label is replaced by `project.name` from the data — Phase 9 D-18 single source), and the old element. For echo/rambis-ui keep the `<div className="container ...">` wrapper and place the two `<JsonLd>` elements inside a fragment around it or as first children of the div (either is fine — scripts render nothing visible). Do NOT edit any `export const metadata` block in these four files.

    `npx tsc --noEmit`, `npm run lint`, and `npx jest __tests__/seo/project-metadata.test.ts` (must stay green). Commit `feat(10-05): server-render CreativeWork + BreadcrumbList on all 7 case-study routes; drop FAQPage`.
  </action>
  <verify>
    <automated>for f in "app/projects/[slug]/page.tsx" app/projects/addvanced/page.tsx app/projects/echo/page.tsx app/projects/nagarro/page.tsx app/projects/rambis-ui/page.tsx; do grep -q "buildCreativeWorkSchema(" "$f" && grep -q "buildBreadcrumbSchema(projectBreadcrumbItems(" "$f" && ! grep -q "structured-data\|project-faq" "$f" || { echo "FAIL $f"; exit 1; }; done && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: `grep -c "buildCreativeWorkSchema(" FILE` == 1, `grep -c "buildBreadcrumbSchema(projectBreadcrumbItems(" FILE` == 1, `grep -c "components/seo/structured-data\|components/seo/project-faq\|ProjectFAQStructuredData\|CreativeWorkStructuredData\|BreadcrumbStructuredData" FILE` == 0
    - `grep -c "export async function generateMetadata" "app/projects/[slug]/page.tsx"` == 1 and `git diff --stat -- "app/projects/[slug]/page.tsx"` shows no changes inside the generateMetadata body (executor confirms via `git diff`)
    - `grep -c "export const metadata" app/projects/addvanced/page.tsx app/projects/echo/page.tsx app/projects/nagarro/page.tsx app/projects/rambis-ui/page.tsx` == 4 (one each, unchanged)
    - `grep -c "Addvanced Career Tracker" app/projects/addvanced/page.tsx` counts only occurrences inside `export const metadata` (the hand-typed breadcrumb label is gone)
    - `npx tsc --noEmit`, `npm run lint`, `npx jest __tests__/seo/project-metadata.test.ts` exit 0
  </acceptance_criteria>
  <done>All 7 case-study routes emit CreativeWork + BreadcrumbList from the server using Phase 9's single-source helpers; FAQPage is no longer emitted anywhere on project pages; metadata untouched for Phase 9.</done>
</task>

<task type="auto">
  <name>Task 3: /about and /metis breadcrumbs via JsonLd; wiring regression test; gate</name>
  <files>app/about/page.tsx, app/metis/page.tsx, __tests__/seo/json-ld-wiring.test.tsx</files>
  <read_first>
    - app/about/page.tsx and app/metis/page.tsx (full — `breadcrumbItems` arrays with hardcoded `https://work.randyellis.design` strings and `<BreadcrumbStructuredData>`)
    - __tests__/projects/mobile-filter-spacing.test.tsx lines 1-20 (jest.mock pattern for heavy client modules)
    - __tests__/seo/project-metadata.test.ts lines 1-12 (fs/path + `@/` imports)
    - app/projects/[slug]/page.tsx and app/projects/addvanced/page.tsx (post-Task-2 state — default export signatures to render in the test)
  </read_first>
  <action>
    app/about/page.tsx and app/metis/page.tsx: replace the `BreadcrumbStructuredData` import with `import { JsonLd } from "@/components/seo/json-ld";`, `import { buildBreadcrumbSchema } from "@/lib/seo/json-ld";`, `import { WEBSITE_URL } from "@/lib/constants";`; rewrite `breadcrumbItems` to use `WEBSITE_URL` (`{ name: "Home", url: WEBSITE_URL }`, `{ name: "About", url: `${WEBSITE_URL}/about` }` / `{ name: "METIS", url: `${WEBSITE_URL}/metis` }`); render `<JsonLd id="breadcrumb-jsonld" data={buildBreadcrumbSchema(breadcrumbItems)} />` in place of the old element. Metadata exports untouched.

    Create `__tests__/seo/json-ld-wiring.test.tsx`:
    (a) Source assertions (fs.readFileSync on each path): for FILES = [app/layout.tsx, app/projects/[slug]/page.tsx, app/projects/addvanced/page.tsx, app/projects/echo/page.tsx, app/projects/nagarro/page.tsx, app/projects/rambis-ui/page.tsx, app/about/page.tsx, app/metis/page.tsx] assert each contains `from "@/components/seo/json-ld"` and none contains `components/seo/structured-data` or `components/seo/project-faq`; the 5 project files contain `buildCreativeWorkSchema(` and `buildBreadcrumbSchema(projectBreadcrumbItems(`; layout contains `buildPersonSchema()` and `buildWebSiteSchema()`; `components/ui/breadcrumb-nav.tsx` does not contain `BreadcrumbStructuredData` or `application/ld+json`.
    (b) Render assertions: `jest.mock("@/app/projects/[slug]/project-detail-client", () => ({ __esModule: true, default: () => null }))` and `jest.mock("@/app/projects/addvanced/addvanced-client", () => ({ __esModule: true, default: () => null }))`; import the default exports of `@/app/projects/[slug]/page` and `@/app/projects/addvanced/page`; `const html = renderToStaticMarkup(await ProjectDetailPage({ params: Promise.resolve({ slug: "growit" }) }))` — extract all `<script type="application/ld+json"...>...</script>` bodies with a regex, `JSON.parse` each, and assert exactly one has `@type === "CreativeWork"` with `name === "GrowIt!"` and `url === "https://work.randyellis.design/projects/growit"`, exactly one has `@type === "BreadcrumbList"` with 3 items ending in `GrowIt!`, and none has `@type` ∈ {FAQPage, Organization, LocalBusiness, ProfessionalService}. Same for `renderToStaticMarkup(AddvancedPage())` expecting CreativeWork name from `PROJECTS.find(p => p.slug === "addvanced")!.name`. If `next/navigation`'s `notFound` import causes trouble in jsdom, `jest.mock("next/navigation", () => ({ notFound: jest.fn() }))`.
    Run `npx jest __tests__/seo/json-ld-wiring.test.tsx` → green. Then the full gate: `npm run lint` → `npx tsc --noEmit` → `npm test` (flaky animation-load-testing excluded). Commit `test(10-05): add JSON-LD wiring regression test; migrate about/metis breadcrumbs`.
  </action>
  <verify>
    <automated>npx jest __tests__/seo/json-ld-wiring.test.tsx 2>&1 | tail -12 && ! grep -q "structured-data" app/about/page.tsx app/metis/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/seo/json-ld-wiring.test.tsx` exits 0
    - `grep -c "components/seo/structured-data\|BreadcrumbStructuredData" app/about/page.tsx app/metis/page.tsx` == 0; `grep -c "buildBreadcrumbSchema(" app/about/page.tsx` == 1 and the same for metis; `grep -c "https://work.randyellis.design" app/about/page.tsx app/metis/page.tsx` == 0 (WEBSITE_URL used)
    - `grep -rln "components/seo/structured-data\|components/seo/project-faq" app components lib` returns ONLY files owned by parallel plans: `app/projects/page.tsx`, `app/blog/layout.tsx`, `app/blog/*/page.mdx` (and the source files themselves) — nothing else
    - `npm run lint`, `npx tsc --noEmit` exit 0; `npm test` has no new failures
  </acceptance_criteria>
  <done>Every page this plan owns emits its schema from the server through JsonLd, a regression test pins the wiring, and the gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data → inline script | Project/page strings serialized into `<script type="application/ld+json">` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering (XSS via JSON-LD) | JsonLd in layout/pages | mitigate | All emission goes through `serializeJsonLd` (escapes `<`/`>`) — no page builds its own `dangerouslySetInnerHTML` |
| T-10-14 | Repudiation (claim drift) | CreativeWork props | mitigate | Derived from `projectCreativeWorkProps(project)` / `projectBreadcrumbItems(project)`; hand-typed labels removed (addvanced) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `npx jest __tests__/seo/json-ld-wiring.test.tsx __tests__/seo/json-ld.test.ts __tests__/seo/project-metadata.test.ts` green
- `grep -rln "components/seo/structured-data" app components lib` lists only Plan 10-06/10-07-owned files (+ the legacy source itself)
- Gate green
</verification>

<success_criteria>
- ROADMAP Phase 10 SC3 (server-rendered Person/WebSite/CreativeWork, FAQ removed) holds for layout + all 7 project routes + about/metis
- D-08, D-09, D-10 implemented and cited; Phase 9 metadata ownership respected
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-05-SUMMARY.md` when done. State explicitly that `generateMetadata`/`export const metadata` were left for Phase 9 plan 09-02 and that 09-02's JSON-LD tasks are now superseded by this plan.
</output>
