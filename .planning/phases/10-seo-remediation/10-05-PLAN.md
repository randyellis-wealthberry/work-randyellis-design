---
phase: 10-seo-remediation
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-03"]
files_modified:
  - app/layout.tsx
  - app/projects/[slug]/page.tsx
  - app/projects/[slug]/project-detail-client.tsx
  - app/projects/addvanced/page.tsx
  - app/projects/echo/page.tsx
  - app/projects/nagarro/page.tsx
  - app/projects/rambis-ui/page.tsx
  - app/about/page.tsx
  - app/metis/page.tsx
  - components/ui/breadcrumb-nav.tsx
  - __tests__/seo/project-route-wiring.test.ts
autonomous: true
requirements: [SEO-04]
tags: [seo, json-ld, rsc, structured-data, project-routes]

must_haves:
  truths:
    - "The root layout server-renders Person + WebSite JSON-LD into <head> via <JsonLd> — no next/script, no client component (D-08, D-10)"
    - "All 7 case-study routes (growit/ohplays/ledgeriq via [slug]; addvanced, echo, nagarro, rambis-ui standalone) server-render CreativeWork + BreadcrumbList derived from their PROJECTS entry through Phase 9's projectCreativeWorkProps/projectBreadcrumbItems helpers; FAQPage stays gone (D-08, D-09, D-10; builds on 09-02)"
    - "/about and /metis server-render BreadcrumbList via <JsonLd>; components/ui/breadcrumb-nav.tsx is a visual-only nav (no JSON-LD, no structuredData prop), so no page emits BreadcrumbList twice"
    - "Phase 9's route-wiring drift test (__tests__/seo/project-route-wiring.test.ts) is updated in place to assert the server-rendered wiring and stays green"
  artifacts:
    - path: "app/layout.tsx"
      provides: "<JsonLd data={buildPersonSchema()}/> and <JsonLd data={buildWebSiteSchema()}/> in <head>"
      contains: "buildPersonSchema()"
    - path: "app/projects/[slug]/page.tsx"
      provides: "projectMetadata() (unchanged from 09-02) + CreativeWork/BreadcrumbList via JsonLd"
      contains: "buildCreativeWorkSchema("
    - path: "__tests__/seo/project-route-wiring.test.ts"
      provides: "Updated D-14 drift test: 5 routes use projectMetadata + JsonLd builders; layout/about/metis/breadcrumb-nav assertions; render checks for growit + addvanced"
      min_lines: 120
  key_links:
    - from: "app/projects/[slug]/page.tsx"
      to: "lib/seo/json-ld.ts buildCreativeWorkSchema / lib/metadata.ts projectBreadcrumbItems"
      via: "import + JsonLd render"
      pattern: "buildBreadcrumbSchema\\(projectBreadcrumbItems\\("
    - from: "app/layout.tsx"
      to: "components/seo/json-ld.tsx"
      via: "import { JsonLd }"
      pattern: "from \"@/components/seo/json-ld\""
---

<objective>
Wire the server-rendered JSON-LD layer into the root layout, all 7 case-study routes, /about and /metis, and strip the client-side schema emission from the breadcrumb nav — so Person/WebSite/CreativeWork/BreadcrumbList are present in the initial HTML.

Purpose: Audit T-04 (all JSON-LD is client-rendered) and T-06 (entity chaos) — D-10 moves schema into RSC output; D-08/D-09 restrict it to the four entity types + navigational breadcrumbs. This plan builds on Phase 9 plan 09-02's work: the 5 project routes already call `projectMetadata()` and render CreativeWork + BreadcrumbList from `projectCreativeWorkProps()`/`projectBreadcrumbItems()` via the CLIENT components in `components/seo/structured-data.tsx`; 10-05 swaps only the emission mechanism (client `<Script>` → RSC `<JsonLd>`). Because Plan 10-08 deletes `components/seo/structured-data.tsx`, every route that imports it must migrate here (or in the parallel Plans 10-06/10-07).

Output: 10 source files migrated, Phase 9's drift test updated in place, lint/tsc/test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-02-SUMMARY.md
@.planning/phases/10-seo-remediation/10-03-SUMMARY.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Contracts from Plan 10-03 (already shipped in wave 1) -->
From lib/seo/json-ld.ts:
```typescript
export type JsonLdObject = Record<string, unknown>;
export const PERSON_ID: string; export const WEBSITE_ID: string;
export function buildPersonSchema(): JsonLdObject;
export function buildWebSiteSchema(): JsonLdObject;
export function buildCreativeWorkSchema(project: Project): JsonLdObject;   // calls projectCreativeWorkProps(project) internally
export function buildArticleSchema(input: {...}): JsonLdObject;
export function buildBreadcrumbSchema(items: ReadonlyArray<{ name: string; url: string }>): JsonLdObject;
export function serializeJsonLd(data: unknown): string;
```
From components/seo/json-ld.tsx (RSC):
```typescript
export function JsonLd({ data, id }: { data: JsonLdObject | JsonLdObject[]; id?: string }): JSX.Element;
```
From lib/metadata.ts (Phase 9): `projectMetadata(project)`, `projectCreativeWorkProps(project)`, `projectBreadcrumbItems(project)` → `[{Home, WEBSITE_URL}, {Projects, /projects}, {project.name, /projects/slug}]`. From lib/constants.ts: `WEBSITE_URL`.

CURRENT state after the Phase 9 merge (verified 2026-08-21):
- `components/seo/structured-data.tsx` ("use client", next/script) has exactly six exports left: `BreadcrumbStructuredData`, `PersonStructuredData`, `WebsiteStructuredData`, `ArticleStructuredData`, `CreativeWorkStructuredData`, `LocalBusinessStructuredData` (09-03 deleted the dead Organization/ProfessionalService/FAQ/FractionalCDOService exports). `components/seo/project-faq.tsx` is ALREADY DELETED (09-02) — nothing imports it.
- Importers of `@/components/seo/structured-data`: app/layout.tsx (PersonStructuredData, WebsiteStructuredData — this plan), app/projects/[slug]/page.tsx + app/projects/{addvanced,echo,nagarro,rambis-ui}/page.tsx (CreativeWorkStructuredData + BreadcrumbStructuredData — this plan), app/about/page.tsx + app/metis/page.tsx (BreadcrumbStructuredData — this plan), components/ui/breadcrumb-nav.tsx (BreadcrumbStructuredData — this plan), app/projects/page.tsx (Plan 10-06), app/blog/layout.tsx + 4× app/blog/*/page.mdx (Plan 10-07).
- All 5 project routes already look like: imports `projectMetadata, projectCreativeWorkProps, projectBreadcrumbItems` from "@/lib/metadata" and `CreativeWorkStructuredData, BreadcrumbStructuredData` from the client module; standalone files declare `const project = PROJECTS.find((p) => p.slug === "<slug>")!;` and `export const metadata: Metadata = projectMetadata(project);`; `[slug]` returns `projectMetadata(project)` from `generateMetadata`; each renders `<CreativeWorkStructuredData {...projectCreativeWorkProps(project)} />` + `<BreadcrumbStructuredData items={projectBreadcrumbItems(project)} />` before the client component (echo/rambis-ui keep a `<div className="container mx-auto max-w-6xl px-4 py-16">` wrapper).
- `components/ui/breadcrumb-nav.tsx` now has a `structuredData?: boolean` prop (default `true`) that gates its `<BreadcrumbStructuredData>`; the ONLY caller passing it is `app/projects/[slug]/project-detail-client.tsx` line ~181 (`structuredData={false}`); `app/blog/layout.tsx` renders `<BreadcrumbNav>` without the prop.
- `__tests__/seo/project-route-wiring.test.ts` (130 lines, Phase 9 D-14 drift test) asserts for the 5 routes: contains `projectMetadata(`, `projectCreativeWorkProps(`, `projectBreadcrumbItems(`, `<CreativeWorkStructuredData`, `<BreadcrumbStructuredData`; no `ProjectFAQStructuredData`/`project-faq`, no `longDescription`, no literal `openGraph:`; standalone routes contain `export const metadata: Metadata = projectMetadata(project);` and `p.slug === "<slug>"`; Waffle uses `createPageMetadata(` and contains neither client component; `components/seo/project-faq.tsx` absent. The `<CreativeWorkStructuredData`/`<BreadcrumbStructuredData`/`projectCreativeWorkProps(` assertions WILL BREAK after this plan — the file must be updated in place (not duplicated).
- app/layout.tsx after Plan 10-01: no PWA wiring; still imports `PersonStructuredData, WebsiteStructuredData` and renders them inside `<head>` under `{/* Structured Data */}`.

Rendering an RSC page in Jest without JSX (`.ts` file): pages are plain (async) functions — `const el = await ProjectDetailPage({ params: Promise.resolve({ slug: "growit" }) }); renderToStaticMarkup(el)` works when the heavy client component is mocked: `jest.mock("@/app/projects/[slug]/project-detail-client", () => ({ __esModule: true, default: () => null }))`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout → server-rendered Person + WebSite; breadcrumb-nav → visual only (drop the structuredData prop)</name>
  <files>app/layout.tsx, components/ui/breadcrumb-nav.tsx, app/projects/[slug]/project-detail-client.tsx</files>
  <read_first>
    - app/layout.tsx (full, post-10-01 state — locate the `PersonStructuredData`/`WebsiteStructuredData` import and the `{/* Structured Data */}` block in `<head>`)
    - components/ui/breadcrumb-nav.tsx (full — `BreadcrumbStructuredData` import, `structuredData` prop + default, `structuredDataItems` computation, `createAbsoluteUrl` import, the `{structuredData && (...)}` render)
    - app/projects/[slug]/project-detail-client.tsx lines 170-190 (the `<BreadcrumbNav ... structuredData={false} />` call — the only caller of the prop; `grep -rn "structuredData=" app components` to confirm)
    - components/seo/json-ld.tsx and lib/seo/json-ld.ts exports (from Plan 10-03)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-08, D-10
  </read_first>
  <action>
    app/layout.tsx: replace `import { PersonStructuredData, WebsiteStructuredData } from "@/components/seo/structured-data";` with `import { JsonLd } from "@/components/seo/json-ld";` and `import { buildPersonSchema, buildWebSiteSchema } from "@/lib/seo/json-ld";`. In `<head>` replace `<PersonStructuredData />` + `<WebsiteStructuredData />` with `<JsonLd id="person-jsonld" data={buildPersonSchema()} />` and `<JsonLd id="website-jsonld" data={buildWebSiteSchema()} />` (keep the `{/* Structured Data */}` comment, append "— server-rendered (D-10); entity story per D-08"). Nothing else in the layout changes.

    components/ui/breadcrumb-nav.tsx: remove the `BreadcrumbStructuredData` import, the `structuredData` prop from `BreadcrumbNavProps` and from the destructured signature (including its `= true` default), the `structuredDataItems` block, the `{structuredData && (<BreadcrumbStructuredData .../>)}` element, and — if now unused — the `createAbsoluteUrl` import (keep `trackBreadcrumbClick`, `cn`, `Link`, icons). Collapse the fragment to return just the `<nav aria-label="Breadcrumb" ...>` unless other siblings exist. Add a one-line comment at the top of the component: "Visual breadcrumb only — BreadcrumbList JSON-LD is server-rendered by each page via <JsonLd> (Phase 10 D-10)".

    app/projects/[slug]/project-detail-client.tsx: delete the `structuredData={false}` prop from its `<BreadcrumbNav>` call (the prop no longer exists; nothing else in this file changes).

    `npm run lint` → `npx tsc --noEmit` clean (CLAUDE.md gate order). Commit `feat(10-05): server-render Person + WebSite JSON-LD in root layout; breadcrumb nav visual-only`.
  </action>
  <verify>
    <automated>grep -q 'from "@/components/seo/json-ld"' app/layout.tsx && grep -q "buildPersonSchema()" app/layout.tsx && grep -q "buildWebSiteSchema()" app/layout.tsx && ! grep -q "structured-data" app/layout.tsx components/ui/breadcrumb-nav.tsx && ! grep -q "BreadcrumbStructuredData\|structuredData" components/ui/breadcrumb-nav.tsx && ! grep -q "structuredData=" "app/projects/[slug]/project-detail-client.tsx" && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PersonStructuredData\|WebsiteStructuredData\|components/seo/structured-data" app/layout.tsx` == 0
    - `grep -c "buildPersonSchema()" app/layout.tsx` == 1 and `grep -c "buildWebSiteSchema()" app/layout.tsx` == 1 and `grep -c "<JsonLd" app/layout.tsx` == 2
    - `grep -c "BreadcrumbStructuredData\|structured-data\|application/ld+json\|structuredData" components/ui/breadcrumb-nav.tsx` == 0
    - `grep -rc "structuredData=" app components` totals 0 (prop removed at its only call site)
    - `head -1 components/ui/breadcrumb-nav.tsx` is still `"use client";` (visual nav remains a client component — it uses analytics click tracking)
    - `npm run lint` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Layout emits Person + WebSite from the server; the breadcrumb nav no longer emits any schema and has no schema prop.</done>
</task>

<task type="auto">
  <name>Task 2: [slug] + 4 standalone project routes → swap client CreativeWork/Breadcrumb components for server-rendered JsonLd (metadata wiring from 09-02 untouched)</name>
  <files>app/projects/[slug]/page.tsx, app/projects/addvanced/page.tsx, app/projects/echo/page.tsx, app/projects/nagarro/page.tsx, app/projects/rambis-ui/page.tsx</files>
  <read_first>
    - app/projects/[slug]/page.tsx (full — the `lib/metadata` + `structured-data` imports, `generateMetadata` returning `projectMetadata(project)` (DO NOT MODIFY), and the two client schema elements before `<ProjectDetailClient>`)
    - app/projects/addvanced/page.tsx, app/projects/echo/page.tsx, app/projects/nagarro/page.tsx, app/projects/rambis-ui/page.tsx (full — each ≤ 30 lines: `const project = PROJECTS.find(...)!`, `export const metadata: Metadata = projectMetadata(project);` (DO NOT MODIFY), the two client schema elements)
    - lib/metadata.ts lines 268-300 (`projectCreativeWorkProps`, `projectBreadcrumbItems`) and lib/seo/json-ld.ts `buildCreativeWorkSchema` signature (takes `Project`)
    - .planning/phases/09-cross-surface-verification/09-02-SUMMARY.md (what 09-02 wired — this plan keeps all of it except the emission mechanism)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-09, D-11, D-12
  </read_first>
  <action>
    In each of the 5 files: replace the `import { CreativeWorkStructuredData, BreadcrumbStructuredData } from "@/components/seo/structured-data";` with `import { JsonLd } from "@/components/seo/json-ld";` and `import { buildCreativeWorkSchema, buildBreadcrumbSchema } from "@/lib/seo/json-ld";`; trim the `@/lib/metadata` import to `projectMetadata, projectBreadcrumbItems` (drop `projectCreativeWorkProps` — `buildCreativeWorkSchema(project)` calls it internally; Phase 9 D-11 single source preserved); replace `<CreativeWorkStructuredData {...projectCreativeWorkProps(project)} />` with `<JsonLd id="creativework-jsonld" data={buildCreativeWorkSchema(project)} />` and `<BreadcrumbStructuredData items={projectBreadcrumbItems(project)} />` with `<JsonLd id="breadcrumb-jsonld" data={buildBreadcrumbSchema(projectBreadcrumbItems(project))} />` (D-08/D-10). Leave `generateMetadata` / `export const metadata: Metadata = projectMetadata(project);`, the `const project = PROJECTS.find(...)!` lines, `relatedProjects`, the container `<div>` wrappers (echo, rambis-ui) and the client component calls exactly as they are — this plan builds on 09-02's `projectMetadata` wiring and changes nothing about metadata. FAQPage is already gone (09-02); simply confirm no `ProjectFAQStructuredData`/`project-faq` text exists.

    `npm run lint` → `npx tsc --noEmit` → `npx jest __tests__/seo/project-metadata.test.ts` (must stay green; `project-route-wiring.test.ts` is expected RED until Task 3 updates it). Commit `feat(10-05): server-render CreativeWork + BreadcrumbList on all 7 case-study routes via JsonLd`.
  </action>
  <verify>
    <automated>for f in "app/projects/[slug]/page.tsx" app/projects/addvanced/page.tsx app/projects/echo/page.tsx app/projects/nagarro/page.tsx app/projects/rambis-ui/page.tsx; do grep -q "buildCreativeWorkSchema(project)" "$f" && grep -q "buildBreadcrumbSchema(projectBreadcrumbItems(project))" "$f" && grep -q "projectMetadata(" "$f" && ! grep -q "structured-data\|project-faq\|projectCreativeWorkProps" "$f" || { echo "FAIL $f"; exit 1; }; done && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: `grep -c "buildCreativeWorkSchema(project)" FILE` == 1, `grep -c "buildBreadcrumbSchema(projectBreadcrumbItems(project))" FILE` == 1, `grep -c "<JsonLd" FILE` == 2, `grep -c "components/seo/structured-data\|CreativeWorkStructuredData\|BreadcrumbStructuredData\|projectCreativeWorkProps\|ProjectFAQStructuredData\|project-faq" FILE` == 0
    - `grep -c "return projectMetadata(project);" "app/projects/[slug]/page.tsx"` == 1 and `grep -c "export const metadata: Metadata = projectMetadata(project);" app/projects/addvanced/page.tsx app/projects/echo/page.tsx app/projects/nagarro/page.tsx app/projects/rambis-ui/page.tsx` == 4 (09-02 metadata wiring untouched); `git diff` for these files shows no change to any metadata line
    - `npm run lint`, `npx tsc --noEmit`, `npx jest __tests__/seo/project-metadata.test.ts` exit 0
  </acceptance_criteria>
  <done>All 7 case-study routes emit CreativeWork + BreadcrumbList from the server using Phase 9's single-source helpers; metadata wiring from 09-02 is intact.</done>
</task>

<task type="auto">
  <name>Task 3: /about and /metis breadcrumbs via JsonLd; update Phase 9's route-wiring drift test in place; gate</name>
  <files>app/about/page.tsx, app/metis/page.tsx, __tests__/seo/project-route-wiring.test.ts</files>
  <read_first>
    - __tests__/seo/project-route-wiring.test.ts (full — the D-14 drift test this task UPDATES; keep its structure, `ROUTES` map, Waffle exclusion and project-faq absence checks)
    - app/about/page.tsx and app/metis/page.tsx (full — `breadcrumbItems` arrays with hardcoded `https://work.randyellis.design` strings and `<BreadcrumbStructuredData>`)
    - __tests__/projects/mobile-filter-spacing.test.tsx lines 1-20 (jest.mock pattern for heavy client modules)
    - app/projects/[slug]/page.tsx and app/projects/addvanced/page.tsx (post-Task-2 state — default export signatures to render in the test)
  </read_first>
  <action>
    app/about/page.tsx and app/metis/page.tsx: replace the `BreadcrumbStructuredData` import with `import { JsonLd } from "@/components/seo/json-ld";`, `import { buildBreadcrumbSchema } from "@/lib/seo/json-ld";`, `import { WEBSITE_URL } from "@/lib/constants";`; rewrite `breadcrumbItems` to use `WEBSITE_URL` (`{ name: "Home", url: WEBSITE_URL }`, `{ name: "About", url: `${WEBSITE_URL}/about` }` / `{ name: "METIS", url: `${WEBSITE_URL}/metis` }`); render `<JsonLd id="breadcrumb-jsonld" data={buildBreadcrumbSchema(breadcrumbItems)} />` in place of the old element. Metadata exports untouched.

    UPDATE `__tests__/seo/project-route-wiring.test.ts` in place (do not create a second wiring test): in the per-route `describe.each` replace `it("uses projectCreativeWorkProps helper")` with `it("derives CreativeWork via buildCreativeWorkSchema(project)")` asserting `source` contains `buildCreativeWorkSchema(project)`; replace `it("renders CreativeWorkStructuredData component")` / `it("renders BreadcrumbStructuredData component")` with assertions that `source` contains `<JsonLd`, `buildBreadcrumbSchema(projectBreadcrumbItems(project))` and `from "@/components/seo/json-ld"`, and does NOT contain `components/seo/structured-data`, `CreativeWorkStructuredData`, `BreadcrumbStructuredData`; keep the `projectMetadata(`, `projectBreadcrumbItems(`, no-FAQ, no-longDescription, no-`openGraph:` assertions, the standalone `export const metadata: Metadata = projectMetadata(project);` + `p.slug === "<slug>"` checks, and the project-faq-absent check. Update the Waffle exclusion to assert Waffle contains neither `buildCreativeWorkSchema` nor `@/components/seo/json-ld` (and still uses `createPageMetadata(`). ADD: (a) a "site-level wiring" describe with source assertions that `app/layout.tsx` contains `buildPersonSchema()`, `buildWebSiteSchema()` and `from "@/components/seo/json-ld"` and not `structured-data`; `app/about/page.tsx` and `app/metis/page.tsx` contain `buildBreadcrumbSchema(` and not `structured-data`; `components/ui/breadcrumb-nav.tsx` contains neither `BreadcrumbStructuredData` nor `application/ld+json` nor `structuredData`. (b) a "rendered JSON-LD" describe: `jest.mock("@/app/projects/[slug]/project-detail-client", () => ({ __esModule: true, default: () => null }))` and `jest.mock("@/app/projects/addvanced/addvanced-client", () => ({ __esModule: true, default: () => null }))` (and `jest.mock("next/navigation", () => ({ notFound: jest.fn() }))` if needed); import the default exports of `@/app/projects/[slug]/page` and `@/app/projects/addvanced/page`; `const html = renderToStaticMarkup(await ProjectDetailPage({ params: Promise.resolve({ slug: "growit" }) }))` (no JSX — the file stays `.ts`); extract all `<script type="application/ld+json"...>...</script>` bodies with a regex, `JSON.parse` each, assert exactly one `@type === "CreativeWork"` with `name === "GrowIt!"` and `url === "https://work.randyellis.design/projects/growit"`, exactly one `@type === "BreadcrumbList"` with 3 items ending in `GrowIt!`, and none with `@type` ∈ {FAQPage, Organization, LocalBusiness, ProfessionalService}; same for `renderToStaticMarkup(AddvancedPage())` expecting the CreativeWork `name` from `PROJECTS.find(p => p.slug === "addvanced")!.name`. Keep the file header comment, amend it to say the routes now emit server-rendered JSON-LD via `<JsonLd>` (Phase 10 D-10) while metadata still derives from `projectMetadata` (Phase 9 D-14).

    Run `npx jest __tests__/seo/project-route-wiring.test.ts` → green. Then the full gate: `npm run lint` → `npx tsc --noEmit` → `npm test` (flaky animation-load-testing excluded). Commit `test(10-05): update route-wiring drift test for server-rendered JSON-LD; migrate about/metis breadcrumbs`.
  </action>
  <verify>
    <automated>npx jest __tests__/seo/project-route-wiring.test.ts 2>&1 | tail -12 && ! grep -q "structured-data" app/about/page.tsx app/metis/page.tsx && ! grep -q "CreativeWorkStructuredData\|BreadcrumbStructuredData" __tests__/seo/project-route-wiring.test.ts && test ! -e __tests__/seo/json-ld-wiring.test.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/seo/project-route-wiring.test.ts` exits 0; no new `__tests__/seo/json-ld-wiring.test.*` file exists (updated in place, not duplicated)
    - `grep -c "buildCreativeWorkSchema(project)" __tests__/seo/project-route-wiring.test.ts` ≥ 1; `grep -c "CreativeWorkStructuredData\|BreadcrumbStructuredData" __tests__/seo/project-route-wiring.test.ts` == 0; `grep -c "renderToStaticMarkup" __tests__/seo/project-route-wiring.test.ts` ≥ 1
    - `grep -c "components/seo/structured-data\|BreadcrumbStructuredData" app/about/page.tsx app/metis/page.tsx` == 0; `grep -c "buildBreadcrumbSchema(" app/about/page.tsx` == 1 and the same for metis; `grep -c "https://work.randyellis.design" app/about/page.tsx app/metis/page.tsx` == 0 (WEBSITE_URL used)
    - `grep -rln "components/seo/structured-data" app components lib` returns ONLY files owned by parallel plans: `app/projects/page.tsx`, `app/blog/layout.tsx`, `app/blog/*/page.mdx` (plus the legacy source file itself) — nothing else
    - `npm run lint`, `npx tsc --noEmit` exit 0; `npm test` has no new failures
  </acceptance_criteria>
  <done>Every page this plan owns emits its schema from the server through JsonLd; Phase 9's drift test now pins the server-rendered wiring; the gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data → inline script | Project/page strings serialized into `<script type="application/ld+json">` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering (XSS via JSON-LD) | JsonLd in layout/pages | mitigate | All emission goes through `serializeJsonLd` (escapes `<`/`>`) — no page builds its own `dangerouslySetInnerHTML` |
| T-10-14 | Repudiation (claim drift) | CreativeWork props | mitigate | Derived from `projectCreativeWorkProps(project)` / `projectBreadcrumbItems(project)` (Phase 9 helpers); the updated drift test fails CI on regression |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `npx jest __tests__/seo/project-route-wiring.test.ts __tests__/seo/json-ld.test.ts __tests__/seo/project-metadata.test.ts` green
- `grep -rln "components/seo/structured-data" app components lib` lists only Plan 10-06/10-07-owned files (+ the legacy source itself)
- Gate green (lint → tsc → test)
</verification>

<success_criteria>
- ROADMAP Phase 10 SC3 (server-rendered Person/WebSite/CreativeWork, FAQ absent) holds for layout + all 7 project routes + about/metis
- D-08, D-09, D-10 implemented and cited; Phase 9 09-02 metadata wiring preserved and its drift test kept meaningful
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-05-SUMMARY.md` when done. State explicitly that `generateMetadata`/`export const metadata` (09-02 wiring) were not modified and that `__tests__/seo/project-route-wiring.test.ts` was updated in place.
</output>
