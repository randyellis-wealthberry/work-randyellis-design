---
phase: 10-seo-remediation
plan: 10
type: execute
wave: 5
depends_on: ["10-09"]
files_modified:
  - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md
  - .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-home.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-growit.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-blog.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/sw-registrations.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/gsc-sitemap.png
autonomous: false
requirements: [SEO-05]
tags: [seo, verification, rich-results, search-console, human-gate]

must_haves:
  truths:
    - "Rich Results Test shows zero errors for home, /projects/growit and /blog/profits-not-pixels, confirmed by a human with screenshots (D-17, D-23)"
    - "Search Console property for work.randyellis.design exists and sitemap.xml is submitted with a Success status, with screenshot proof (D-18)"
    - "10-VERIFICATION-CHECKLIST.md has all 13 rows at pass with proof paths, or the phase is explicitly blocked with the failing rows and a gap-closure path (D-23)"
  artifacts:
    - path: ".planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md"
      provides: "13/13 pass, run log complete"
      contains: "gsc-sitemap.png"
    - path: ".planning/phases/10-seo-remediation/10-verification-screenshots/gsc-sitemap.png"
      provides: "Search Console sitemap submission proof"
  key_links:
    - from: "10-VERIFICATION-CHECKLIST.md row 13"
      to: "10-verification-screenshots/gsc-sitemap.png"
      via: "Proof column"
      pattern: "gsc-sitemap.png"
---

<objective>
Close the loop on the human-only verifications: Randy confirms the Rich Results Test results (running them manually for any URL Plan 10-09 could not automate), submits the sitemap in Google Search Console, and Claude finalizes the checklist — or, if Rich Results reports errors, the phase blocks until the fix ships and re-verifies (D-23).

Purpose: D-18 makes Search Console submission required (needs Randy's Google account — no API access); D-23 makes Rich Results errors a hard stop. This plan is the final gate for ROADMAP Phase 10 SC5.

Output: completed 13/13 checklist with screenshots; or a blocked status with named gaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md
@.planning/phases/10-seo-remediation/10-09-SUMMARY.md

<interfaces>
Rich Results Test: https://search.google.com/test/rich-results (public). Expected: Article detected on the blog post; home and project pages report structured data found but likely "no rich results eligible" for Person/WebSite/CreativeWork — that is NOT an error. Warnings for non-standard CreativeWork keys `teamSize`/`role` (D-14) and for SearchAction (Google retired the sitelinks-searchbox rich result in 2024) are expected and not blocking. ERRORS (red) block per D-23.
Search Console: https://search.google.com/search-console → property `work.randyellis.design` (domain or URL-prefix) → Sitemaps → enter `sitemap.xml` → Submit → status "Success".
Screenshot dir: `.planning/phases/10-seo-remediation/10-verification-screenshots/` (D-19 layout from Plan 10-08).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Rich Results Test results for the 3 sampled URLs (D-17) and the service-worker check; D-23 block on errors</name>
  <files>.planning/phases/10-seo-remediation/10-verification-screenshots/rrt-home.png, .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-growit.png, .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-blog.png, .planning/phases/10-seo-remediation/10-verification-screenshots/sw-registrations.png</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md rows 9–12 (what 10-09 already captured; which rows are still pending)
    - .planning/phases/10-seo-remediation/10-09-SUMMARY.md (automation outcome)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-17, D-23
  </read_first>
  <action>
    Before pausing, Claude: lists rows 9–12 with their current Status and Proof from the checklist, states which PNGs already exist in `10-verification-screenshots/` (with byte sizes), prints the three exact Rich Results Test URLs, and presents the verification steps below. Claude does not mark any of rows 9–12 `pass` on its own without an existing screenshot.
  </action>
  <what-built>Server-rendered Person/WebSite (home), CreativeWork + BreadcrumbList (/projects/growit), Article + BreadcrumbList (/blog/profits-not-pixels) are live; kill-switch sw.js is live. Plan 10-09 saved any screenshots it could capture automatically (see rows 9–12 Proof column).</what-built>
  <how-to-verify>
    For each row 9–12 still `pending` (or to double-check a `pass`):
    1. Open https://search.google.com/test/rich-results, test `https://work.randyellis.design/` → wait for results → confirm there are NO red errors (warnings are fine) → screenshot the results panel and save/drop it as `10-verification-screenshots/rrt-home.png`.
    2. Repeat for `https://work.randyellis.design/projects/growit` → `rrt-growit.png`.
    3. Repeat for `https://work.randyellis.design/blog/profits-not-pixels` → `rrt-blog.png` (expect "Article" detected).
    4. Open https://work.randyellis.design/ in Chrome, DevTools → Application → Service Workers: expect no registration for the origin after the page loads (if you previously had the old SW, reload once — it should disappear). Screenshot → `sw-registrations.png`.
    Reply "approved" if all three tests show zero errors and no SW remains registered; otherwise paste the exact error text(s) and which URL.
  </how-to-verify>
  <verify>
    <automated>cd .planning/phases/10-seo-remediation/10-verification-screenshots && for f in rrt-home.png rrt-growit.png rrt-blog.png sw-registrations.png; do test -s "$f" && [ "$(wc -c < "$f")" -gt 10240 ] || { echo "missing/small $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The four PNG files exist (> 10 KB each) in `10-verification-screenshots/` and are referenced by rows 9–12
    - User reply is "approved", OR it lists specific Rich Results errors → D-23: Claude records each error in the row Notes with Status `fail`, returns `BLOCKED` to the orchestrator naming the row(s) and the likely source file (`lib/seo/json-ld.ts` / `components/seo/blog-post-json-ld.tsx`), and the phase is NOT marked complete until a gap-closure plan ships the fix, redeploys (human), and this checkpoint is re-run clean
  </acceptance_criteria>
  <done>Rows 9–12 are human-confirmed with screenshots (pass), or the phase is explicitly blocked per D-23 with the exact Rich Results error text recorded.</done>
  <resume-signal>Type "approved" or paste the Rich Results error text + URL</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Search Console sitemap submission (D-18 — required)</name>
  <files>.planning/phases/10-seo-remediation/10-verification-screenshots/gsc-sitemap.png</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md row 13
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-18
  </read_first>
  <action>
    Claude cannot access Randy's Google account or the Search Console API without OAuth credentials — this is a genuine human-only step. Before pausing, Claude prints the exact sitemap URL (`https://work.randyellis.design/sitemap.xml`), re-confirms it returns HTTP 200 via `curl -sS -o /dev/null -w "%{http_code}" https://work.randyellis.design/sitemap.xml`, and presents the steps below.
  </action>
  <what-built>Live sitemap.xml with lastModified on all 19 URLs (verified in 10-09 row 2).</what-built>
  <how-to-verify>
    1. Open https://search.google.com/search-console and select (or add + verify) the property for work.randyellis.design.
    2. Sitemaps → "Add a new sitemap" → enter `sitemap.xml` → Submit (re-submit if already present).
    3. Wait until the status reads "Success" (may take a minute; "Couldn't fetch" → retry once after a few minutes).
    4. Screenshot the Sitemaps panel showing the URL, submitted date and Success status → save as `10-verification-screenshots/gsc-sitemap.png` (crop out your account email if visible).
    Reply "submitted" when done (or describe the blocker, e.g. property not verified).
  </how-to-verify>
  <verify>
    <automated>test -s .planning/phases/10-seo-remediation/10-verification-screenshots/gsc-sitemap.png && [ "$(wc -c < .planning/phases/10-seo-remediation/10-verification-screenshots/gsc-sitemap.png)" -gt 10240 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gsc-sitemap.png` exists (> 10 KB) and shows status Success for `https://work.randyellis.design/sitemap.xml`
    - User reply "submitted"
  </acceptance_criteria>
  <done>Search Console sitemap submission is confirmed with a screenshot (D-18).</done>
  <resume-signal>Type "submitted" (or describe the blocker)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize the checklist, reconcile STATE notes, commit the proof</name>
  <files>.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md (all 13 rows)
    - .planning/phases/10-seo-remediation/10-verification-screenshots/ listing (every Proof path must exist)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-16, D-19, D-23
  </read_first>
  <action>
    Fill rows 9–13 with Result / Proof / Status from Tasks 1–2 (Status `pass` only with an existing proof file). Verify every Proof path in the table resolves to an existing file (`ls` each). Complete the Run log (human verifier name from the checkpoint replies, dates). If all 13 rows are `pass`: add a final line "## Outcome — 13/13 pass — Phase 10 SC5 satisfied (D-16/D-17/D-18/D-22)". If any row is `fail`: add "## Outcome — BLOCKED (D-23)" listing the failing rows, and return a blocked result to the orchestrator (the phase stays open for gap closure). Commit `docs(10-10): complete Phase 10 live verification checklist with screenshots` including all files under `10-verification-screenshots/`. In the SUMMARY, list the STATE.md updates: Chameleon blocker resolved (from 10-08), Phase 10 verification outcome, and the observations carried (blog-data date mismatch, profits-not-pixels headline) for Randy.
  </action>
  <verify>
    <automated>cd .planning/phases/10-seo-remediation && test "$(grep -c '| pending |' 10-VERIFICATION-CHECKLIST.md)" -eq 0 && grep -Eq "## Outcome" 10-VERIFICATION-CHECKLIST.md && for f in $(grep -o "10-verification-screenshots/[A-Za-z0-9._-]*" 10-VERIFICATION-CHECKLIST.md | sort -u); do test -s "$f" || { echo "missing $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| pending |" 10-VERIFICATION-CHECKLIST.md` == 0
    - Every `10-verification-screenshots/<file>` referenced in the table exists and is non-empty
    - The checklist ends with an `## Outcome` line reading either `13/13 pass` or `BLOCKED (D-23)` with row numbers
    - Commit exists containing the checklist and all proof files
  </acceptance_criteria>
  <done>The D-16/D-19 proof artifact is complete and committed; Phase 10 is either verified live or explicitly blocked with a named gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → Google tools | Rich Results Test / Search Console use Randy's browser/account; Claude never receives credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | Information Disclosure | Screenshots committed to repo | mitigate | Screenshots show only public URLs and Search Console sitemap status; the user is instructed to crop any account email before saving |
| T-10-24 | Repudiation | Verification sign-off | mitigate | Checkpoint replies + screenshots + commit form the audit trail (D-19) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- Checklist 13/13 pass with existing proof files, committed; or BLOCKED with named rows
</verification>

<success_criteria>
- ROADMAP Phase 10 SC5 satisfied: live robots/sitemap fetch, Rich Results Test on home / one project / one blog post, Search Console sitemap submission — all recorded with proof
- D-16, D-17, D-18, D-19, D-22, D-23 honored
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-10-SUMMARY.md` when done.
</output>
