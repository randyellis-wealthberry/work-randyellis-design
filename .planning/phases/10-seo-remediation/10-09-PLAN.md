---
phase: 10-seo-remediation
plan: 09
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06", "10-07", "10-08"]
files_modified:
  - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md
  - .planning/phases/10-seo-remediation/10-verification-screenshots/robots.txt
  - .planning/phases/10-seo-remediation/10-verification-screenshots/sitemap.xml
  - .planning/phases/10-seo-remediation/10-verification-screenshots/sw.js.txt
  - .planning/phases/10-seo-remediation/10-verification-screenshots/redirects.txt
  - .planning/phases/10-seo-remediation/10-verification-screenshots/jsonld-home.txt
  - .planning/phases/10-seo-remediation/10-verification-screenshots/jsonld-growit.txt
  - .planning/phases/10-seo-remediation/10-verification-screenshots/jsonld-blog.txt
  - .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-home.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-growit.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-blog.png
  - .planning/phases/10-seo-remediation/10-verification-screenshots/sw-registrations.png
autonomous: false
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-05]
tags: [seo, verification, post-deploy, live]

must_haves:
  truths:
    - "Every Phase 10 code change is live on the production domain (not a preview) before any check runs (D-22)"
    - "Checklist rows 1–8 are filled from real curl output against https://work.randyellis.design with raw responses saved as proof files"
    - "Rich Results Test and service-worker-registration checks have been attempted via browser automation with screenshots saved, or are explicitly marked for human completion in Plan 10-10"
  artifacts:
    - path: ".planning/phases/10-seo-remediation/10-verification-screenshots/robots.txt"
      provides: "Raw live robots.txt"
    - path: ".planning/phases/10-seo-remediation/10-verification-screenshots/sitemap.xml"
      provides: "Raw live sitemap.xml"
    - path: ".planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md"
      provides: "Rows 1–8 with Result/Proof/Status filled; run log with commit SHA"
      contains: "Run log"
  key_links:
    - from: "10-VERIFICATION-CHECKLIST.md rows 1-8"
      to: "10-verification-screenshots/*.txt|xml"
      via: "Proof column paths"
      pattern: "10-verification-screenshots/"
---

<objective>
Get Phase 10 onto production and capture machine-verifiable live proof: confirm the production deployment carries the Phase 10 commits, then fetch robots.txt, sitemap.xml, sw.js, the redirects, the deleted routes and the raw HTML of home / one project / one blog post, writing each result into the checklist with saved proof files; then attempt the Rich Results Test and service-worker checks through browser automation.

Purpose: The audit ran offline (SEO-AUDIT.md §Limitation); D-16/D-22 make live post-deploy verification mandatory on production. Automation-first: everything curl/Playwright can prove is proven here; Plan 10-10 holds the human-only confirmations (Rich Results review, Search Console).

Output: filled checklist rows 1–8 (+9–12 if automation succeeds), proof files in `10-verification-screenshots/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/research/SEO-AUDIT.md
@.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md
@.planning/phases/10-seo-remediation/10-08-SUMMARY.md
@CLAUDE.md

<interfaces>
Production: https://work.randyellis.design (Vercel, region iad1; `vercel.json`). Deploy scripts in package.json: `deploy:vercel` (production), `deploy:preview`. Normal release flow: merge `gsd/phase-10-seo-remediation` → `main` → Vercel auto-deploys production. Claude must not merge/push/deploy without explicit user instruction (repo governance) — the deploy is a human gate; afterwards Claude verifies the live commit.
Expected live facts (from Plans 10-01..10-08): robots has `*` + AI group, no `/_next/`; sitemap 19 `<url>` each with `<lastmod>`; `/sw.js` < 2 KB containing `registration.unregister`, `Cache-Control: public, max-age=0, must-revalidate`; `/test-glow` and `/test/codeblock` → 308 (Next permanent redirect) or 301 to `/`; `/offline` and `/manifest.webmanifest` → 404; home HTML contains `"@type":"Person"` + `"@type":"WebSite"` inline; `/projects/growit` contains `"@type":"CreativeWork"` + `"@type":"BreadcrumbList"`; `/blog/profits-not-pixels` contains `"@type":"Article"`.
Vercel exposes the deployment via response header `x-vercel-id`; the repo's HEAD SHA is the reference; if `vercel` CLI is authenticated, `vercel ls --prod` / `vercel inspect <url>` shows the commit. Memory note: prefer `mcp__playwright__*` (isolated) for browser automation if available; Rich Results Test is a public tool at https://search.google.com/test/rich-results (no login) but may present a consent dialog or bot challenge.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Production deployment of Phase 10 (D-22)</name>
  <files>none</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-08-SUMMARY.md (gate results — must be green before deploying)
    - CLAUDE.md §Deployment & Cleanup (deploy scripts) and §Verifying Changes
  </read_first>
  <action>
    Before pausing, Claude: (1) confirms `git status` is clean and lists the Phase 10 commits (`git log --oneline main..HEAD` or equivalent) with the HEAD SHA; (2) if `gh` is authenticated, reports whether a PR for `gsd/phase-10-seo-remediation` exists (`gh pr view --json url,state` — read-only; do NOT create/merge/push unless the user explicitly asks in this session); (3) presents the deployment instructions below and pauses. After the user replies, Claude runs the `<verify>` command; if it fails the deploy has not landed — ask again and do not proceed to Task 2.
  </action>
  <what-built>All Phase 10 code changes (Plans 10-01..10-08): kill-switch sw.js + PWA removal, robots/redirects/middleware, server-rendered JSON-LD, /projects filter, sitemap lastModified, legacy schema deletion — gate green per 10-08-SUMMARY.</what-built>
  <how-to-verify>
    1. Merge branch `gsd/phase-10-seo-remediation` into `main` (PR merge is preferred; or tell Claude explicitly to run `npm run deploy:vercel` if you want a direct production deploy from this branch).
    2. Wait for the Vercel production deployment of that merge commit to reach "Ready".
    3. Reply with: the merged/deployed commit SHA and the production URL (https://work.randyellis.design). Claude will then verify the live site serves the new `/sw.js` (contains `registration.unregister`) before running the checks.
  </how-to-verify>
  <verify>
    <automated>curl -sS --max-time 30 https://work.randyellis.design/sw.js | grep -q "registration.unregister" && curl -sS --max-time 30 https://work.randyellis.design/robots.txt | grep -vq "Disallow: /_next/" && echo DEPLOY_LIVE</automated>
  </verify>
  <acceptance_criteria>
    - User reply contains a 7+ char commit SHA that is an ancestor-or-equal of the Phase 10 HEAD and states the production deployment is Ready
    - `curl -s https://work.randyellis.design/sw.js | grep -c "registration.unregister"` ≥ 1 (Claude runs this immediately after the reply; if 0, the deploy has not landed — ask again, do not proceed)
  </acceptance_criteria>
  <done>Production serves the Phase 10 build (kill-switch sw.js live); the deployed commit SHA is recorded for the run log.</done>
  <resume-signal>Reply with the deployed commit SHA + "deployed" (or describe the blocker)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Live curl verification — rows 1–8 with proof files</name>
  <files>.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md, .planning/phases/10-seo-remediation/10-verification-screenshots/robots.txt, .planning/phases/10-seo-remediation/10-verification-screenshots/sitemap.xml, .planning/phases/10-seo-remediation/10-verification-screenshots/sw.js.txt, .planning/phases/10-seo-remediation/10-verification-screenshots/redirects.txt, .planning/phases/10-seo-remediation/10-verification-screenshots/jsonld-home.txt, .planning/phases/10-seo-remediation/10-verification-screenshots/jsonld-growit.txt, .planning/phases/10-seo-remediation/10-verification-screenshots/jsonld-blog.txt</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md (rows 1–8 expected values — fill exactly those rows)
    - app/robots.ts, app/sitemap.ts (expected counts: 19 URLs; AI group tokens)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-16, D-19, D-22
  </read_first>
  <action>
    All against `https://work.randyellis.design` (D-22 — production only). Use `curl -sS -L --max-time 30 -A "Mozilla/5.0 (Phase10-Verify)"` and save raw outputs into `10-verification-screenshots/` with the file names listed in `<files>`:
    1. robots: save body to `robots.txt`; PASS iff `grep -c "Disallow: /_next/" robots.txt` == 0 AND `grep -ci "User-agent: GPTBot" robots.txt` ≥ 1 AND it contains `Sitemap: https://work.randyellis.design/sitemap.xml` AND `Disallow: /admin/` ≥ 1.
    2. sitemap: save to `sitemap.xml`; PASS iff `grep -o "<url>" | wc -l` == 19 AND `grep -o "<lastmod>" | wc -l` == 19 AND it contains `/privacy-policy` and `/terms-of-service`.
    3. sw.js: save `curl -sI` headers + body to `sw.js.txt`; PASS iff body contains `registration.unregister`, does not contain `precacheAndRoute`, and the `cache-control` header contains `max-age=0`.
    4. redirects: for `/test-glow`, `/test/codeblock`, `/ledgeriq` run `curl -sS -o /dev/null -w "%{url} %{http_code} %{redirect_url}\n" --max-time 30` (no -L) and append to `redirects.txt`; PASS iff `/test-glow` and `/test/codeblock` return 301/307/308 with redirect_url `https://work.randyellis.design/` and `/ledgeriq` → `/projects/ledgeriq`.
    5. deleted routes: append `/offline` and `/manifest.webmanifest` codes to `redirects.txt`; PASS iff both are 404 (a 308 to `/` is also acceptable for `/offline` if a redirect was added — note it).
    6. home: save `curl -s https://work.randyellis.design/` to `jsonld-home.txt` (raw HTML — no JS executed); PASS iff it contains `"@type":"Person"` and `"@type":"WebSite"` and `x.com/iamrandyellis` and NOT `twitter.com/iamrandyellis` and NOT `"@type":"Organization"` and NOT `FAQPage`.
    7. growit: save raw HTML to `jsonld-growit.txt`; PASS iff contains `"@type":"CreativeWork"` AND `"@type":"BreadcrumbList"` AND NOT `FAQPage`.
    8. blog: save `/blog/profits-not-pixels` raw HTML to `jsonld-blog.txt`; PASS iff contains `"@type":"Article"` AND `"@type":"BreadcrumbList"` AND `"@id":"https://work.randyellis.design/#person"`.
    Fill each row's Result (the observed values, e.g. "19 url / 19 lastmod"), Proof (relative path under `10-verification-screenshots/`), Status (`pass`/`fail`). Fill the Run log (commit SHA from Task 1, URL, UTC timestamp, "executor: Claude"). If ANY row fails: keep Status `fail`, write the exact discrepancy in Notes, and STOP — return a blocked result to the orchestrator naming the failing row (fix belongs to a gap-closure plan; do not patch production ad hoc). Commit `docs(10-09): record live curl verification results (rows 1-8)`.
  </action>
  <verify>
    <automated>cd .planning/phases/10-seo-remediation && test -s 10-verification-screenshots/robots.txt && test -s 10-verification-screenshots/sitemap.xml && test -s 10-verification-screenshots/sw.js.txt && test -s 10-verification-screenshots/jsonld-home.txt && [ "$(grep -c '| pass |' 10-VERIFICATION-CHECKLIST.md)" -ge 8 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 7 proof files exist and are non-empty
    - `grep -c "Disallow: /_next/" 10-verification-screenshots/robots.txt` == 0; `grep -o "<lastmod>" 10-verification-screenshots/sitemap.xml | wc -l` == 19
    - `grep -c "registration.unregister" 10-verification-screenshots/sw.js.txt` ≥ 1
    - `grep -c '"@type":"Person"' 10-verification-screenshots/jsonld-home.txt` ≥ 1 and `grep -c '"@type":"CreativeWork"' 10-verification-screenshots/jsonld-growit.txt` ≥ 1 and `grep -c '"@type":"Article"' 10-verification-screenshots/jsonld-blog.txt` ≥ 1
    - Checklist rows 1–8 have Status `pass` (or the plan returned blocked with the failing row named); Run log has a commit SHA
  </acceptance_criteria>
  <done>Rows 1–8 are proven against production with saved raw evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Attempt Rich Results Test + SW-registration checks via browser automation (rows 9–12)</name>
  <files>.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md, .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-home.png, .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-growit.png, .planning/phases/10-seo-remediation/10-verification-screenshots/rrt-blog.png, .planning/phases/10-seo-remediation/10-verification-screenshots/sw-registrations.png</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md rows 9–12
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-17, D-23
    - Available browser tools in this session (prefer `mcp__playwright__*` isolated browser per project memory; fall back to any other browser MCP)
  </read_first>
  <action>
    If a browser-automation tool is available: for each of the three URLs (`https://work.randyellis.design/`, `/projects/growit`, `/blog/profits-not-pixels`) open https://search.google.com/test/rich-results, enter the URL, run the test, wait for completion (up to 90 s), screenshot the full results panel to `rrt-home.png` / `rrt-growit.png` / `rrt-blog.png`, and read the result text: record detected item types and whether any ERROR is reported (warnings about non-standard properties `teamSize`/`role` are expected per D-14 and are not errors; "page not eligible for rich results" for CreativeWork/WebSite is expected — only Article may yield a rich-result type). Then open https://work.randyellis.design/, wait 3 s, evaluate `navigator.serviceWorker.getRegistrations().then(r => r.length)` → expect 0, screenshot to `sw-registrations.png`. Fill rows 9–12: Result (types detected / error count / registrations count), Proof (png path), Status `pass` only when 0 errors (rows 9–11) and 0 registrations (row 12); `fail` with details otherwise.
    If NO browser tool is available, or the tool is blocked (consent wall/captcha) after two attempts: leave rows 9–12 as `pending`, write "automation unavailable — human step in 10-10" in Notes, and do NOT fabricate screenshots.
    D-23: any RRT ERROR → Status `fail`; record the exact error message; the phase is blocked until fixed (Plan 10-10 handles the decision/fix loop). Commit `docs(10-09): record Rich Results / service worker automation results (rows 9-12)`.
  </action>
  <verify>
    <automated>cd .planning/phases/10-seo-remediation && for r in 9 10 11 12; do grep -E "^\| $r \|" 10-VERIFICATION-CHECKLIST.md | grep -Eq "\| (pass|fail|pending) \|" || { echo "row $r missing"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Rows 9–12 each have Status ∈ {pass, fail, pending} and a Notes entry; `pass` rows have an existing .png in Proof
    - If any png exists it is a real screenshot (> 10 KB) of the named tool, not a placeholder
    - No row is marked pass without a corresponding proof file
  </acceptance_criteria>
  <done>Browser-verifiable rows are either proven with screenshots or explicitly handed to the human in Plan 10-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → production site (read-only) | curl/browser fetches of public URLs |
| Human → deploy pipeline | Merge/deploy is a governance action reserved for the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-21 | Elevation of Privilege | Deploy step | mitigate | Claude never merges/pushes/deploys without explicit instruction in-session; Task 1 is a blocking human gate |
| T-10-22 | Repudiation | Verification evidence | mitigate | Raw curl outputs + screenshots stored under `10-verification-screenshots/` and referenced per row; run log carries commit SHA + timestamp |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- Checklist rows 1–8 pass with proof files; rows 9–12 pass or handed to 10-10; run log filled
</verification>

<success_criteria>
- ROADMAP Phase 10 SC1–SC4 observed live (robots, sitemap lastmod, kill-switch sw.js, server-rendered JSON-LD), SC5 partially (RRT) — completion in 10-10
- D-16, D-17, D-22, D-23 honored
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-09-SUMMARY.md` when done (deployed SHA, per-row outcomes, whether browser automation was available).
</output>
