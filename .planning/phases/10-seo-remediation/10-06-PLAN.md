---
phase: 10-seo-remediation
plan: 06
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - lib/project-utils.ts
  - app/projects/projects-client.tsx
  - app/projects/page.tsx
  - __tests__/lib/filter-projects-by-category.test.ts
  - __tests__/projects/projects-category-filter.test.tsx
autonomous: true
requirements: [SEO-04]
tags: [seo, search-action, projects-filter, tdd]

must_haves:
  truths:
    - "Visiting /projects?category=<term> shows only projects whose category, categories[], tags[] or name contain <term> (case-insensitive), with a visible 'Showing N projects matching “term”' line and a Clear-filter link; /projects with no param is unchanged (D-13 — the WebSite SearchAction urlTemplate must resolve to real filtering, not a fabricated endpoint)"
    - "/projects server-renders BreadcrumbList via <JsonLd> and no longer imports components/seo/structured-data"
    - "Existing tests that render ProjectsClient without a router (mobile-filter-spacing, waffle-grid-card) still pass — the filter tolerates a null useSearchParams()"
  artifacts:
    - path: "lib/project-utils.ts"
      provides: "filterProjectsByCategory(projects, term) pure helper"
      exports: ["filterProjectsByCategory"]
    - path: "app/projects/projects-client.tsx"
      provides: "useSearchParams-driven category filter over PROJECTS"
      contains: "useSearchParams"
    - path: "app/projects/page.tsx"
      provides: "<Suspense> around ProjectsClient + JsonLd breadcrumb"
      contains: "Suspense"
    - path: "__tests__/lib/filter-projects-by-category.test.ts"
      provides: "Pure-function contract tests"
      min_lines: 30
  key_links:
    - from: "lib/seo/json-ld.ts buildWebSiteSchema potentialAction.target.urlTemplate"
      to: "app/projects/projects-client.tsx ?category= handling"
      via: "same query-param name `category`"
      pattern: "searchParams?.get\\(\"category\"\\)"
    - from: "app/projects/page.tsx"
      to: "app/projects/projects-client.tsx"
      via: "<Suspense fallback={null}><ProjectsClient /></Suspense>"
      pattern: "<Suspense"
---

<objective>
Make the `/projects` page filterable through a `?category=` query parameter so the WebSite schema's SearchAction (`/projects?category={search_term_string}`, D-13) is a truthful, working endpoint — and migrate the projects index breadcrumb schema to the server-rendered JsonLd.

Purpose: D-13 locks "WebSite schema includes search action using /projects page category filter. Not fabricated — filter functionality exists." The codebase audit found NO filter in `app/projects/projects-client.tsx` (no useSearchParams/useState; it maps `PROJECTS` unconditionally). Emitting a SearchAction against a URL that does nothing would be fabricated markup (CRED-07), so the minimal URL-driven filter is implemented here. Deliberately minimal: one pure helper, one hook read, one status line, no new UI primitives.

Output: pure helper + tests, client filter + UI test, page.tsx with Suspense and JsonLd breadcrumb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/10-seo-remediation/10-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
From lib/data/types.ts: `Project = { name: string; category: string; categories?: string[]; tags: string[]; slug: string; ... }`; `PROJECT_CATEGORIES` const also exported (unused by the grid today).
From lib/project-utils.ts: currently exports only `getRandomProjects<T>(items, count)` — add the new helper here (kebab-case file already exists; named export convention).
From app/projects/projects-client.tsx: `"use client"`; imports `motion` from "motion/react", `Link`, `PROJECTS`, `cn`, ui Card/Badge/Button/Separator; renders `<motion.main>` → intro section → `<motion.div className="projects-grid-pattern">{PROJECTS.map((project) => (...card...))}</motion.div>`; no state, no hooks. Keep all markup/classes; only the data source for the map and an added status line change.
From app/projects/page.tsx: server component; `createPageMetadata({...})`; renders `<BreadcrumbStructuredData items={breadcrumbItems} />` (hardcoded `https://work.randyellis.design` URLs) + `<ProjectsClient />`.
From Plan 10-03: `JsonLd` (components/seo/json-ld.tsx), `buildBreadcrumbSchema` (lib/seo/json-ld.ts), `WEBSITE_URL` (lib/constants.ts).
Next 15 facts: `useSearchParams()` in a client component rendered by a statically prerendered page MUST be inside a `<Suspense>` boundary or `next build` fails with "useSearchParams() should be wrapped in a suspense boundary" — `npm run build` is NOT part of the verify gate, so the Suspense wrapper is mandatory by plan, not discovered by build. In Jest/jsdom without an App Router context `useSearchParams()` returns `null` → code must use `searchParams?.get("category")`.
Existing tests rendering ProjectsClient: `__tests__/projects/mobile-filter-spacing.test.tsx` (mocks motion/react + PROJECTS), `__tests__/projects/waffle/waffle-grid-card.test.tsx`. Mock pattern for next/navigation: `jest.mock("next/navigation", () => ({ useSearchParams: () => new URLSearchParams("category=Mobile App") }))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filterProjectsByCategory pure helper (RED → GREEN)</name>
  <files>lib/project-utils.ts, __tests__/lib/filter-projects-by-category.test.ts</files>
  <read_first>
    - lib/project-utils.ts (existing `getRandomProjects` style, exports)
    - lib/data/types.ts `Project` (category, categories?, tags, name) and lib/data/projects.ts lines 1-25 (growit: category "Mobile App", categories ["Mobile App","UI/UX"], tags incl. "Gardening")
    - __tests__/lib/get-random-projects.test.ts (test conventions for this module)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-13
  </read_first>
  <behavior>
    - `filterProjectsByCategory(PROJECTS, undefined)`, `(PROJECTS, null)`, `(PROJECTS, "")`, `(PROJECTS, "   ")` each return an array equal to `PROJECTS` (same length, same order)
    - `filterProjectsByCategory(PROJECTS, "Mobile App")` includes growit and every project whose `category` or `categories` contains "Mobile App"; `"mobile app"` and `"  MOBILE APP "` return the same result (case-insensitive, trimmed)
    - `filterProjectsByCategory(PROJECTS, "gardening")` returns exactly the projects with a tag containing "gardening" (growit) — tags are searched
    - `filterProjectsByCategory(PROJECTS, "growit")` matches by name; `"ui/ux"` matches via categories
    - `filterProjectsByCategory(PROJECTS, "zzz-no-such-category")` returns `[]`
    - the helper is generic: `filterProjectsByCategory([{ name: "A", category: "X", tags: [] }], "x")` works for any object with name/category/tags (categories optional); the input array is not mutated
  </behavior>
  <action>
    RED: create `__tests__/lib/filter-projects-by-category.test.ts` importing `filterProjectsByCategory` from "@/lib/project-utils" and `PROJECTS` from "@/lib/data/projects"; write the behaviors; run → fails (not exported). Commit `test(10-06): add failing filterProjectsByCategory tests`.
    GREEN: in `lib/project-utils.ts` add `export function filterProjectsByCategory<T extends { name: string; category: string; categories?: string[]; tags: string[] }>(projects: readonly T[], term: string | null | undefined): T[]` — `const q = term?.trim().toLowerCase() ?? ""; if (!q) return [...projects];` then filter where any of `[p.name, p.category, ...(p.categories ?? []), ...p.tags]` lowercased `.includes(q)`. JSDoc: "Backs the `/projects?category=` URL filter that the WebSite SearchAction advertises (Phase 10 D-13)". Run tests → green; `npx tsc --noEmit`; `npm run lint`. Commit `feat(10-06): add filterProjectsByCategory helper`.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/filter-projects-by-category.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/lib/filter-projects-by-category.test.ts` exits 0
    - `grep -c "export function filterProjectsByCategory" lib/project-utils.ts` == 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A tested, generic, case-insensitive filter helper exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the ?category= filter into ProjectsClient, wrap in Suspense, migrate the /projects breadcrumb to JsonLd</name>
  <files>app/projects/projects-client.tsx, app/projects/page.tsx, __tests__/projects/projects-category-filter.test.tsx</files>
  <read_first>
    - app/projects/projects-client.tsx (full — the `PROJECTS.map` grid at ~line 196, imports, intro section markup to place the status line after)
    - app/projects/page.tsx (full)
    - __tests__/projects/mobile-filter-spacing.test.tsx (full — motion + PROJECTS mock pattern; confirm what it asserts so the null-param path keeps it green)
    - __tests__/projects/waffle/waffle-grid-card.test.tsx lines 1-40 (also renders ProjectsClient)
    - components/seo/json-ld.tsx, lib/seo/json-ld.ts `buildBreadcrumbSchema`, lib/constants.ts
    - .planning/codebase/CONVENTIONS.md §Component Patterns (zinc palette, `cn`, no indigo/blue)
  </read_first>
  <behavior>
    `__tests__/projects/projects-category-filter.test.tsx` (mock `motion/react` like mobile-filter-spacing; mock `@/lib/data/projects` with 3 projects: {name "Alpha App", slug "alpha", category "Mobile App", categories ["Mobile App"], tags ["Gardening"]}, {name "Beta Web", slug "beta", category "Web App", tags ["Dashboard"]}, {name "Gamma", slug "gamma", category "Design System", categories ["UI/UX"], tags []} with the other required Project fields stubbed; mock `next/navigation` per test via `jest.doMock`/factory variable):
    - Test 1: with `useSearchParams` returning `new URLSearchParams("category=mobile app")`, rendering `<ProjectsClient />` shows "Alpha App" and NOT "Beta Web"/"Gamma", and shows text matching /Showing 1 project/ and a link with accessible name /clear filter/i whose href is "/projects"
    - Test 2: with `category=zzz`, no project card renders and text matching /No projects match/ is shown with the clear link
    - Test 3: with `useSearchParams` returning `null` (no router, as existing tests do), all 3 projects render and no status line / clear link is shown
    - Test 4 (source assertion): `app/projects/page.tsx` contains `<Suspense` wrapping `<ProjectsClient />`, imports `Suspense` from "react", contains `buildBreadcrumbSchema(` and `from "@/components/seo/json-ld"`, and does not contain `structured-data`
  </behavior>
  <action>
    RED: write the test file; run → fails (no filter/status line; page.tsx lacks Suspense/JsonLd). Commit `test(10-06): add failing /projects category filter tests`.

    GREEN — app/projects/projects-client.tsx: import `useSearchParams` from "next/navigation" and `filterProjectsByCategory` from "@/lib/project-utils". Inside `ProjectsClient`: `const searchParams = useSearchParams(); const categoryTerm = searchParams?.get("category")?.trim() ?? "";` (optional chaining is REQUIRED — returns null outside the App Router, e.g. in Jest); `const visibleProjects = filterProjectsByCategory(PROJECTS, categoryTerm);`. Replace `PROJECTS.map(` in the grid with `visibleProjects.map(`. Immediately after the intro `<p>` inside the first `<motion.section>`, when `categoryTerm` is non-empty render a `<p className="text-sm text-zinc-600 dark:text-zinc-400" role="status">` reading `Showing {visibleProjects.length} {visibleProjects.length === 1 ? "project" : "projects"} matching “{categoryTerm}” · ` followed by `<Link href="/projects" className="underline underline-offset-4">Clear filter</Link>`; when `categoryTerm` is non-empty AND `visibleProjects.length === 0` render instead `No projects match “{categoryTerm}”. ` + the same Clear-filter link (the grid then renders nothing). Add a short comment: "URL-driven filter backing the WebSite SearchAction (/projects?category=…) — Phase 10 D-13; keep the param name in sync with lib/seo/json-ld.ts". No other markup/animation changes; Prettier-clean.

    GREEN — app/projects/page.tsx: replace the `BreadcrumbStructuredData` import with `import { Suspense } from "react"; import { JsonLd } from "@/components/seo/json-ld"; import { buildBreadcrumbSchema } from "@/lib/seo/json-ld"; import { WEBSITE_URL } from "@/lib/constants";`; `breadcrumbItems` → `[{ name: "Home", url: WEBSITE_URL }, { name: "Projects", url: `${WEBSITE_URL}/projects` }]`; render `<JsonLd id="breadcrumb-jsonld" data={buildBreadcrumbSchema(breadcrumbItems)} />` then `<Suspense fallback={null}><ProjectsClient /></Suspense>` (comment: Suspense is required by Next 15 for `useSearchParams` on a statically prerendered route — without it `next build` fails; build is not in the verify gate, so do not remove). Metadata untouched.

    Run `npx jest __tests__/projects/projects-category-filter.test.tsx __tests__/projects/mobile-filter-spacing.test.tsx __tests__/projects/waffle/waffle-grid-card.test.tsx` → all green. `npx tsc --noEmit`, `npm run lint`. Optional sanity (not a gate): `npm run build` once to confirm no Suspense error; if it fails for unrelated pre-existing reasons, note it. Commit `feat(10-06): URL-driven /projects?category= filter behind the WebSite SearchAction; server-render projects breadcrumb`.
  </action>
  <verify>
    <automated>npx jest __tests__/projects/projects-category-filter.test.tsx __tests__/projects/mobile-filter-spacing.test.tsx __tests__/projects/waffle/waffle-grid-card.test.tsx 2>&1 | tail -12 && grep -q "<Suspense" app/projects/page.tsx && grep -q 'searchParams?.get("category")' app/projects/projects-client.tsx && ! grep -q "structured-data" app/projects/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - the three jest suites above exit 0
    - `grep -c 'searchParams?.get("category")' app/projects/projects-client.tsx` == 1; `grep -c "filterProjectsByCategory(PROJECTS" app/projects/projects-client.tsx` == 1; `grep -c "visibleProjects.map(" app/projects/projects-client.tsx` == 1; `grep -c "Clear filter" app/projects/projects-client.tsx` ≥ 1
    - `grep -c "<Suspense" app/projects/page.tsx` == 1; `grep -c "buildBreadcrumbSchema(" app/projects/page.tsx` == 1; `grep -c "structured-data\|https://work.randyellis.design" app/projects/page.tsx` == 0
    - `npx tsc --noEmit` and `npm run lint` exit 0
    - the param name `category` appears in both `lib/seo/json-ld.ts` (`?category={search_term_string}`) and `projects-client.tsx`
  </acceptance_criteria>
  <done>`/projects?category=<term>` really filters the grid; the SearchAction urlTemplate advertised by the WebSite schema resolves to working functionality; projects index breadcrumb is server-rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → React render | `?category=` is user-controlled text rendered in the status line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Tampering (reflected XSS) | projects-client status line | mitigate | Term is rendered as a React text node (auto-escaped); no dangerouslySetInnerHTML; no use in hrefs |
| T-10-16 | Denial of Service | filter over 8 projects | accept | O(n) over a constant 8-element array; term length is bounded by URL limits |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `npx jest __tests__/lib/filter-projects-by-category.test.ts __tests__/projects/projects-category-filter.test.tsx __tests__/projects/mobile-filter-spacing.test.tsx __tests__/projects/waffle/waffle-grid-card.test.tsx` green
- `npm run lint` → `npx tsc --noEmit` clean
</verification>

<success_criteria>
- D-13 honored without fabrication: the SearchAction target is a live filter
- /projects no longer depends on components/seo/structured-data
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-06-SUMMARY.md` when done. State explicitly that no URL-driven filter existed before this plan (planner finding vs CONTEXT D-13 wording) and what was added.
</output>
