---
phase: 10-seo-remediation
plan: 08
type: execute
wave: 3
depends_on: ["10-02", "10-04", "10-05", "10-06", "10-07"]
files_modified:
  - components/seo/structured-data.tsx
  - components/seo/project-faq.tsx
  - app/about/about-client.tsx
  - lib/data/static-data.ts
  - __tests__/seo/no-legacy-schema.test.ts
  - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md
  - .planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep
autonomous: true
requirements: [SEO-04, SEO-05]
tags: [seo, cleanup, structured-data, chameleon, verification-checklist]

must_haves:
  truths:
    - "components/seo/structured-data.tsx and components/seo/project-faq.tsx no longer exist; nothing in app/, components/, lib/ emits LocalBusiness, FAQPage, ProfessionalService or a standalone Organization, and the only `application/ld+json` emitter is components/seo/json-ld.tsx (D-08, D-09, D-10)"
    - "The Chameleon Collective URL on the About page and in WORK_EXPERIENCE points at the verified final destination and is not referenced by any schema (D-11 resolved: URL resolves → keep the visible link, normalized; not a Person.sameAs target)"
    - "A pre-populated 10-VERIFICATION-CHECKLIST.md with one row per live check and a screenshots directory exist so Plans 10-09/10-10 only fill in results (D-16, D-19)"
    - "Full gate (lint → tsc → test) passes on the fully migrated codebase"
  artifacts:
    - path: "__tests__/seo/no-legacy-schema.test.ts"
      provides: "Regression: legacy files absent; no forbidden @type values in app/components/lib; single JSON-LD emitter"
      min_lines: 30
    - path: ".planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md"
      provides: "13-row markdown table (check | command/URL | expected | result | proof | status | notes) with status pending"
      contains: "Rich Results Test"
    - path: ".planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep"
      provides: "Screenshot storage location (D-19)"
  key_links:
    - from: "__tests__/seo/no-legacy-schema.test.ts"
      to: "components/seo/structured-data.tsx"
      via: "fs.existsSync === false"
      pattern: "structured-data"
---

<objective>
Delete the legacy client-side schema modules now that every importer has migrated (Plans 10-05/06/07), pin that state with a regression test, resolve the Chameleon Collective URL on the visible About surface, author the verification checklist skeleton + screenshot directory, and run the full gate.

Purpose: Completes audit T-04/T-06 at the code level (no client-rendered or invalid schema left anywhere), closes the v1.0 carry-over blocker (Chameleon URL — D-11 / Phase 9 D-16) and prepares the D-16/D-19 proof artifact that the post-deploy plans fill in.

Output: two files deleted; regression test; Chameleon URL normalized; checklist skeleton; gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/research/SEO-AUDIT.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@CLAUDE.md

<interfaces>
Legacy modules to delete: `components/seo/structured-data.tsx` ("use client", next/script; exports Breadcrumb/Organization/Person/Website/ProfessionalService/FAQ/Article/CreativeWork/FractionalCDOService/LocalBusiness components) and `components/seo/project-faq.tsx` (ProjectFAQStructuredData). After wave 2 their importers are: none (verify with grep before deleting). `components/seo/related-content.tsx` is unrelated — keep.

`lib/analytics.ts` exports `trackStructuredDataView` and `trackLocalBusinessView` (generic trackers; tests in __tests__/lib/analytics-seo.test.ts) — KEEP; the regression test must grep for schema `@type` values, not function names.

Chameleon Collective occurrences (visible surfaces): `app/about/about-client.tsx` ~line 56 `companyUrl: "https://www.chameleon.co"` inside the `experience` array; `lib/data/static-data.ts` line 10 `link: "https://www.chameleon.co"` inside `WORK_EXPERIENCE`. Planner pre-check 2026-08-20: `curl -sS -o /dev/null -w "%{http_code} %{url_effective}" -L https://www.chameleon.co` → `200 https://chameleoncollective.com/` (308 → chameleon.co → chameleoncollective.com; page title "Chameleon Collective: Transformation experts. Lead, deliver, recruit."). It is the firm's homepage (Randy's fractional employer per the About page), not a page about Randy → valid as a visible employer link, NOT valid as Person.sameAs (Plan 10-03 already excludes it from schema).

Phase 9 precedent for the proof artifact: 09-CONTEXT D-14 matrix (one row per claim, status cells). D-19 format: markdown table, one row per check, screenshot column. Screenshot dir: `.planning/phases/10-seo-remediation/10-verification-screenshots/` (CONTEXT §specifics).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete legacy schema modules and pin the state with a regression test</name>
  <files>components/seo/structured-data.tsx, components/seo/project-faq.tsx, __tests__/seo/no-legacy-schema.test.ts</files>
  <read_first>
    - `grep -rn "components/seo/structured-data\|components/seo/project-faq" app components lib __tests__` output (must be empty except the two files themselves and tests that only assert their absence)
    - components/seo/structured-data.tsx lines 1-10 (confirm identity) and components/seo/project-faq.tsx lines 1-10
    - __tests__/seo/json-ld-wiring.test.tsx (Plan 10-05) and __tests__/seo/blog-post-json-ld.test.tsx (Plan 10-07) — avoid duplicating their assertions
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-08, D-09, D-10
  </read_first>
  <behavior>
    - Test 1: `fs.existsSync("components/seo/structured-data.tsx") === false` and `fs.existsSync("components/seo/project-faq.tsx") === false`
    - Test 2: recursively reading every `.ts/.tsx/.mdx` file under `app/`, `components/`, `lib/` (skip `node_modules`, `.next`), NO file contains any of the regexes `/"@type":\s*"(LocalBusiness|FAQPage|ProfessionalService|Organization)"/` or `/@type:\s*"(LocalBusiness|FAQPage|ProfessionalService|Organization)"/` (object-literal style) — the forbidden types of D-08
    - Test 3: the only files under app/, components/, lib/ containing the literal `application/ld+json` are `components/seo/json-ld.tsx` (single emitter, D-10); and no file under app/, components/, lib/ imports `next/script` together with `ld+json`
    - Test 4: no file under app/, components/, lib/ matches `/from "@\/components\/seo\/(structured-data|project-faq)"/`
  </behavior>
  <action>
    RED: write `__tests__/seo/no-legacy-schema.test.ts` with a small recursive file walker (fs.readdirSync with `withFileTypes`), run → Test 1 fails (files exist) and Test 2/3 fail (legacy file contains the forbidden types and a second ld+json emitter). Commit `test(10-08): add failing no-legacy-schema regression test`.
    GREEN: confirm zero importers with `grep -rln "components/seo/structured-data\|components/seo/project-faq" app components lib` (expected: only the two files). Delete `components/seo/structured-data.tsx` and `components/seo/project-faq.tsx` (`git rm`). Run the test → green. `npx tsc --noEmit` and `npm run lint`. Commit `feat(10-08): delete legacy client-side structured-data and project-faq modules`.
  </action>
  <verify>
    <automated>test ! -e components/seo/structured-data.tsx && test ! -e components/seo/project-faq.tsx && npx jest __tests__/seo/no-legacy-schema.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e components/seo/structured-data.tsx` and `test ! -e components/seo/project-faq.tsx` succeed
    - `npx jest __tests__/seo/no-legacy-schema.test.ts` exits 0
    - `grep -rln "application/ld+json" app components lib` prints exactly `components/seo/json-ld.tsx`
    - `grep -rn '"@type": "LocalBusiness"\|"@type": "FAQPage"\|"@type": "ProfessionalService"\|"@type": "Organization"' app components lib` returns 0 matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Legacy schema code is gone and cannot silently return.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the Chameleon Collective URL on visible surfaces (D-11)</name>
  <files>app/about/about-client.tsx, lib/data/static-data.ts</files>
  <read_first>
    - app/about/about-client.tsx lines 50-62 (the `experience` entry with `companyUrl`)
    - lib/data/static-data.ts lines 1-20 (WORK_EXPERIENCE entry with `link`)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-11; .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-16; .planning/STATE.md §Blockers ("Verify Chameleon Collective URL")
  </read_first>
  <action>
    Re-verify live: `curl -sS -o /dev/null -w "%{http_code} %{url_effective}\n" -L --max-time 20 https://www.chameleon.co`. Decision rule (D-11):
    - If the final status is 2xx (planner pre-check: 200 at `https://chameleoncollective.com/`): KEEP the link and normalize both occurrences to the final `url_effective` (expected `https://chameleoncollective.com/`) so the About page links directly without a 2-hop redirect chain — edit `companyUrl` in `app/about/about-client.tsx` and `link` in `lib/data/static-data.ts`. Do not change company name/title/period text.
    - If the final status is not 2xx or the page is unrelated to Chameleon Collective: remove `companyUrl` from the about-client entry (render the company name unlinked — check the JSX handles a missing `companyUrl` gracefully; if it does not, render a `<span>` instead of `<a>`) and remove `link` from the WORK_EXPERIENCE entry (check `WorkExperience` type: if `link` is required, set it to `""` only if consumers guard empty strings; otherwise make the field optional in lib/data/types.ts and guard consumers).
    Confirm no schema references Chameleon: `grep -rni "chameleon" lib/seo components/seo` → 0. Record the curl output and the decision in the SUMMARY; mark the STATE.md blocker "Verify Chameleon Collective URL" as resolved in the SUMMARY's state-update notes. `npx tsc --noEmit`, `npm run lint`, `npx jest __tests__/about-professional-experience.test.tsx` (existing About test) green. Commit `fix(10-08): resolve Chameleon Collective URL on About/work-experience surfaces (D-11)`.
  </action>
  <verify>
    <automated>! grep -q "https://www.chameleon.co\"" app/about/about-client.tsx lib/data/static-data.ts && ! grep -rqi "chameleon" lib/seo components/seo && npx jest __tests__/about-professional-experience.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'https://www.chameleon.co"' app/about/about-client.tsx lib/data/static-data.ts` == 0 (either normalized to the verified final URL or removed)
    - If kept: `grep -c "chameleoncollective.com" app/about/about-client.tsx` == 1 and the same for lib/data/static-data.ts, and `curl -sS -o /dev/null -w "%{http_code}" -L https://chameleoncollective.com/` prints 200 at execution time
    - `grep -rci "chameleon" lib/seo components/seo` totals 0
    - `npx jest __tests__/about-professional-experience.test.tsx` exits 0; `npx tsc --noEmit` exits 0
    - SUMMARY contains the curl output line and the keep/remove decision
  </acceptance_criteria>
  <done>The v1.0 carry-over blocker is closed with recorded evidence; no schema carries the URL.</done>
</task>

<task type="auto">
  <name>Task 3: Author the verification checklist skeleton + screenshots dir; run the full gate</name>
  <files>.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md, .planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep</files>
  <read_first>
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-16, D-17, D-18, D-19, D-22, D-23 and §specifics (screenshot dir)
    - .planning/research/SEO-AUDIT.md §Limitation and §Prioritized Action Plan item 5 (the required live checks incl. "confirm returning-visitor SW unregistration")
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-14 (matrix precedent: one row per check, status cells)
    - app/sitemap.ts (post-10-04: 7 static + 8 project + 4 blog URLs = 19 expected `<url>` entries) and app/robots.ts (post-10-02 AI group)
  </read_first>
  <action>
    Create `.planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep` (empty). Create `.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md` with: a header block (Phase 10 live verification; production only per D-22; status legend `pending | pass | fail | blocked`; rule D-23: any Rich Results Test ERROR blocks phase completion until fixed and re-verified; warnings for non-standard CreativeWork keys `teamSize`/`role` (D-14) and for SearchAction not being a rich result are expected and NOT blocking); then ONE markdown table with columns `# | Check | Command / URL | Expected | Result | Proof (screenshot/file) | Status | Notes` and exactly these 13 rows, all `Status = pending`, `Result`/`Proof` empty:
    1. robots.txt live — `curl -s https://work.randyellis.design/robots.txt` — no `/_next/` in any Disallow; `User-agent: GPTBot` group present with `Allow: /`; `Sitemap: https://work.randyellis.design/sitemap.xml`
    2. sitemap.xml live — `curl -s https://work.randyellis.design/sitemap.xml` — 19 `<url>` entries, every one has `<lastmod>`; includes /privacy-policy and /terms-of-service
    3. sw.js live — `curl -sI` + `curl -s` `https://work.randyellis.design/sw.js` — body contains `registration.unregister` and no `precacheAndRoute`; `Cache-Control` contains `max-age=0`
    4. /test-glow and /test/codeblock redirects — `curl -sI -o /dev/null -w "%{http_code} %{redirect_url}"` — 301 or 308 with Location ending in `/`
    5. Deleted PWA routes — `/offline` and `/manifest.webmanifest` — HTTP 404
    6. Home raw HTML JSON-LD — `curl -s https://work.randyellis.design/ | grep -o 'application/ld+json' | wc -l` ≥ 2 and body contains `"@type":"Person"` and `"@type":"WebSite"` (server-rendered proof, D-10)
    7. /projects/growit raw HTML — contains `"@type":"CreativeWork"` and `"@type":"BreadcrumbList"`, no `FAQPage`
    8. /blog/profits-not-pixels raw HTML — contains `"@type":"Article"` and `"@type":"BreadcrumbList"`
    9. Rich Results Test — https://work.randyellis.design/ (D-17) — no errors; detected items listed — screenshot `rrt-home.png`
    10. Rich Results Test — https://work.randyellis.design/projects/growit — no errors — screenshot `rrt-growit.png`
    11. Rich Results Test — https://work.randyellis.design/blog/profits-not-pixels — no errors; Article detected — screenshot `rrt-blog.png`
    12. Returning-visitor SW unregistration — browser DevTools → Application → Service Workers (or `navigator.serviceWorker.getRegistrations()` in console) after loading the site — zero registrations — screenshot `sw-registrations.png`
    13. Search Console — property for work.randyellis.design exists; sitemap `https://work.randyellis.design/sitemap.xml` submitted with status Success (D-18) — screenshot `gsc-sitemap.png`
    Below the table add a "## Run log" section with placeholders: deployment commit SHA, production URL, date/time, executor.

    Then run the full gate on the fully migrated tree: `npm run lint` → `npx tsc --noEmit` → `npm test`; paste the final counts into the SUMMARY (flaky animation-load-testing excluded per CLAUDE.md). Also run the phase-wide grep sweep and record it: `grep -rn "next/script" app components lib | grep -i "ld+json"` → 0; `grep -rn "twitter.com" lib/seo app/layout.tsx` → 0; `grep -rn "/_next/" app/robots.ts` → 0. Commit `docs(10-08): add live verification checklist skeleton; record full gate results`.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep && grep -c "| pending |" .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md | xargs test 13 -eq && grep -q "Rich Results Test" .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md && grep -q "Search Console" .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| pending |" 10-VERIFICATION-CHECKLIST.md` == 13; the table header contains `Proof (screenshot/file)` and `Status`
    - The checklist contains the literal strings "D-23", "robots.txt", "sitemap.xml", "sw.js", "/test-glow", "Rich Results Test", "Search Console", "getRegistrations"
    - `.planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep` exists
    - `npm run lint` exits 0; `npx tsc --noEmit` exits 0; `npm test` has no failures other than the documented flaky suite — counts recorded in SUMMARY
  </acceptance_criteria>
  <done>The proof artifact is ready to be filled post-deploy and the migrated codebase passes the full gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| About page → external link | Outbound employer link (Chameleon Collective) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Spoofing (phishing via stale redirect chain) | Chameleon link | mitigate | Normalized to the verified final HTTPS destination; curl evidence recorded |
| T-10-20 | Tampering (regression) | schema emitters | mitigate | no-legacy-schema test fails CI if forbidden `@type` or a second ld+json emitter reappears |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `npx jest __tests__/seo/no-legacy-schema.test.ts` green; legacy files absent
- Chameleon decision recorded with curl output; `grep -rci chameleon lib/seo components/seo` == 0
- Checklist skeleton has 13 pending rows; full gate green
</verification>

<success_criteria>
- ROADMAP Phase 10 SC3 fully satisfied at the code level (consolidated, server-rendered, valid-only, LocalBusiness/Organization/FAQ removed, Chameleon resolved)
- D-08, D-09, D-10, D-11, D-16, D-19 cited and implemented; proof artifact ready for D-22/D-23 execution
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-08-SUMMARY.md` when done, including gate counts, grep sweep results, and the Chameleon curl evidence. Note for STATE.md: blocker "Verify Chameleon Collective URL" resolved.
</output>
