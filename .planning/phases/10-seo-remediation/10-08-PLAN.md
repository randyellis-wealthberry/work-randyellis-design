---
phase: 10-seo-remediation
plan: 08
type: execute
wave: 3
depends_on: ["10-02", "10-04", "10-05", "10-06", "10-07"]
files_modified:
  - components/seo/structured-data.tsx
  - app/about/about-client.tsx
  - lib/data/static-data.ts
  - __tests__/seo/no-legacy-schema.test.ts
  - .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md
  - .planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep
autonomous: true
requirements: [SEO-04, SEO-05]
tags: [seo, cleanup, structured-data, chameleon, verification-checklist]

must_haves:
  truths:
    - "components/seo/structured-data.tsx no longer exists (components/seo/project-faq.tsx was already deleted by 09-02 and stays absent); nothing in app/, components/, lib/ emits LocalBusiness, FAQPage, ProfessionalService or a standalone Organization, and the only `application/ld+json` emitter is components/seo/json-ld.tsx (D-08, D-09, D-10)"
    - "The Chameleon Collective link on the About page and in WORK_EXPERIENCE (already normalized to https://chameleoncollective.com by 09-03) is re-verified live and is not referenced by any schema (D-11)"
    - "A pre-populated 10-VERIFICATION-CHECKLIST.md with one row per live check and a screenshots directory exist so Plans 10-09/10-10 only fill in results (D-16, D-19 — mirrors 09-CROSS-SURFACE-MATRIX.md)"
    - "Full gate (lint → tsc → test) passes on the fully migrated codebase"
  artifacts:
    - path: "__tests__/seo/no-legacy-schema.test.ts"
      provides: "Regression: legacy files absent; no forbidden @type values in app/components/lib; single JSON-LD emitter"
      min_lines: 30
    - path: ".planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md"
      provides: "13-row markdown table (check | command/URL | expected | result | proof | status | notes) with status pending"
      contains: "Rich Results Test"
    - path: ".planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep"
      provides: "Screenshot storage location (D-19)"
  key_links:
    - from: "__tests__/seo/no-legacy-schema.test.ts"
      to: "components/seo/structured-data.tsx"
      via: "fs.existsSync === false"
      pattern: "structured-data"
---

<objective>
Delete the legacy client-side schema module now that every importer has migrated (Plans 10-05/06/07), pin that state with a regression test, re-verify the Chameleon Collective URL that Phase 9 plan 09-03 already fixed, author the verification checklist skeleton + screenshot directory (mirroring Phase 9's matrix pattern), and run the full gate.

Purpose: Completes audit T-04/T-06 at the code level (no client-rendered or invalid schema left anywhere), confirms the v1.0 carry-over (Chameleon URL — closed by 09-03 under Phase 9 D-16; D-11 here) and prepares the D-16/D-19 proof artifact that the post-deploy plans fill in.

Output: `components/seo/structured-data.tsx` deleted; regression test; Chameleon verification recorded; checklist skeleton; gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/research/SEO-AUDIT.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-03-SUMMARY.md
@.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md
@CLAUDE.md

<interfaces>
Legacy module to delete: `components/seo/structured-data.tsx` ("use client", next/script). After Phase 9 plan 09-03 it has exactly six exports: `BreadcrumbStructuredData`, `PersonStructuredData`, `WebsiteStructuredData`, `ArticleStructuredData`, `CreativeWorkStructuredData`, `LocalBusinessStructuredData` (no page imports LocalBusiness; the other five lose their last importers in wave 2). `components/seo/project-faq.tsx` was ALREADY DELETED by 09-02 — do not recreate; only assert absence. After wave 2 the importer list must be empty (verify with grep before deleting). `components/seo/related-content.tsx` is unrelated — keep.

`lib/analytics.ts` exports `trackStructuredDataView` and `trackLocalBusinessView` (generic trackers; tests in __tests__/lib/analytics-seo.test.ts) — KEEP; the regression test must grep for schema `@type` values, not function names. After deleting structured-data.tsx nothing imports `trackStructuredDataView` — leave the export in place (used by tests).

Chameleon Collective (D-11): Phase 9 plan 09-03 (commit 1d82ca9) verified the chain `https://www.chameleon.co → 308 → https://chameleon.co/ → 308 → https://chameleoncollective.com/` and set BOTH visible occurrences to `https://chameleoncollective.com`: `app/about/about-client.tsx` ~line 56 `companyUrl` and `lib/data/static-data.ts` line 10 `link` (planner re-check 2026-08-21 confirms both read `https://chameleoncollective.com`). The remaining client Person schema still names "Chameleon Collective" in `description`/`knowsAbout` text — that schema is deleted in Task 1 and the new Person (Plan 10-03) carries no reference. The firm's homepage is not a page about Randy → never a Person.sameAs target.

Phase 9 precedent for the proof artifact: `.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md` — header with Method + Verdict Definitions, then one table row per claim/check with status cells (`agree | fixed→X | pulled | open`), plus a per-project "open for Randy" list. D-19 format for this phase: markdown table, one row per check, screenshot column. Screenshot dir: `.planning/phases/10-seo-remediation/10-verification-screenshots/` (CONTEXT §specifics).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the legacy schema module (project-faq already gone — assert absent) and pin the state with a regression test</name>
  <files>components/seo/structured-data.tsx, __tests__/seo/no-legacy-schema.test.ts</files>
  <read_first>
    - `grep -rn "components/seo/structured-data\|components/seo/project-faq" app components lib __tests__` output (must be empty except the legacy file itself and tests that only assert absence — `__tests__/seo/project-route-wiring.test.ts` asserts project-faq absence)
    - components/seo/structured-data.tsx lines 1-10 and its six `export function` lines (confirm identity; `ls components/seo/` must show project-faq.tsx absent already)
    - __tests__/seo/project-route-wiring.test.ts (Plan 10-05 updated it) and __tests__/seo/blog-post-json-ld.test.tsx (Plan 10-07) — avoid duplicating their assertions
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-08, D-09, D-10
  </read_first>
  <behavior>
    - Test 1: `fs.existsSync("components/seo/structured-data.tsx") === false` and `fs.existsSync("components/seo/project-faq.tsx") === false` (the latter already true — 09-02)
    - Test 2: recursively reading every `.ts/.tsx/.mdx` file under `app/`, `components/`, `lib/` (skip `node_modules`, `.next`), NO file contains any of the regexes `/"@type":\s*"(LocalBusiness|FAQPage|ProfessionalService|Organization)"/` or `/@type:\s*"(LocalBusiness|FAQPage|ProfessionalService|Organization)"/` (object-literal style) — the forbidden types of D-08
    - Test 3: the only file under app/, components/, lib/ containing the literal `application/ld+json` is `components/seo/json-ld.tsx` (single emitter, D-10); and no file under app/, components/, lib/ imports `next/script` together with `ld+json`
    - Test 4: no file under app/, components/, lib/ matches `/from "@\/components\/seo\/(structured-data|project-faq)"/`
  </behavior>
  <action>
    RED: write `__tests__/seo/no-legacy-schema.test.ts` with a small recursive file walker (fs.readdirSync with `withFileTypes`), run → Test 1 fails (structured-data.tsx exists) and Test 2/3 fail (legacy file contains `"@type": "LocalBusiness"` and a second ld+json emitter). Commit `test(10-08): add failing no-legacy-schema regression test`.
    GREEN: confirm zero importers with `grep -rln "components/seo/structured-data" app components lib` (expected: only the file itself). `git rm components/seo/structured-data.tsx`. Confirm `components/seo/project-faq.tsx` is already absent (no action — 09-02 deleted it; record "already absent" in the SUMMARY). Run the test → green. `npm run lint` → `npx tsc --noEmit`. Commit `feat(10-08): delete legacy client-side structured-data module`.
  </action>
  <verify>
    <automated>test ! -e components/seo/structured-data.tsx && test ! -e components/seo/project-faq.tsx && npx jest __tests__/seo/no-legacy-schema.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e components/seo/structured-data.tsx` and `test ! -e components/seo/project-faq.tsx` succeed
    - `npx jest __tests__/seo/no-legacy-schema.test.ts` exits 0
    - `grep -rln "application/ld+json" app components lib` prints exactly `components/seo/json-ld.tsx`
    - `grep -rn '"@type": "LocalBusiness"\|"@type": "FAQPage"\|"@type": "ProfessionalService"\|"@type": "Organization"' app components lib` returns 0 matches
    - `npm run lint` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Legacy schema code is gone and cannot silently return.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the Chameleon Collective URL that 09-03 fixed (D-11) — verify-only unless it no longer resolves</name>
  <files>app/about/about-client.tsx, lib/data/static-data.ts</files>
  <read_first>
    - .planning/phases/09-cross-surface-verification/09-03-SUMMARY.md §"D-16: Chameleon Collective URL Fix" (what was verified and changed)
    - app/about/about-client.tsx lines 50-62 (the `experience` entry with `companyUrl: "https://chameleoncollective.com"`)
    - lib/data/static-data.ts lines 1-20 (WORK_EXPERIENCE entry with `link: "https://chameleoncollective.com"`)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-11; .planning/STATE.md §Blockers ("Verify Chameleon Collective URL")
  </read_first>
  <action>
    Re-verify live: `curl -sS -o /dev/null -w "%{http_code} %{url_effective}\n" -L --max-time 20 https://chameleoncollective.com` (the URL 09-03 set) and confirm both files still carry exactly `https://chameleoncollective.com` (`grep -n "chameleoncollective.com" app/about/about-client.tsx lib/data/static-data.ts` → 1 hit each). Decision rule (D-11):
    - If the status is 2xx (expected — 09-03 recorded HTTP/2 200 in one hop): NO EDIT. Record the curl line and "09-03 fix confirmed; no change" in the SUMMARY.
    - If the status is not 2xx or the page is unrelated to Chameleon Collective: remove `companyUrl` from the about-client entry (render the company name unlinked — check the JSX handles a missing `companyUrl`; if not, render a `<span>` instead of `<a>`) and remove `link` from the WORK_EXPERIENCE entry (check the `WorkExperience` type: if `link` is required, make it optional in lib/data/types.ts and guard consumers).
    Confirm no schema references Chameleon: `grep -rni "chameleon" lib/seo components/seo` → 0 (the legacy module with the name mentions was deleted in Task 1). Record the outcome in the SUMMARY and note for STATE.md that the "Verify Chameleon Collective URL" blocker is closed (fixed by 09-03, re-verified here). Run `npx jest __tests__/about-professional-experience.test.tsx` (existing About test) — green. Commit only if a file changed: `fix(10-08): remove unresolvable Chameleon Collective link (D-11)`; otherwise no commit for this task.
  </action>
  <verify>
    <automated>! grep -q "https://www.chameleon.co\"" app/about/about-client.tsx lib/data/static-data.ts && ! grep -rqi "chameleon" lib/seo components/seo && npx jest __tests__/about-professional-experience.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'https://www.chameleon.co"' app/about/about-client.tsx lib/data/static-data.ts` == 0
    - If kept (expected): `grep -c "chameleoncollective.com" app/about/about-client.tsx` == 1 and the same for lib/data/static-data.ts, and `curl -sS -o /dev/null -w "%{http_code}" -L https://chameleoncollective.com` prints 2xx at execution time
    - `grep -rci "chameleon" lib/seo components/seo` totals 0
    - `npx jest __tests__/about-professional-experience.test.tsx` exits 0
    - SUMMARY contains the curl output line and the verify-only / removal decision, citing 09-03 as the fix
  </acceptance_criteria>
  <done>The Chameleon URL state set by 09-03 is confirmed live with recorded evidence; no schema carries the URL.</done>
</task>

<task type="auto">
  <name>Task 3: Author the verification checklist skeleton + screenshots dir (mirroring 09-CROSS-SURFACE-MATRIX.md); run the full gate</name>
  <files>.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md, .planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep</files>
  <read_first>
    - .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md lines 1-40 (header/Method/Verdict-Definitions/table layout to mirror — Phase 9 D-14 proof-artifact pattern)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-16, D-17, D-18, D-19, D-22, D-23 and §specifics (screenshot dir)
    - .planning/research/SEO-AUDIT.md §Limitation and §Prioritized Action Plan item 5 (the required live checks incl. "confirm returning-visitor SW unregistration")
    - app/sitemap.ts (post-10-04: 7 static + 8 project + 4 blog URLs = 19 expected `<url>` entries) and app/robots.ts (post-10-02 AI group)
  </read_first>
  <action>
    Create `.planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep` (empty). Create `.planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md` mirroring the 09-CROSS-SURFACE-MATRIX.md shape: a header block (Produced date, Requirement SEO-05, Method: "each check run against production https://work.randyellis.design per D-22; proof saved under 10-verification-screenshots/"), a "Status Definitions" list (`pending | pass | fail | blocked`), the rule D-23 (any Rich Results Test ERROR blocks phase completion until fixed and re-verified; warnings for the non-standard CreativeWork keys `teamSize`/`role` (D-14) and for SearchAction not being a rich result are expected and NOT blocking); then ONE markdown table with columns `# | Check | Command / URL | Expected | Result | Proof (screenshot/file) | Status | Notes` and exactly these 13 rows, all `Status = pending`, `Result`/`Proof` empty:
    1. robots.txt live — `curl -s https://work.randyellis.design/robots.txt` — no `/_next/` in any Disallow; `User-agent: GPTBot` group present with `Allow: /`; `Sitemap: https://work.randyellis.design/sitemap.xml`
    2. sitemap.xml live — `curl -s https://work.randyellis.design/sitemap.xml` — 19 `<url>` entries, every one has `<lastmod>`; includes /privacy-policy and /terms-of-service
    3. sw.js live — `curl -sI` + `curl -s` `https://work.randyellis.design/sw.js` — body contains `registration.unregister` and no `precacheAndRoute`; `Cache-Control` contains `max-age=0`
    4. /test-glow and /test/codeblock redirects — `curl -sI -o /dev/null -w "%{http_code} %{redirect_url}"` — 301 or 308 with Location ending in `/`
    5. Deleted PWA routes — `/offline` and `/manifest.webmanifest` — HTTP 404
    6. Home raw HTML JSON-LD — `curl -s https://work.randyellis.design/ | grep -o 'application/ld+json' | wc -l` ≥ 2 and body contains `"@type":"Person"` and `"@type":"WebSite"` (server-rendered proof, D-10)
    7. /projects/growit raw HTML — contains `"@type":"CreativeWork"` and `"@type":"BreadcrumbList"`, no `FAQPage`
    8. /blog/profits-not-pixels raw HTML — contains `"@type":"Article"` and `"@type":"BreadcrumbList"`
    9. Rich Results Test — https://work.randyellis.design/ (D-17) — no errors; detected items listed — screenshot `rrt-home.png`
    10. Rich Results Test — https://work.randyellis.design/projects/growit — no errors — screenshot `rrt-growit.png`
    11. Rich Results Test — https://work.randyellis.design/blog/profits-not-pixels — no errors; Article detected — screenshot `rrt-blog.png`
    12. Returning-visitor SW unregistration — browser DevTools → Application → Service Workers (or `navigator.serviceWorker.getRegistrations()` in console) after loading the site — zero registrations — screenshot `sw-registrations.png`
    13. Search Console — property for work.randyellis.design exists; sitemap `https://work.randyellis.design/sitemap.xml` submitted with status Success (D-18) — screenshot `gsc-sitemap.png`
    Below the table add a "## Run log" section with placeholders: deployment commit SHA, production URL, date/time, executor; and a "## Open for Randy" section (empty list) mirroring the matrix's open-items pattern.

    Then run the full gate on the fully migrated tree: `npm run lint` → `npx tsc --noEmit` → `npm test`; paste the final counts into the SUMMARY (flaky animation-load-testing excluded per CLAUDE.md). Also run the phase-wide grep sweep and record it: `grep -rn "next/script" app components lib | grep -i "ld+json"` → 0; `grep -rn "twitter.com" lib/seo app/layout.tsx` → 0; `grep -rn "/_next/" app/robots.ts` → 0. Commit `docs(10-08): add live verification checklist skeleton; record full gate results`.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep && grep -c "| pending |" .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md | xargs test 13 -eq && grep -q "Rich Results Test" .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md && grep -q "Search Console" .planning/phases/10-seo-remediation/10-VERIFICATION-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| pending |" 10-VERIFICATION-CHECKLIST.md` == 13; the table header contains `Proof (screenshot/file)` and `Status`
    - The checklist contains the literal strings "D-23", "robots.txt", "sitemap.xml", "sw.js", "/test-glow", "Rich Results Test", "Search Console", "getRegistrations", "Status Definitions", "Run log"
    - `.planning/phases/10-seo-remediation/10-verification-screenshots/.gitkeep` exists
    - `npm run lint` exits 0; `npx tsc --noEmit` exits 0; `npm test` has no failures other than the documented flaky suite — counts recorded in SUMMARY
  </acceptance_criteria>
  <done>The proof artifact is ready to be filled post-deploy and the migrated codebase passes the full gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| About page → external link | Outbound employer link (Chameleon Collective) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Spoofing (phishing via stale redirect chain) | Chameleon link | mitigate | 09-03 normalized to the verified final HTTPS destination; re-verified here with curl evidence |
| T-10-20 | Tampering (regression) | schema emitters | mitigate | no-legacy-schema test fails CI if forbidden `@type` or a second ld+json emitter reappears |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `npx jest __tests__/seo/no-legacy-schema.test.ts` green; legacy module absent
- Chameleon re-verification recorded with curl output; `grep -rci chameleon lib/seo components/seo` == 0
- Checklist skeleton has 13 pending rows; full gate green
</verification>

<success_criteria>
- ROADMAP Phase 10 SC3 fully satisfied at the code level (consolidated, server-rendered, valid-only, LocalBusiness/Organization/FAQ removed, Chameleon resolved)
- D-08, D-09, D-10, D-11, D-16, D-19 cited and implemented; proof artifact ready for D-22/D-23 execution
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-08-SUMMARY.md` when done, including gate counts, grep sweep results, and the Chameleon curl evidence (citing 09-03 as the fix). Note for STATE.md: blocker "Verify Chameleon Collective URL" closed.
</output>
