---
phase: 10-seo-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/robots.ts
  - next.config.js
  - middleware.ts
  - app/test/codeblock
  - test
  - __tests__/seo/robots.test.ts
  - __tests__/seo/redirects.test.ts
  - __tests__/seo/middleware-indexing.test.ts
autonomous: true
requirements: [SEO-02, SEO-03]
tags: [seo, robots, redirects, middleware, crawlability, tdd]

must_haves:
  truths:
    - "robots.txt no longer disallows /_next/ for any user-agent, still disallows /private/, /admin/, /api/, /drafts/, and explicitly allows the AI crawlers named in D-21 (audit T-02, T-07)"
    - "GET /test-glow and GET /test/<anything> respond with a permanent redirect to / (D-04, D-07); no /test route directory exists in app/ (audit T-03)"
    - "middleware no longer carries dead noindex checks for /test-glow and /offline, still noindexes /admin and /api, and serves /sw.js with a non-immutable Cache-Control so the kill switch is never stuck in HTTP cache"
    - "Every app/ route has been classified against nav + sitemap + documented structure (D-05) with the outcome recorded; /admin/* retained and noindexed (D-06); the untracked Docusaurus scaffold at repo-root test/ is disposed of"
  artifacts:
    - path: "app/robots.ts"
      provides: "Two rule groups: * and AI-crawler list, both allow / with the 4 disallows; sitemap + host"
      contains: "GPTBot"
    - path: "next.config.js"
      provides: "Single redirects() with /ledgeriq, /test-glow, /test/:path* entries; no next-pwa comment block"
      contains: "/test/:path*"
    - path: "middleware.ts"
      provides: "shouldBlockIndexing = /admin + /api only; /sw.js cache rule"
      contains: "/sw.js"
    - path: "__tests__/seo/robots.test.ts"
      provides: "Asserts robots() output shape (no /_next/, AI allow group, disallow list, sitemap URL)"
      min_lines: 30
    - path: "__tests__/seo/redirects.test.ts"
      provides: "Asserts next.config.js redirects() entries and single definition"
      min_lines: 20
  key_links:
    - from: "app/robots.ts"
      to: "lib/constants.ts WEBSITE_URL"
      via: "sitemap/host fields"
      pattern: "WEBSITE_URL"
    - from: "next.config.js redirects()"
      to: "/ (home)"
      via: "permanent redirect for /test-glow and /test/:path*"
      pattern: "source: \"/test/:path\\*\""
    - from: "middleware.ts applyCacheHeaders"
      to: "/sw.js"
      via: "pathname === \"/sw.js\" branch before the .js branch"
      pattern: "pathname === \"/sw.js\""
---

<objective>
Unblock crawler rendering and remove stray routes: drop `/_next/` from robots.txt and open the site to AI crawlers with one consistent rule group, add permanent redirects for the deleted `/test-glow` and `/test/*` URLs, clean the dead middleware checks and add a no-cache rule for `/sw.js`, audit `app/` for other dev-only routes, and dispose of the untracked Docusaurus scaffold at repo-root `test/`.

Purpose: Audit T-02 (robots blocks the JS/CSS Googlebot needs), T-03 (stray test routes), T-07 (inconsistent AI-crawler policy — decided open per D-21). D-04..D-07 lock deletion + 301s + admin retention + per-route audit.

Output: rewritten `app/robots.ts`; `next.config.js` with a single merged `redirects()` and no next-pwa comment block; trimmed `middleware.ts`; `app/test/` gone; root `test/` gone (or gitignored with a note if it turns out not to be scaffold); three Jest tests; route-audit table in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-remediation/10-CONTEXT.md
@.planning/phases/10-seo-remediation/10-RESEARCH.md
@.planning/research/SEO-AUDIT.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Verified state 2026-08-20. -->

app/robots.ts today: rule `*` with `disallow: ["/private/", "/admin/", "/api/", "/_next/", "/drafts/"]` plus five `disallow: "/"` groups for GPTBot, CCBot, ChatGPT-User, anthropic-ai, Claude-Web; `sitemap: `${WEBSITE_URL}/sitemap.xml``; `host: WEBSITE_URL`. Type: `MetadataRoute.Robots` — `rules[].userAgent` accepts `string | string[]`.

next.config.js today: lines 1-~60 are a commented-out `withPWA = require("next-pwa")({...})` block; `async redirects()` is defined TWICE (≈line 238 and ≈line 293, both returning only the `/ledgeriq → /projects/ledgeriq` permanent entry — the second definition silently wins); `async headers()` ≈line 249 has rules for `/_next/static/(.*)` (prod only), `/fonts/(.*)`, `/(.*)`; last lines: `module.exports = withMDX(nextConfig);` and a commented `// module.exports = withPWA(withMDX(nextConfig));`. `node -e "require('./next.config.js').redirects().then(console.log)"` works (so a Jest test can require it).

middleware.ts today: `applyCacheHeaders` first branch `pathname.match(/\.(js|css)$/)` stamps `Cache-Control: public, max-age=31536000, immutable` (+ `CDN-Cache-Control`, `Vercel-CDN-Cache-Control`) — this currently applies to `/sw.js`; `shouldBlockIndexing(pathname)` returns true for `/admin`, `/api`, `/test-glow`, `/offline`; `INDEXABLE_HOSTS` = work.randyellis.design, localhost, 127.0.0.1; matcher `"/((?!_next/static|_next/image|favicon.ico).*)"` (so `/sw.js` passes through middleware). `vercel.json` and `next.config.js headers()` define NO Cache-Control for `/sw.js` or generic `*.js` — so the middleware rule is the only place to add it (header-triplication rule: verified no sync needed, record that in SUMMARY).

Route state: `app/test-glow/` does NOT exist; `app/test/codeblock/` exists but is an EMPTY, untracked directory (its page.tsx was deleted in commit 02a9aa1); `app/offline/` is deleted by Plan 10-01 (parallel wave — do not touch it here); `app/admin/email-test/page.tsx` and `app/admin/test-results-demo/page.tsx` exist (keep, D-06); `app/data/` is an empty dir; nav links in app/header.tsx: `/`, `/about`, `/projects`, `/blog`; sitemap static URLs: `/`, `/projects`, `/about`, `/blog`, `/metis`; footer-linked legal pages: `/privacy-policy`, `/terms-of-service`.

Repo-root `test/` (untracked, `git status` shows `?? test/`): Docusaurus 3.10.2 scaffold — `package.json` name "test", `docusaurus.config.ts`, `sidebars.ts`, `docs/{intro.mdx,tutorial-basics,tutorial-extras}`, `blog/{2019-05-28-first-blog-post.mdx,2019-05-29-long-blog-post.mdx,2021-08-01-mdx-blog-post.mdx,2021-08-26-welcome,authors.yml,tags.yml}`, `src/{components/HomepageFeatures,css/custom.css,pages/{index.tsx,index.module.css,markdown-page.mdx}}`, `static/`, `node_modules/` (779 entries). These are the stock `create-docusaurus` classic-template files. `tsconfig.json` already excludes `test`; jest's custom `testPathIgnorePatterns` does NOT cover `test/node_modules` (only `<rootDir>/node_modules/`).

Next.js request pipeline order: next.config `headers` → next.config `redirects` → middleware → rewrites — so a `/test-glow` request is redirected before middleware runs; the middleware `/test-glow` check is dead code once the redirect exists.

Existing middleware-adjacent test: `__tests__/security/csp-headers.test.ts` (self-mocked; not a pattern to copy). For a real middleware test use a `/** @jest-environment node */` docblock so `Request`/`Headers` globals exist, then `import { middleware } from "@/middleware"` and `import { NextRequest } from "next/server"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: robots.ts — remove /_next/, open AI crawlers as one consistent group (RED → GREEN)</name>
  <files>app/robots.ts, __tests__/seo/robots.test.ts</files>
  <read_first>
    - app/robots.ts (current rules)
    - lib/constants.ts (WEBSITE_URL)
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-06, D-21
    - .planning/research/SEO-AUDIT.md T-02, T-07
    - __tests__/seo/project-metadata.test.ts lines 1-12 (import style for a `@/` module test)
  </read_first>
  <behavior>
    - Test 1: `robots()` returns `rules` as an array where NO rule's `disallow` (string or array) contains `"/_next/"`
    - Test 2: NO rule has `disallow` equal to `"/"` or `["/"]` (no crawler is fully blocked — D-21)
    - Test 3: the `userAgent: "*"` rule has `allow: "/"` and `disallow` exactly `["/private/", "/admin/", "/api/", "/drafts/"]` (D-06 keeps /admin/ blocked; D-15-of-research keeps the other three)
    - Test 4: exactly one rule has an array `userAgent` that includes every token in ["GPTBot", "ChatGPT-User", "OAI-SearchBot", "ClaudeBot", "Claude-Web", "Claude-SearchBot", "Claude-User", "anthropic-ai", "CCBot", "PerplexityBot", "Perplexity-User", "Google-Extended", "Applebot-Extended", "Bytespider", "meta-externalagent"], with `allow: "/"` and the same 4-item `disallow` as `*`
    - Test 5: `sitemap === `${WEBSITE_URL}/sitemap.xml`` and `host === WEBSITE_URL`
  </behavior>
  <action>
    RED: create `__tests__/seo/robots.test.ts` importing `robots` (default export) from `@/app/robots` and `WEBSITE_URL` from `@/lib/constants`; write the five tests; run `npx jest __tests__/seo/robots.test.ts` — Tests 1, 2, 4 MUST fail against the current file. Commit `test(10-02): add failing robots.ts policy test`.

    GREEN: rewrite `app/robots.ts` to return exactly two rule groups: (a) `{ userAgent: "*", allow: "/", disallow: ["/private/", "/admin/", "/api/", "/drafts/"] }` (the `/_next/` entry is removed per D-21/T-02 — Googlebot needs `/_next/static` JS/CSS to render; Next.js never recommends blocking it); (b) `{ userAgent: [the 15 AI tokens above in that order], allow: "/", disallow: ["/private/", "/admin/", "/api/", "/drafts/"] }` — a dedicated group is required because a crawler that matches a specific `User-agent` group ignores `*`, so the disallows must be repeated to keep /admin/ and /api/ protected for AI crawlers too. Keep `sitemap` and `host`. Add a short comment above the AI group citing D-21 (open to AI crawlers for AEO/GEO visibility; trade-off accepted) and one above `*` noting `/_next/` must never be re-added. Run the test — MUST pass. Commit `feat(10-02): open robots.txt to AI crawlers and stop blocking /_next/`.
  </action>
  <verify>
    <automated>npx jest __tests__/seo/robots.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/seo/robots.test.ts` exits 0 with 5 passing tests
    - `grep -c "/_next/" app/robots.ts` == 0
    - `grep -c 'disallow: "/"' app/robots.ts` == 0
    - `grep -c "GPTBot\|ClaudeBot\|PerplexityBot\|Google-Extended" app/robots.ts` ≥ 4
    - `grep -c '"/admin/"' app/robots.ts` == 2 (both groups keep it)
  </acceptance_criteria>
  <done>robots.ts emits one `*` group and one AI-crawler group, both allowing `/` with the four disallows, no `/_next/` anywhere, proven by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: next.config.js — single redirects() with /test-glow and /test/:path* → /, drop the next-pwa comment block; middleware cleanup + /sw.js cache rule (RED → GREEN)</name>
  <files>next.config.js, middleware.ts, __tests__/seo/redirects.test.ts, __tests__/seo/middleware-indexing.test.ts</files>
  <read_first>
    - next.config.js (full — both `async redirects()` definitions, the `headers()` block, the commented next-pwa block at top and the commented `withPWA` export at bottom)
    - middleware.ts (full — `applyCacheHeaders` branch order, `shouldBlockIndexing`, `INDEXABLE_HOSTS`, matcher)
    - vercel.json `headers` (confirm no Cache-Control rule matches `/sw.js` — triplication check)
    - .planning/codebase/ARCHITECTURE.md §Anti-Patterns "Header triplication"
    - .planning/phases/10-seo-remediation/10-CONTEXT.md D-04, D-07 and §Integration Points
    - .planning/phases/10-seo-remediation/10-RESEARCH.md §Pitfall 1 (middleware after route deletion)
  </read_first>
  <behavior>
    - redirects.test.ts Test 1: `await require("<root>/next.config.js").redirects()` contains `{ source: "/ledgeriq", destination: "/projects/ledgeriq", permanent: true }` (existing entry preserved)
    - redirects.test.ts Test 2: it contains `{ source: "/test-glow", destination: "/", permanent: true }` and `{ source: "/test/:path*", destination: "/", permanent: true }` (D-07)
    - redirects.test.ts Test 3: the raw source text of next.config.js matches `/async redirects\(\)/g` exactly once, and contains neither `next-pwa` nor `withPWA` (D-01 clean break; the duplicate-definition bug is fixed)
    - middleware-indexing.test.ts (node env) Test 1: `middleware(new NextRequest("https://work.randyellis.design/sw.js", { headers: { host: "work.randyellis.design" } }))` returns a response whose `Cache-Control` header contains `max-age=0` and does NOT contain `immutable`
    - middleware-indexing.test.ts Test 2: `/admin/email-test` and `/api/newsletter/stats` on the prod host get `X-Robots-Tag: noindex, nofollow`
    - middleware-indexing.test.ts Test 3: `/about` on host work.randyellis.design has NO `X-Robots-Tag`; `/about` on host `preview-abc.vercel.app` gets `noindex, nofollow`
    - middleware-indexing.test.ts Test 4: the source of middleware.ts does not contain `"/test-glow"` or `"/offline"`
  </behavior>
  <action>
    RED: write both tests. `__tests__/seo/redirects.test.ts`: `const cfg = require(path.join(process.cwd(), "next.config.js"))` then `await cfg.redirects()`; read the file text with `fs.readFileSync` for Test 3. `__tests__/seo/middleware-indexing.test.ts`: first line docblock `/** @jest-environment node */`; `import { NextRequest } from "next/server"; import { middleware } from "@/middleware";` — if `NextRequest` cannot be constructed in Jest, fall back to a minimal stub `{ nextUrl: new URL(url), headers: new Headers({ host }) } as unknown as NextRequest` (middleware only reads `request.nextUrl.pathname` and `request.headers.get("host")`). Run both — redirects Tests 2-3 and middleware Tests 1 and 4 MUST fail. Commit `test(10-02): add failing redirect and middleware indexing tests`.

    GREEN — next.config.js: delete the commented-out next-pwa block at the top of the file (every line from `// Temporarily commented out next-pwa` through the closing `// });` of that block) and the trailing `// Temporarily disable PWA to test build` + `// module.exports = withPWA(withMDX(nextConfig));` comment lines, leaving `module.exports = withMDX(nextConfig);` as the sole export (D-01/D-02 clean break — Plan 10-01 removed the package). Merge the two `async redirects()` definitions into ONE (keep the position of the first, delete the second under `// REDIRECTS`) returning, in order: the existing `/ledgeriq` entry, `{ source: "/test-glow", destination: "/", permanent: true }`, `{ source: "/test/:path*", destination: "/", permanent: true }` (D-07: deleted dev routes 301 to home; `:path*` covers `/test/codeblock` and any future `/test/...` bookmark). Add a one-line comment above the test entries referencing audit T-03 / D-04.

    GREEN — middleware.ts: in `applyCacheHeaders`, insert a new FIRST branch `if (pathname === "/sw.js") { set "Cache-Control": "public, max-age=0, must-revalidate", "CDN-Cache-Control": "max-age=0", "Vercel-CDN-Cache-Control": "max-age=0"; return response; }` with a comment: the kill-switch service worker must always be re-fetched so returning visitors pick it up on their first visit instead of waiting up to 24h on a cached immutable copy (D-03). Triplication check: `next.config.js headers()` and `vercel.json` define no `/sw.js`/`*.js` Cache-Control, so no sync edit is needed — state this in the SUMMARY. In `shouldBlockIndexing`, remove the `pathname.startsWith("/test-glow")` and `pathname.startsWith("/offline")` lines (routes deleted: `/test-glow` is redirected before middleware runs; `/offline` is deleted by Plan 10-01) leaving `/admin` and `/api` (D-06 keeps admin noindexed). Do not change `INDEXABLE_HOSTS` or the matcher. Run both tests — MUST pass. Then `npx tsc --noEmit` and `npm run lint`. Commit `feat(10-02): redirect deleted test routes, merge redirects(), drop dead middleware checks, no-cache sw.js`.
  </action>
  <verify>
    <automated>npx jest __tests__/seo/redirects.test.ts __tests__/seo/middleware-indexing.test.ts 2>&1 | tail -12 && node -e "require('./next.config.js').redirects().then(r=>{const s=r.map(x=>x.source);if(!s.includes('/test-glow')||!s.includes('/test/:path*'))process.exit(1)})" && echo REDIRECTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/seo/redirects.test.ts __tests__/seo/middleware-indexing.test.ts` exits 0
    - `grep -c "async redirects()" next.config.js` == 1
    - `grep -c "next-pwa\|withPWA" next.config.js` == 0
    - `grep -c 'source: "/test-glow"' next.config.js` == 1 and `grep -c 'source: "/test/:path\*"' next.config.js` == 1
    - `grep -c '"/test-glow"\|"/offline"' middleware.ts` == 0
    - `grep -c 'pathname === "/sw.js"' middleware.ts` == 1 and `grep -c 'startsWith("/admin")' middleware.ts` == 1
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Deleted test URLs 301 to `/`, next.config.js has one redirects() and no PWA remnants, middleware has no dead route checks and serves /sw.js uncached — all test-proven.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the empty app/test tree, run the D-05 dev-route audit, dispose of the root test/ Docusaurus scaffold</name>
  <files>app/test/codeblock, test</files>
  <read_first>
    - AGENTS.md line 26 (stray-route warning — `.md/.mdx` inside app/ become routes)
    - app/header.tsx (nav hrefs: /, /about, /projects, /blog) and app/sitemap.ts (static URLs) — the "linked from nav/sitemap" criterion of D-05
    - app/admin/email-test/page.tsx and app/admin/test-results-demo/page.tsx headers (confirm both are admin tooling → keep per D-06)
    - test/package.json, test/docusaurus.config.ts, test/README.md (confirm stock Docusaurus scaffold — name "test", @docusaurus/core 3.10.2)
    - tsconfig.json `exclude` (already lists "test") and jest.config.js `testPathIgnorePatterns`
  </read_first>
  <action>
    1. `app/test`: confirm `find app/test -type f` prints nothing, then `rmdir app/test/codeblock app/test`. Also confirm `test ! -d app/test-glow` (already absent). D-04 satisfied: no test route files or directories remain.
    2. D-05 route audit: run `find app -type f \( -name "page.tsx" -o -name "page.mdx" -o -name "route.ts" \) | sort` and classify EVERY page route (not API) into one of: nav-linked (header), sitemap-listed, documented structure (about/projects/blog/metis + footer legal pages privacy-policy/terms-of-service), admin (`/admin/*` — keep + noindex per D-06, already disallowed in robots `*` group and stamped `X-Robots-Tag` by middleware), or CANDIDATE. Additionally run `grep -rln -i "test\|demo\|debug\|dev" app --include=page.tsx` and inspect each hit. Expected outcome (verify, don't assume): the only candidates are `/offline` (deleted by Plan 10-01), `/test/*` (removed in step 1), and `/admin/test-results-demo` + `/admin/email-test` (kept, noindexed, D-06). If any OTHER page route is neither nav/sitemap/documented nor admin: per D-10 delete it if it is clearly internal scratch (no imports from elsewhere, no data dependency), otherwise add `robots: { index: false, follow: false }` to its exported `metadata` and list it in the SUMMARY as "noindexed, may be revived". Record the full classification table (route → class → action) in the SUMMARY. Also note the empty `app/data/` directory (no route — Next ignores dirs without page files; leave it, or `rmdir app/data` if empty; record which).
    3. Root `test/` disposal: verify the scaffold markers exist — `test/docs/intro.mdx`, `test/blog/2019-05-28-first-blog-post.mdx`, `test/src/pages/index.tsx`, `grep -q '"@docusaurus/core"' test/package.json` — and that `git ls-files test | wc -l` is 0 (untracked). If ALL markers match → it is stock `create-docusaurus` output with no project content: `rm -rf test` (it is not served by Next, not type-checked, and its `node_modules` is not excluded by jest's custom ignore list, so it is pure hazard). If any marker is missing or `test/docs`/`test/blog` contain non-stock files → do NOT delete; instead append `/test/` to `.gitignore` under a `# local scratch` comment and flag "root test/ left in place — contains non-scaffold content, Randy to decide" in the SUMMARY.
    4. Commit `chore(10-02): remove empty app/test dirs, record dev-route audit, dispose root test/ scaffold` (if `test/` was deleted nothing is staged for it — it was untracked; the SUMMARY is the record).
  </action>
  <verify>
    <automated>test ! -d app/test && test ! -d app/test-glow && ( test ! -d test || grep -q "^/test/$" .gitignore ) && echo ROUTES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d app/test` and `test ! -d app/test-glow` succeed
    - Either `test ! -d test` succeeds, OR `.gitignore` contains the line `/test/` and the SUMMARY has a "root test/ left in place" note
    - `ls app/admin/email-test/page.tsx app/admin/test-results-demo/page.tsx` both exist (D-06)
    - SUMMARY contains a route-audit table with one row per `app/**/page.{tsx,mdx}` found by the find command, each with class ∈ {nav, sitemap, documented, admin, deleted, noindexed}
    - `grep -c "/admin/" app/robots.ts` ≥ 1 (admin still disallowed)
  </acceptance_criteria>
  <done>No `/test*` route directories exist, every app/ route is classified with admin kept + noindexed, and the stray Docusaurus scaffold is removed (or explicitly gitignored and flagged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crawler → robots.txt | Public policy file; controls what search/AI crawlers fetch |
| Client → next.config redirects | Redirect targets are fixed server-side constants |
| Client → middleware | Sets cache + indexing headers per path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | robots.txt AI group | mitigate | AI group repeats the `/admin/`, `/api/`, `/private/`, `/drafts/` disallows (crawlers matching a specific UA group ignore `*`); test 4 asserts it |
| T-10-06 | Spoofing (open redirect) | next.config redirects | mitigate | Destinations are the literal `/`; no user-controlled destination; test asserts exact entries |
| T-10-07 | Tampering | middleware /sw.js cache rule | mitigate | Only weakens caching for the kill switch path (max-age=0), never loosens security headers; triplication check recorded |
| T-10-08 | Information Disclosure | /admin/* retained | accept | Noindexed via robots + X-Robots-Tag (D-06); admin pages hold no secrets (email-test UI, mock test results) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx jest __tests__/seo/robots.test.ts __tests__/seo/redirects.test.ts __tests__/seo/middleware-indexing.test.ts` green
- `grep -c "/_next/" app/robots.ts` == 0; `grep -c "async redirects()" next.config.js` == 1
- `npm run lint` → `npx tsc --noEmit` clean
- SUMMARY carries the D-05 route-audit table and the root `test/` disposition
</verification>

<success_criteria>
- ROADMAP Phase 10 SC2 satisfied at code level: robots.ts no longer disallows `/_next/`; `/test/*` routes deleted with 301s (live proof in Plan 10-09)
- D-04, D-05, D-06, D-07, D-21 implemented and cited
</success_criteria>

<output>
Create `.planning/phases/10-seo-remediation/10-02-SUMMARY.md` when done, including the route-audit table, the header-triplication check result for `/sw.js`, and the root `test/` disposition.
</output>
