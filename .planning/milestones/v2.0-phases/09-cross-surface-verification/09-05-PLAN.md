---
phase: 09-cross-surface-verification
plan: 05
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CRED-09]
tags: [verification, gate, docs]

must_haves:
  truths:
    - "A stale-claim grep across app/, lib/, components/seo/ finds no claim that Phase 8/9 changed on one surface but left on another — every pattern returns 0 (or only the documented allowed hit) and the results are recorded in the matrix (D-19, ROADMAP SC2)"
    - "`npm run lint`, `npx tsc --noEmit`, `npm test` pass clean across the touched surface, with only the known-flaky animation-load-testing baseline tolerated (D-19, ROADMAP SC3)"
    - "ROADMAP.md reflects reality: Phases 6–8 (done, previously unchecked) are checked and their progress-table rows read Complete (Claude's-discretion item, CONTEXT.md)"
  artifacts:
    - path: ".planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md"
      provides: "Part G filled: grep table with counts + gate results line"
      contains: "## Part G"
    - path: ".planning/ROADMAP.md"
      provides: "Phases 6–8 checked; progress table rows Complete"
      contains: "[x] **Phase 8"
  key_links:
    - from: "Part G grep table"
      to: "app/, lib/, components/seo/"
      via: "grep -rn patterns with expected 0 counts"
      pattern: "grep"
---

<objective>
Run the D-19 verification gate the way v1.0's remediation did — stale-claim grep across `app/`, `lib/`, `components/seo/` mirroring "stale-content grep clean", then lint → tsc → test — record the results as Part G of the matrix, and reconcile the ROADMAP.md checkboxes for Phases 6–8 in the docs commit.

Purpose: ROADMAP SC2 and SC3 are the phase's exit criteria; recording them in the matrix makes CRED-09 auditable, and the roadmap reconciliation stops the milestone from reading as 20% done when it is at Phase 9.
Output: matrix Part G; ROADMAP.md with Phases 6–8 checked; SUMMARY with the verification line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md
@.planning/phases/09-cross-surface-verification/09-02-SUMMARY.md
@.planning/phases/09-cross-surface-verification/09-03-SUMMARY.md
@.planning/phases/09-cross-surface-verification/09-04-SUMMARY.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@CLAUDE.md

<interfaces>
<!-- Stale-claim grep register. Scope: app/ lib/ components/seo/ (D-19). Each pattern's expected result after Plans 02–04. -->
| # | Pattern (grep -rn, fixed strings unless noted) | Scope | Expected | Why |
|---|---|---|---|---|
| G01 | `ProjectFAQStructuredData` \| `seo/project-faq` \| `FAQPage` | app lib components/seo | 0 | D-09 FAQ schema pulled |
| G02 | `-w FAQStructuredData`, `OrganizationStructuredData`, `ProfessionalServiceStructuredData`, `FractionalCDOServiceStructuredData` | app lib components/seo | 0 | D-10 dead exports deleted |
| G03 | `Logistics Innovation` \| `Design Leadership at Scale` \| `Addvanced Career Tracker` \| `Modern Design System Innovation` \| `Design System Excellence` \| `Strategic Design Leadership Case Study` | app lib components/seo | 0 | old hand-typed titles/breadcrumb (D-01/D-04) |
| G04 | `Case Study \|` (title separator pattern) in `app/projects/*/page.tsx` | app/projects | 0 | rambis/echo/nagarro old title style |
| G05 | `A1-Home.png` \| `digital-accessibility-strategy.png` \| `echo/img1.jpg` in `app/projects/*/page.tsx` | app/projects/*/page.tsx | 0 | bespoke OG files replaced by thumbnail (D-05) — the same assets may legitimately appear inside `*-client.tsx` galleries, so scope is page.tsx only |
| G06 | `longDescription` in `app/projects/[slug]/page.tsx` and `app/projects/*/page.tsx` | route files | 0 | D-03 |
| G07 | `type: "website"` in `app/projects/[slug]/page.tsx` | route file | 0 | D-06 |
| G08 | `15,000 to 18,000` \| `36 countries` \| `100+ qualified leads` \| `\$50M+ in business impact` in `app/projects/nagarro/page.tsx` | route file | 0 | nagarro metadata now derived; the same phrases in nagarro-client.tsx are visible copy and allowed |
| G09 | `\$50M+` | app lib components | exactly 1: app/projects/nagarro/nagarro-client.tsx | D-15 |
| G10 | `\$50M value delivered` \| `\$50M+ product value` | app lib components | 0 | D-15 |
| G11 | `184\.4` \| `16% revenue` \| `12% shipment` \| `1,000 beta` \| `10,000+ active drivers` | app lib components/seo | 0 | Echo NDA figures (CRED-08) |
| G12 | `chameleon\.co"` (old host, with closing quote) | app lib components/seo | 0 | D-16 |
| G13 | `example\.com` | app lib components | exactly 1: app/admin/email-test/page.tsx `placeholder=` attribute | D-17 |
| G14 | `-i lorem` \| `ipsum` | app lib components | 0 | D-17 |
| G15 | `25K+ cities` | components/seo | 0 | dead FAQ claim gone (data metric "25K+" in lib/data/projects.ts is legitimate) |
| G16 | `6 awards` \| `100K+` (in app/projects/opengraph-image.tsx, app/about) | app | 0 | v1.0 remediation regressions guard |
| G17 | `METIS` \| `Metis` in components/seo | components/seo | 0 | orphan FAQ project gone |

Gate commands (CLAUDE.md verify order): `npm run lint` → `npx tsc --noEmit` → `npm test`. Known baseline: `__tests__/performance/animation-load-testing.test.tsx` is flaky (FPS in jsdom) — a failure there alone is not a regression; anything else failing is.

ROADMAP.md reconciliation facts (planning time): lines ~32–34 read `- [x] **Phase 6…**`, `- [ ] **Phase 7…**`, `- [ ] **Phase 8…**`; the Progress table rows: `| 7. Bespoke Convergence | v2.0 | 1/1 | Structural wiring complete (content pending Phase 8) | 2026-08-15 |`, `| 8. Content Rewrite & Credibility Guardrails | v2.0 | 0/TBD | Not started | - |`. Phases 6–8 have NO `.planning/phases/06-*`, `07-*`, `08-*` directories (they were executed without GSD PLAN/SUMMARY artifacts, like v1.0's Phases 1–3), so `gsd-sdk query phase.complete 7` will throw "Phase 7 not found". STATE.md says Phase 8 completed 2026-08-16 (CNT-01..08 complete) and REQUIREMENTS.md already has TPL/MIG/CNT/CRED-06..08 checked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale-claim grep sweep and record Part G</name>
  <files>.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md</files>
  <read_first>
    - .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md (Part G placeholder; Parts D–F for context)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md lines 60–68 (the "stale-content grep clean" verification-line style to mirror)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-19 and ROADMAP §Phase 9 SC2
    - 09-02/03/04 SUMMARYs (to add any project-specific old strings they list that are not in G01–G17)
  </read_first>
  <action>
    Run every pattern G01–G17 from the interfaces block exactly as scoped (use `grep -rn`; add `-w` where noted; exclude nothing else). Extend the register with any additional "old value" strings that Plans 02–04 SUMMARYs list as replaced (e.g., each `fixed→X` row's "before" value in Part A) — each becomes G18+ with expected 0. For each pattern record `count` and, when non-zero, the file:line list. Expected results: all 0 except G09 (exactly one hit, nagarro-client.tsx) and G13 (exactly one hit, the email-test `placeholder=` attribute). If any pattern returns an unexpected hit, that is a stale claim: fix it in place following D-18 (visible copy wins), add/adjust the corresponding Part A row (`fixed→X` or `pulled`), and re-run until clean — do not suppress by narrowing the pattern. Fill `## Part G — Stale-claim grep & verification gate (D-19)` with a table `| # | Pattern | Scope | Expected | Actual | Hits (file:line) | Status |` and leave a `### Verification gate` subsection for Task 2. Also add a final line to Part G's intro: the exact shell used (so it can be re-run by the verifier).
  </action>
  <verify>
    <automated>f=.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md; grep -q "^## Part G" $f && [ "$(grep -c '^| G[0-9]' $f)" -ge 17 ] && ! grep -rn "ProjectFAQStructuredData\|seo/project-faq" app lib components/seo && ! grep -rn "Logistics Innovation\|Design Leadership at Scale\|Addvanced Career Tracker\|Modern Design System Innovation\|Design System Excellence" app lib components/seo && [ "$(grep -rn '50M+' app lib components | wc -l | tr -d ' ')" = "1" ] && [ "$(grep -rn 'example\.com' app lib components | wc -l | tr -d ' ')" = "1" ] && ! grep -rn "184\.4\|16% revenue\|12% shipment\|1,000 beta\|10,000+ active drivers" app lib components/seo</automated>
  </verify>
  <acceptance_criteria>
    - Part G table has ≥ 17 rows (`grep -c '^| G[0-9]' MATRIX` ≥ 17), each with Expected, Actual, Status columns filled
    - Live re-run: G01, G02, G03, G06, G07, G08, G10, G11, G12, G14, G15, G16, G17 each print nothing; G04/G05 print nothing within `app/projects/*/page.tsx`
    - `grep -rn '\$50M+' app lib components | wc -l` prints 1 and the hit is `app/projects/nagarro/nagarro-client.tsx`
    - `grep -rn 'example\.com' app lib components | wc -l` prints 1 and the hit is `app/admin/email-test/page.tsx` containing `placeholder=`
    - Any unexpected hit found during the sweep has both a code fix and a Part A row (SUMMARY lists them; if none, SUMMARY says "no stale hits")
    - Part G intro contains the literal shell used (`grep -rn`)
  </acceptance_criteria>
  <done>Stale-claim grep register executed and recorded; every unexpected hit fixed and logged; SC2 evidence in the matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Run the lint → tsc → test gate and record the verification line</name>
  <files>.planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md</files>
  <read_first>
    - CLAUDE.md §Verifying Changes (order; `npm run build` is NOT a gate; no `type-check` script)
    - .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md Part G `### Verification gate` placeholder
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md line ~67 (post-fix verification sentence format)
  </read_first>
  <action>
    Run `rm -rf .next/types` first (stale build artifacts for deleted routes otherwise fail tsc; only errors outside `.next/` count), then in order `npm run lint` (must exit 0), `npx tsc --noEmit` (must exit 0), `npm test` (capture the Jest summary line: suites/tests passed/failed). Also run `npx jest __tests__/seo` and `npx jest __tests__/projects` separately and capture their summaries. If anything other than the known-flaky `__tests__/performance/animation-load-testing.test.tsx` fails, it is a regression from this phase: fix it (or, if it is a pre-existing `describe.skip`/stale suite unrelated to Phase 9 changes, document it precisely with file + reason) and re-run. Write the `### Verification gate` subsection in Part G with: the three commands + exit codes, the Jest totals, the flaky-baseline note, and one v1.0-style sentence, e.g. "Post-fix verification: `npm run lint` clean, `npx tsc --noEmit` clean, N/M tests pass (1 known-flaky FPS baseline), stale-claim grep clean (17 patterns; 2 documented allowed hits)". Do not modify test files to make them pass except to fix a genuine Phase 9-caused break.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npx jest __tests__/seo __tests__/projects && grep -q "### Verification gate" .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md && grep -q "npx tsc --noEmit" .planning/phases/09-cross-surface-verification/09-CROSS-SURFACE-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm test` output shows 0 failed test suites, or exactly one failed suite and it is `__tests__/performance/animation-load-testing.test.tsx` (documented flaky)
    - `npx jest __tests__/seo` exits 0 (project-metadata + project-route-wiring)
    - Part G contains `### Verification gate` with the literal strings `npm run lint`, `npx tsc --noEmit`, `npm test`, and a "Tests:" totals line copied from Jest
    - Part G contains the sentence starting "Post-fix verification:"
  </acceptance_criteria>
  <done>SC3 evidence recorded; gate green (flaky baseline excepted).</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP.md checkboxes for Phases 6–8 (docs commit)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 25–40 (phase checklist) and the `## Progress` table
    - .planning/STATE.md (Phase 6 complete 2026-08-15; Phase 7 structural wiring complete 2026-08-15; Phase 8 CNT-01..08 complete 2026-08-16)
    - .planning/REQUIREMENTS.md traceability table (TPL/MIG/CNT/CRED-06..08 rows already Complete)
    - .planning/phases/ directory listing (confirms no 06-/07-/08- directories exist)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md "Claude's Discretion" last bullet
  </read_first>
  <action>
    Prefer the SDK: run `gsd-sdk query phase.complete 7` and `gsd-sdk query phase.complete 8` (Phase 6 is already checked; run `phase.complete 6` only if its checkbox is still `[ ]`). Because Phases 6–8 have no phase directories, expect `GSDError: Phase N not found`; if instead they succeed, review `git diff .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`, keep the ROADMAP/REQUIREMENTS changes, and revert any STATE.md `Current Position`/`stopped_at` regression that moves the cursor away from Phase 9 (Phase 9 is mid-execution; its own completion will rewrite STATE.md). If the handler throws (expected), fall back to a minimal direct edit of `.planning/ROADMAP.md` only: change `- [ ] **Phase 7: Bespoke Convergence** …` to `- [x] … (completed 2026-08-15)` and `- [ ] **Phase 8: Content Rewrite & Credibility Guardrails** …` to `- [x] … (completed 2026-08-16)`; in the `## Progress` table set row 7 Status to `Complete` (keep `1/1` and `2026-08-15`) and row 8 to `pre-GSD (traced) | Complete | 2026-08-16` (mirroring how v1.0 Phases 1–3 are recorded); leave Phase 9/10 rows alone (Phase 9's own completion flow updates them). Do not touch STATE.md or REQUIREMENTS.md in the fallback path. Verify with `gsd-sdk query roadmap.analyze` (must parse; Phases 7 and 8 report complete) and record which path (handler vs fallback) was taken in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q '^\- \[x\] \*\*Phase 7' .planning/ROADMAP.md && grep -q '^\- \[x\] \*\*Phase 8' .planning/ROADMAP.md && grep -q '^| 8\. Content Rewrite[^|]*| v2.0 |[^|]*| Complete' .planning/ROADMAP.md && grep -q '^| 7\. Bespoke Convergence[^|]*| v2.0 |[^|]*| Complete' .planning/ROADMAP.md && gsd-sdk query roadmap.analyze >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\- \[x\] \*\*Phase [678]' .planning/ROADMAP.md` returns 3
    - Progress table rows for Phase 7 and Phase 8 contain `| Complete` (row 8 no longer `Not started`; row 7 no longer `Structural wiring complete`)
    - `grep -c '^\- \[ \] \*\*Phase 9' .planning/ROADMAP.md` returns 1 (Phase 9 left unchecked — its own completion flow owns it)
    - `gsd-sdk query roadmap.analyze` exits 0
    - `git diff --quiet HEAD -- .planning/STATE.md` exits 0 in the fallback path (STATE.md untouched); if the handler path succeeded, STATE.md `Phase:` line still names Phase 9 (or 09)
    - SUMMARY states "handler" or "fallback (phase dirs 06–08 absent)"
  </acceptance_criteria>
  <done>ROADMAP checklist and progress table match reality for Phases 6–8; Phase 9/10 rows untouched; method recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs (ROADMAP/STATE) → GSD tooling | Docs are consumed by `gsd-sdk` handlers; malformed edits break later automation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering (planning-doc corruption) | .planning/ROADMAP.md fallback edit | mitigate | Handler-first; fallback limited to two checkbox lines + two table rows; `gsd-sdk query roadmap.analyze` must parse afterwards. |
| T-09-12 | Repudiation (unverifiable "clean" claim) | Part G | mitigate | Exact grep shell + counts + Jest totals recorded so a verifier can re-run. |
| T-09-SC | Tampering (supply chain) | npm installs | accept | No new dependencies. |
</threat_model>

<verification>
- tsc note: at planning time `npx tsc --noEmit` reported errors ONLY under `.next/types/**` (stale build artifacts referencing deleted routes `/ledgeriq`, `/archive`; tsconfig includes `.next/types/**/*.ts`). If you see those, run `rm -rf .next/types` and re-run; only errors outside `.next/` count as failures. Never treat `npm run build` as a gate (CLAUDE.md).
- Part G complete: 17+ grep rows with actuals; verification-gate sentence present
- `npm run lint`, `npx tsc --noEmit` exit 0; `npm test` clean except documented flaky baseline
- ROADMAP Phases 6–8 checked; `roadmap.analyze` parses
</verification>

<success_criteria>
- ROADMAP §Phase 9 SC2 (grep sweep) and SC3 (lint/tsc/test) satisfied and evidenced in the matrix
- Every D-19 element (lint → tsc → test, matrix, stale-claim grep) recorded
- Roadmap reconciled per the CONTEXT.md discretion item
</success_criteria>

<output>
Create `.planning/phases/09-cross-surface-verification/09-05-SUMMARY.md` when done, quoting the "Post-fix verification:" sentence and stating the roadmap-reconciliation path taken.
</output>
