---
phase: 09-cross-surface-verification
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - components/seo/structured-data.tsx
  - lib/metadata.ts
  - app/about/page.tsx
  - app/about/about-client.tsx
  - app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx
autonomous: true
requirements: [CRED-09]
tags: [seo, json-ld, credibility, sweep]

must_haves:
  truths:
    - "components/seo/structured-data.tsx exports only schema components that have at least one importer; the zero-importer FAQStructuredData (\"25K+ cities\"), OrganizationStructuredData, ProfessionalServiceStructuredData and FractionalCDOServiceStructuredData are gone (D-10)"
    - "Every career-wide $50M surface reads `$50M` + `product value` (no `+`); the Nagarro-specific `$50M+ in business impact` stays on nagarro-client.tsx (D-15, D-18)"
    - "The Chameleon Collective link on /about points at a URL that returns 200 without a redirect chain; structured-data.tsx has no Chameleon URL to fix (name mentions only) (D-16)"
    - "No `example.com`, lorem, or placeholder copy remains in app/, lib/, components/ apart from HTML `placeholder=` attributes (D-17)"
  artifacts:
    - path: "components/seo/structured-data.tsx"
      provides: "Live schema components only: Breadcrumb, Person, Website, Article, CreativeWork, LocalBusiness"
      contains: "export function CreativeWorkStructuredData"
    - path: "lib/metadata.ts"
      provides: "base description with `$50M product value delivered`"
      contains: "$50M product value delivered"
    - path: "app/about/about-client.tsx"
      provides: "Chameleon Collective companyUrl resolving 200"
      contains: "https://chameleoncollective.com"
  key_links:
    - from: "app/about/page.tsx openGraph.description"
      to: "lib/metadata.ts base description"
      via: "same career-wide wording `$50M product value delivered`"
      pattern: "\\$50M product value delivered"
    - from: "app/about/about-client.tsx experience[0].companyUrl"
      to: "https://chameleoncollective.com"
      via: "curl -sI returns HTTP 200"
      pattern: "chameleoncollective\\.com"
---

<objective>
Purge dead schema exports from `components/seo/structured-data.tsx` (D-10), align the career-wide `$50M` wording across its surfaces and confirm the Nagarro-specific claim is the only `$50M+` (D-15/D-18), verify and fix the Chameleon Collective URL (D-16), and run the bounded placeholder sweep with fixes (D-17). Every change here becomes a matrix row in Plan 04.

Purpose: These are the v1.0/Phase-5 carry-overs and the "landmine" class of dead code that carries stale claims — the same reasoning that deleted `app/data.ts`.
Output: leaner `structured-data.tsx`; `$50M` wording uniform; `about-client.tsx` link fixed; placeholder "Further Reading" links removed from one blog post; a findings list handed to Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-surface-verification/09-CONTEXT.md
@.planning/phases/09-cross-surface-verification/09-PATTERNS.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@components/seo/structured-data.tsx
@lib/metadata.ts
@app/about/page.tsx
@app/about/about-client.tsx
@CLAUDE.md

<interfaces>
<!-- Importer census at planning time (grep -rlw over app lib components __tests__, excluding structured-data.tsx itself). Re-verify before deleting. -->
| Export in components/seo/structured-data.tsx | Importers | Disposition |
|---|---|---|
| BreadcrumbStructuredData | 6 (projects/page, addvanced/page, metis/page, about/page, blog/layout, ui/breadcrumb-nav) + Plan 02 routes | keep |
| OrganizationStructuredData | 0 | DELETE (D-10) |
| PersonStructuredData | 1 (app/layout.tsx) | keep |
| WebsiteStructuredData | 1 (app/layout.tsx) | keep |
| ProfessionalServiceStructuredData | 0 | DELETE (D-10) |
| FAQStructuredData (site-level FAQPage, "25K+ cities") | 0 | DELETE (D-10) |
| ArticleStructuredData | 4 (blog mdx pages) | keep |
| CreativeWorkStructuredData | 1+ ([slug] + Plan 02 routes) | keep |
| FractionalCDOServiceStructuredData | 0 (contains Chameleon Collective mentions at ~605, ~664) | DELETE (D-10) |
| LocalBusinessStructuredData | 1 | keep (Phase 10 decides its fate) |
Shared imports: `useEffect` / `trackStructuredDataView` are used by LocalBusinessStructuredData (live) — keep; `createAbsoluteUrl` used by Person/Website/Article/CreativeWork/LocalBusiness — keep.

$50M surfaces at planning time (grep '\$50M' app lib components):
- app/opengraph-image.tsx:143 `$50M` + label `Product Value`            → career-wide, already canonical
- app/about/opengraph-image.tsx:264 `$50M` + label `Product Value`      → career-wide, already canonical
- app/about/about-client.tsx:43 `value: "$50M"`, label "Product Value"  → career-wide, already canonical
- components/core/animated-number-basic.tsx (counter to 50 with `$`/`M`, caption line 102 "in product value") → career-wide, already canonical
- app/page.tsx:361 "Portfolio optimized for $50M product value"        → career-wide, already canonical
- lib/metadata.ts:25 "...2.5M+ users impacted, $50M+ product value delivered." → FIX: drop the `+` (visible copy wins, D-18)
- app/about/page.tsx:25 "...4 awards won, $50M value delivered. See how..." → FIX: "$50M product value delivered"
- app/projects/nagarro/nagarro-client.tsx:577 "...and $50M+ in business impact." → Nagarro-specific, KEEP as-is (D-15)
- app/projects/nagarro/page.tsx:7 "generating $50M+ in business impact" → removed by Plan 02 derivation (metadata = project.description)

Chameleon Collective at planning time:
- app/about/about-client.tsx:58 `companyUrl: "https://www.chameleon.co"` → curl: 308 → https://chameleon.co/ → 308 → https://chameleoncollective.com/ → 200
- components/seo/structured-data.tsx:87 (Person description) and :133 (knowsAbout "Chameleon Collective Partnership") → name mentions, no URL; :605/:664 are inside FractionalCDOServiceStructuredData (deleted in Task 1)

Placeholder sweep hits at planning time (grep app lib components):
- example.com: app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx:135-137 — "### Further Reading" list of three fake links → DELETE the section; app/admin/email-test/page.tsx:205 `placeholder="test@example.com"` → HTML attribute, ALLOWED (log as excluded)
- lorem/ipsum: 0 hits
- "placeholder" as copy: 0 hits (all hits are `placeholder=` props/attributes/code comments)
- TODO/TBD/FIXME/Coming Soon/John Doe/Acme in copy: 0 hits (lib/tag-config.ts comments show `G-XXXXXXXXX` format examples — code comment, not copy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete zero-importer schema exports from structured-data.tsx (D-10)</name>
  <files>components/seo/structured-data.tsx</files>
  <read_first>
    - components/seo/structured-data.tsx (whole file — locate the four dead functions by `export function` name: OrganizationStructuredData ~37–74, ProfessionalServiceStructuredData ~276–329, FAQStructuredData ~331–372, FractionalCDOServiceStructuredData ~539–699; line numbers approximate)
    - app/layout.tsx lines 1–35 and ~115–125 (confirms only Person + Website are imported there)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-10
  </read_first>
  <action>
    Before deleting anything, re-run the census for each candidate: `grep -rlw NAME app lib components __tests__ | grep -v components/seo/structured-data.tsx` must print nothing for OrganizationStructuredData, ProfessionalServiceStructuredData, FAQStructuredData (word-match, so the already-deleted ProjectFAQStructuredData does not count), FractionalCDOServiceStructuredData. Delete each zero-importer `export function NAME() { ... }` block in full (including its leading comment line and the schema object inside it). Do NOT delete Breadcrumb, Person, Website, Article, CreativeWork, LocalBusiness. Keep the file's imports if still used (`useEffect`/`trackStructuredDataView` are used by LocalBusinessStructuredData; `createAbsoluteUrl` by several) — remove an import only if `npm run lint` flags it unused. Record, for Plan 04's matrix "Dead Schema Removed" section, each deleted export with the stale claims it carried (FAQStructuredData: "240K+ active users across 25K+ cities" FAQ answer; FractionalCDOServiceStructuredData: Chameleon Collective / Go Fractional service description; ProfessionalService/Organization: Wealthberry Labs org + service claims) and the note "recoverable from git history if Phase 10 selects one as the agreed service representation". Run `npm run lint`, `npx tsc --noEmit`, `npx jest __tests__/seo`.
  </action>
  <verify>
    <automated>for n in OrganizationStructuredData ProfessionalServiceStructuredData FAQStructuredData FractionalCDOServiceStructuredData; do grep -qw "$n" components/seo/structured-data.tsx && exit 1; done; for n in BreadcrumbStructuredData PersonStructuredData WebsiteStructuredData ArticleStructuredData CreativeWorkStructuredData LocalBusinessStructuredData; do grep -q "export function $n" components/seo/structured-data.tsx || exit 1; done; npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cw "OrganizationStructuredData\|ProfessionalServiceStructuredData\|FAQStructuredData\|FractionalCDOServiceStructuredData" components/seo/structured-data.tsx` returns 0
    - `grep -c "^export function" components/seo/structured-data.tsx` returns 6
    - `grep -c "25K+ cities\|FAQPage" components/seo/structured-data.tsx` returns 0
    - `grep -c "Chameleon" components/seo/structured-data.tsx` returns 2 (Person description + knowsAbout only)
    - `grep -rn "OrganizationStructuredData\|ProfessionalServiceStructuredData\|FractionalCDOServiceStructuredData" app lib components __tests__` prints nothing; `grep -rnw "FAQStructuredData" app lib components __tests__` prints nothing
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npx jest __tests__/seo` exits 0
  </acceptance_criteria>
  <done>Four dead exports removed, six live ones intact, build gates clean, deletions itemized for the matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Align $50M wording across career-wide surfaces (D-15, D-18)</name>
  <files>lib/metadata.ts, app/about/page.tsx</files>
  <read_first>
    - lib/metadata.ts line ~25 (createBaseMetadata description — has `$50M+ product value delivered`)
    - app/about/page.tsx lines 18–30 (openGraph.description — has `$50M value delivered`)
    - app/opengraph-image.tsx lines 135–152, app/about/opengraph-image.tsx lines 255–275, app/about/about-client.tsx lines 35–50, components/core/animated-number-basic.tsx lines 80–105, app/page.tsx lines 355–365 (verify-only: confirm each reads `$50M` + `Product Value`/`product value` with no `+`)
    - app/projects/nagarro/nagarro-client.tsx lines 570–580 (verify-only: `$50M+ in business impact` stays)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-15, D-18
    - .planning/phases/05-foundation-cleanup/05-CONTEXT.md `<deferred>` "$50M in product value" (the item this closes)
  </read_first>
  <action>
    Career-wide claim (Randy confirmed real, D-15): the visible surfaces (about card, animated counter, homepage terminal, both OG images) all say `$50M` with `product value` — per D-18 visible copy wins, so conform the two metadata strings: in `lib/metadata.ts` createBaseMetadata description change `$50M+ product value delivered.` to `$50M product value delivered.` (only the `+` is removed; the rest of the sentence unchanged); in `app/about/page.tsx` openGraph.description change `$50M value delivered.` to `$50M product value delivered.`. Make no edits to the five already-canonical surfaces or to `nagarro-client.tsx` (the Nagarro-specific `$50M+ in business impact` is a different, also-confirmed claim; after Plan 02 it is the only surface carrying it, so it is self-consistent). Run `grep -rn '\$50M' app lib components` and record the final per-surface list (file:line + exact wording) for Plan 04's matrix Part D. Run `npm run lint`, `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>grep -q '50M product value delivered' lib/metadata.ts && grep -q '50M product value delivered' app/about/page.tsx && [ "$(grep -rn '50M+' app lib components | grep -v 'app/projects/nagarro/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\$50M product value delivered' lib/metadata.ts` returns 1 and `grep -c '\$50M+' lib/metadata.ts` returns 0
    - `grep -c '\$50M product value delivered' app/about/page.tsx` returns 1 and `grep -c '\$50M value delivered' app/about/page.tsx` returns 0
    - `grep -rn '50M+' app lib components | grep -v 'app/projects/nagarro/'` prints nothing (the only surviving `$50M+` is the Nagarro-specific claim in `app/projects/nagarro/nagarro-client.tsx`; `app/projects/nagarro/page.tsx` is owned by Plan 02 running in the same wave and is checked by Plan 05's final grep)
    - `git diff --quiet HEAD -- app/opengraph-image.tsx app/about/opengraph-image.tsx app/about/about-client.tsx components/core/animated-number-basic.tsx app/page.tsx app/projects/nagarro/nagarro-client.tsx` exits 0 (already-canonical surfaces untouched by this task; about-client.tsx is edited only in Task 3)
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Career-wide `$50M product value` wording identical across metadata + visible surfaces; Nagarro `$50M+ in business impact` isolated to its page copy; per-surface list recorded for the matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Verify/fix Chameleon Collective URL (D-16) and run the bounded placeholder sweep (D-17)</name>
  <files>app/about/about-client.tsx, app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx</files>
  <read_first>
    - app/about/about-client.tsx lines 50–70 (experience[0]: `company: "Chameleon Collective"`, `companyUrl: "https://www.chameleon.co"`) and wherever `companyUrl` is rendered (find with `grep -n companyUrl app/about/about-client.tsx`) to confirm the anchor uses `target="_blank"` with `rel="noopener noreferrer"`
    - components/seo/structured-data.tsx (post-Task-1: confirm the two remaining "Chameleon" hits are name mentions with no URL)
    - app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx lines 120–145 (the "### Further Reading" section with three `https://example.com/...` links)
    - app/admin/email-test/page.tsx line ~205 (the allowed `placeholder="test@example.com"` attribute — verify-only)
    - .planning/phases/09-cross-surface-verification/09-CONTEXT.md D-16, D-17
    - .planning/phases/05-foundation-cleanup/05-CONTEXT.md `<deferred>` "Broader placeholder/example.com sweep" (the item this closes)
  </read_first>
  <action>
    D-16: run `curl -sIL --max-time 15 -A "Mozilla/5.0" https://www.chameleon.co | grep -i "^HTTP\|^location"` and record the chain. At planning time it was 308 → https://chameleon.co/ → 308 → https://chameleoncollective.com/ → 200, i.e. it resolves but through two redirects. Set `companyUrl` in `app/about/about-client.tsx` to the final 200 destination `https://chameleoncollective.com` (verify with `curl -sI https://chameleoncollective.com` → HTTP/2 200) so the /about link is a single hop; if the live result differs from the planning-time chain, use whatever URL returns 200 directly, and if nothing under either domain returns 200, remove `companyUrl` from that experience entry so no dead link renders — record the outcome either way for the matrix Part D. Confirm the anchor rendering `companyUrl` keeps `rel="noopener noreferrer"` (add it if missing). `structured-data.tsx` needs no URL fix (name mentions only) — record that. D-17: run the bounded sweep — `grep -rn "example\.com" app lib components`, `grep -rni "lorem\|ipsum" app lib components`, `grep -rni "placeholder" app lib components | grep -vi "placeholder=\|placeholder:\|placeholder-\|placeholder?\|placeholder ="` (copy hits only, not the HTML attribute/prop), `grep -rn "TODO\|TBD\|FIXME\|[Cc]oming [Ss]oon\|John Doe\|Jane Doe\|Acme" app lib components` — and log every hit for the matrix Part F. Fix the one real hit: in the blog MDX delete the entire `### Further Reading` heading and its three `example.com` bullet lines (and the now-redundant `---` separator directly above it if it leaves two consecutive separators), leaving "### Questions for Reflection" and "### Music for Inspiration" intact — these are placeholder links, not real references, so removal is the honest fix (do not invent replacement URLs). Leave `app/admin/email-test/page.tsx` `placeholder="test@example.com"` (HTML attribute — allowed by D-17) and log it as excluded. Run `npm run lint`, `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>grep -q 'companyUrl: "https://chameleoncollective.com"' app/about/about-client.tsx && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 -A 'Mozilla/5.0' https://chameleoncollective.com)" = "200" ] && [ "$(grep -rn 'example\.com' app lib components | grep -v 'placeholder="test@example.com"' | wc -l | tr -d ' ')" = "0" ] && ! grep -q "Further Reading" app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chameleon.co\"" app/about/about-client.tsx` returns 0 and `grep -c 'https://chameleoncollective.com' app/about/about-client.tsx` returns 1 (or, only if no URL under the domain returns 200 live, `grep -c companyUrl` for that entry is 0 and the SUMMARY says so)
    - `curl -s -o /dev/null -w '%{http_code}' https://chameleoncollective.com` prints 200
    - `grep -c "Chameleon" components/seo/structured-data.tsx` returns 2 and `grep -c "chameleon" components/seo/structured-data.tsx` returns 0 (no lowercase URL host)
    - `grep -rn "example\.com" app lib components` prints exactly one line and it is `app/admin/email-test/page.tsx` with `placeholder="test@example.com"`
    - `grep -c "Further Reading\|example.com" app/blog/exploring-the-intersection-of-design-ai-and-design-engineering/page.mdx` returns 0; `grep -c "Questions for Reflection\|Music for Inspiration" ...page.mdx` returns 2
    - `grep -rni "lorem\|ipsum" app lib components | wc -l` prints 0
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>Chameleon link resolves 200 in one hop (or is removed with the reason logged), placeholder sweep hits enumerated and the only real one fixed, gates clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /about page → external site (Chameleon Collective) | Outbound link to a third-party domain rendered from a static constant. |
| Blog MDX → external links | Static author-controlled links; placeholder `example.com` links removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Spoofing (link to wrong/expired domain) | about-client.tsx companyUrl | mitigate | curl-verified 200 destination pinned; anchor keeps `rel="noopener noreferrer"` with `target="_blank"` (verify in Task 3). |
| T-09-07 | Information Disclosure (stale claims in dead code shipping later) | structured-data.tsx dead exports | mitigate | Deleted in Task 1 (D-10); census re-run before deletion; recoverable from git if Phase 10 needs one. |
| T-09-08 | Repudiation (silent claim edits) | $50M wording, placeholder removals | mitigate | Every edit is recorded file:line + before/after for the Plan 04 matrix so Randy can veto (D-18). |
| T-09-SC | Tampering (supply chain) | npm installs | accept | No new dependencies. |
</threat_model>

<verification>
- tsc note: at planning time `npx tsc --noEmit` reported errors ONLY under `.next/types/**` (stale build artifacts referencing deleted routes `/ledgeriq`, `/archive`; tsconfig includes `.next/types/**/*.ts`). If you see those, run `rm -rf .next/types` and re-run; only errors outside `.next/` count as failures. Never treat `npm run build` as a gate (CLAUDE.md).
- `npx tsc --noEmit`, `npm run lint`, `npx jest __tests__/seo` clean
- Dead-export greps return 0; `$50M+` appears only in nagarro-client.tsx; `example.com` only in the allowed HTML attribute
- Findings list (dead exports, $50M surfaces, Chameleon chain, placeholder hits) written into the SUMMARY for Plan 04
</verification>

<success_criteria>
- D-10, D-15, D-16, D-17 each have a concrete outcome recorded in the SUMMARY (for the matrix)
- No new schema/claims introduced; only removals and wording alignment
</success_criteria>

<output>
Create `.planning/phases/09-cross-surface-verification/09-03-SUMMARY.md` when done. Include three lists Plan 04 will paste into the matrix: (1) deleted exports + the claims they carried, (2) final `$50M` per-surface table (file:line, wording, career-wide vs Nagarro), (3) Chameleon curl chain + resulting URL, and (4) placeholder sweep hits with disposition (fixed / excluded-attribute).
</output>
