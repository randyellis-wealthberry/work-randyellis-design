---
phase: 05-foundation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ledgeriq/page.tsx
  - app/ledgeriq/ledgeriq-client.tsx
  - next.config.js
  - components/case-study/case-study-hero.tsx
  - components/case-study/case-study-layout.tsx
  - components/case-study/case-study-section.tsx
  - components/case-study/image-gallery.tsx
  - components/case-study/metrics-card.tsx
  - components/case-study/video-player.tsx
  - app/projects/echo/echo-client-final.tsx
  - app/projects/[slug]/project-detail-client.tsx
  - app/about/about-client.tsx
  - __tests__/about-professional-experience.test.tsx
  - app/data.ts
  - app/archive/page.tsx
  - app/archive/archive-client.tsx
  - app/footer.tsx
  - components/ui/archive-thumbnail.tsx
  - lib/data/static-data.ts
  - lib/data/types.ts
  - lib/data/index.ts
  - app/page.tsx
  - app/projects/projects-client.tsx
  - __tests__/integration/route-coverage.test.tsx
autonomous: false
requirements: [FND-01, FND-02, FND-04, DEBT-01, DEBT-02]

must_haves:
  truths:
    - "D-01: /ledgeriq no longer resolves to a bespoke page; next.config.js issues a permanent (301) redirect to /projects/ledgeriq, and /projects/ledgeriq is the single canonical LedgerIQ URL"
    - "D-02: Nothing from the bespoke LedgerIQ page is salvaged into lib/data/projects.ts — its 5-step process narrative and its metrics are deleted, not migrated"
    - "D-09: The /archive route, its footer link, components/ui/archive-thumbnail.tsx, and the ARCHIVE_ITEMS / ARCHIVE_CATEGORIES / ArchiveItem exports are all gone — the fabricated-content defect class v1.0 purged is removed from this surface too"
    - "D-10: app/projects/[slug]/project-detail-client.tsx imports Project from @/lib/data/types and app/data.ts no longer exists, so Phase 6's decisions[] and roleNarrative fields will typecheck against the model actually used"
    - "D-11: All 6 files under components/case-study/ are deleted, including case-study-section.tsx"
    - "D-12 CARRY-FORWARD (recorded, not implemented here): Phase 6's section wrapper must preserve the deleted case-study-section.tsx accessibility contract — a section id, role=region, and aria-labelledby pointing at the id-heading element — while rendering its heading via ScrambleSectionTitle, not a plain centered h2"
    - "D-13: app/projects/echo/echo-client-final.tsx is deleted and app/projects/echo/echo-client.tsx (Phase 7's MIG-02 target) is untouched"
    - "D-14: The hero renders a short positioning line plus a 3-chip proof row above the existing CTAs, and AnimatedNumberBasic is unchanged — the two blocks divide labor instead of restating each other"
    - "D-15: The absolutely-positioned Live Product badge wrapper in projects-client.tsx carries pointer-events-none so clicks fall through to the Link; waffle-client.tsx is untouched"
    - "D-16 CONSTRAINT (applies here to DEBT-01): the chips are built from the allowlisted Badge primitive with no lucide icons, no Card wrapper, no new color/type/spacing tokens, and no placeholder imagery"
    - "D-17: This plan shares no files_modified with 05-01 and declares no dependency on it, so both run in parallel in wave 1"
    - "D-18: All four verification methods run — standard gate (lint then tsc then test), link-integrity grep sweep, deliberate test-suite audit for deleted surfaces, and a manual browser check"
    - "No surviving reference to /archive, /ledgeriq, app/data, components/case-study, or echo-client-final exists anywhere in app/, components/, lib/, or __tests__/"
  artifacts:
    - path: "next.config.js"
      provides: "Permanent /ledgeriq to /projects/ledgeriq redirect"
      contains: "async redirects()"
    - path: "app/projects/[slug]/project-detail-client.tsx"
      provides: "Project type resolved from the live data model (FND-01 blocker fix)"
      contains: "@/lib/data/types"
    - path: "app/page.tsx"
      provides: "Hero proof-chip row (DEBT-01)"
      contains: "@/components/ui/badge"
    - path: "app/projects/projects-client.tsx"
      provides: "Click-through Live Product badge (DEBT-02)"
      contains: "pointer-events-none absolute top-3 right-3 z-10"
  key_links:
    - from: "app/projects/[slug]/project-detail-client.tsx"
      to: "lib/data/types.ts"
      via: "type-only import of Project"
      pattern: "@/lib/data/types"
    - from: "app/about/about-client.tsx"
      to: "lib/data/index.ts"
      via: "getEmail barrel import"
      pattern: "getEmail.*@/lib/data"
    - from: "next.config.js"
      to: "/projects/ledgeriq"
      via: "permanent redirect entry"
      pattern: "destination.*projects/ledgeriq"
---

<objective>
Delete every orphaned, dead, and fabricated surface in the rewrite path; consolidate
the data model onto lib/data so Phase 6's narrative fields will typecheck; and close
the two v1.0 tech-debt items (POS-02 proof chips, WAF-02 badge dead zone).

Purpose: FND-01 is a hard prerequisite for Phase 6 — decisions[] and roleNarrative will
not typecheck while project-detail-client.tsx imports Project from the stale
app/data.ts. FND-02 and FND-04 stop Phases 7 and 8 from editing files no user can
reach. DEBT-01 and DEBT-02 clear unrelated debt while the codebase is already open.

Output: 12 files deleted, 9 files edited, one redirects() block added.

Non-goal: this plan writes zero narrative components (Phase 6), migrates zero bespoke
pages (Phase 7), and rewrites zero case-study copy (Phase 8). It does not touch
lib/data/projects.ts, components/core/animated-number-basic.tsx,
app/projects/echo/echo-client.tsx, or app/projects/waffle/waffle-client.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-cleanup/05-CONTEXT.md
@.planning/phases/05-foundation-cleanup/05-PATTERNS.md
@CLAUDE.md

<interfaces>
The surviving data surface. Both Project and getEmail are reachable from either the
barrel or the direct module; D-10 pins which one each file uses. Use these directly —
no codebase exploration needed.

lib/data/types.ts exports:
  type Project = { id, name, subtitle?, slug, description, longDescription, category,
    categories?, tags, link, githubLink?, video, thumbnail?, images, timeline, status,
    technologies, featured, isLiveProduct?, archived?, metrics?, challenges?,
    solutions?, learnings?, teamSize?, role?, overview?, constraints?, processStory? }
  type WorkExperience, BlogPost, SocialLink
  type ArchiveItem                     -- DELETED by this plan (lib/data/types.ts:81-96)
  const getEmail, EMAIL_ENCODED, PROJECT_CATEGORIES

lib/data/index.ts (barrel) re-exports:
  star-export from ./types
  star-export from ./static-data
  named re-export of WORK_EXPERIENCE, BLOG_POSTS, SOCIAL_LINKS, ARCHIVE_ITEMS from
    ./static-data  -- ARCHIVE_ITEMS is removed at index.ts:17 by this plan
  named re-export of getEmail, EMAIL_ENCODED, PROJECT_CATEGORIES from ./types

lib/data/projects.ts — 8 slugs (growit, ohplays, ledgeriq, addvanced, echo, nagarro,
rambis-ui, waffle) vs app/data.ts's 4. Strict superset; WORK_EXPERIENCE is identical
between the two files. No data is lost when app/data.ts is deleted. DO NOT EDIT this
file in this plan.

components/ui/badge.tsx — cva variants: default, secondary, destructive, outline.
Import path used everywhere else in the app: @/components/ui/badge
</interfaces>
</context>

<tdd_note>
workflow.tdd_mode is enabled, but no task in this plan is TDD-eligible and none carries
tdd="true". Task 1 is deletions plus a Next.js config function (not unit-testable in
jest). Task 2 is file deletions plus mechanical import-path swaps (glue code wiring
already-tested modules). Task 3 is JSX and styling — layout and one CSS class. Per the
TDD heuristic, none of these admit an expect(fn(input)).toBe(output) written before the
code. The test work in this plan is Task 4's deliberate maintenance of
__tests__/integration/route-coverage.test.tsx, which is test-suite correction for
deleted surfaces, not a RED-GREEN cycle. tsconfig.json excludes __tests__/, so test
files are not typechecked by npx tsc --noEmit; npm test is their only gate.
</tdd_note>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead surfaces and add the /ledgeriq redirect (FND-02, FND-04)</name>
  <files>app/ledgeriq/page.tsx, app/ledgeriq/ledgeriq-client.tsx, components/case-study/case-study-hero.tsx, components/case-study/case-study-layout.tsx, components/case-study/case-study-section.tsx, components/case-study/image-gallery.tsx, components/case-study/metrics-card.tsx, components/case-study/video-player.tsx, app/projects/echo/echo-client-final.tsx, next.config.js</files>
  <read_first>
    - next.config.js — read the tail of the nextConfig object; async headers() begins
      around line 239 and the object closes around line 274. The redirects() block goes
      in as a sibling of headers() inside the same object.
    - .planning/phases/05-foundation-cleanup/05-PATTERNS.md, the next.config.js section
      — carries the exact surrounding structure and the confirmed finding that neither
      middleware.ts nor vercel.json defines any redirect or rewrite block, so CLAUDE.md's
      three-place header-sync warning does NOT apply to redirects.
    - app/ledgeriq/page.tsx — skim only, to confirm it is the orphan route and not a
      re-export of something live.
    - components/case-study/case-study-section.tsx — read before deleting, to capture the
      D-12 accessibility contract for the SUMMARY handoff note.
    - .planning/phases/05-foundation-cleanup/05-CONTEXT.md — D-01, D-02, D-11, D-12, D-13
  </read_first>
  <action>
    Delete these 9 files. All have zero importers, confirmed by repo-wide grep in the
    Deletion-Safety Grep Results section of 05-PATTERNS.md.

    - app/ledgeriq/page.tsx and app/ledgeriq/ledgeriq-client.tsx (remove the whole
      app/ledgeriq/ directory)
    - All 6 files in components/case-study/: case-study-hero.tsx, case-study-layout.tsx,
      case-study-section.tsx, image-gallery.tsx, metrics-card.tsx, video-player.tsx
      (remove the whole directory)
    - app/projects/echo/echo-client-final.tsx

    D-02: salvage nothing from ledgeriq-client.tsx. Its 5-step process narrative
    (Discovery and Research, Information Architecture, Design System, High-Fidelity
    Design, Development and Testing) and its four metrics are unverified, and every
    image it references under /ledgeriq/ 404s because public/ledgeriq/ is empty. Do not
    copy any of it into lib/data/projects.ts.

    D-13: delete echo-client-final.tsx ONLY. app/projects/echo/echo-client.tsx (no
    -final suffix) is the live file and is Phase 7's MIG-02 target — leave it and
    app/projects/echo/page.tsx untouched.

    D-12 CARRY-FORWARD — record this in the SUMMARY BEFORE deleting
    case-study-section.tsx, because that file is the only place it is written down. The
    salvageable thing there was its accessibility contract, not its markup: a section
    id, role="region", and aria-labelledby pointing at a heading whose id is the section
    id suffixed with -heading. Phase 6's section wrapper must keep that contract (it is
    what CaseStudyTOC anchors to) while rendering its heading via ScrambleSectionTitle,
    which is used 39 times in project-detail-client.tsx and IS this platform's
    section-heading language. Resurrecting the old plain centered h2 would fork the
    design language. Do NOT implement it here — this is a handoff note, not a Phase 5
    task.

    Then add a redirects() block to next.config.js. Insert async redirects() as a
    sibling of the existing async headers() method, inside the nextConfig object —
    immediately after headers()'s closing brace-comma and before the object's final
    closing brace-semicolon. It returns a single-element array whose one entry has
    source "/ledgeriq", destination "/projects/ledgeriq", and permanent true.

    Security constraint (threat T-05-02-01): the destination is a hardcoded internal
    path literal. Do NOT use a :path* wildcard in source, do NOT add has or missing
    conditions, do NOT derive the destination from a request value or env var, and do
    NOT use an absolute URL with a host. Any of those turns a fixed internal redirect
    into an open-redirect shape.

    Do not touch middleware.ts or vercel.json — verified they contain no redirect or
    rewrite rules.
  </action>
  <verify>
    <automated>test ! -d app/ledgeriq && test ! -d components/case-study && test ! -f app/projects/echo/echo-client-final.tsx && grep -q "async redirects()" next.config.js && node -e "require('./next.config.js')"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d app/ledgeriq` exits 0
    - `test ! -d components/case-study` exits 0
    - `test ! -f app/projects/echo/echo-client-final.tsx` exits 0
    - `test -f app/projects/echo/echo-client.tsx` exits 0 (the live file survives)
    - next.config.js contains the literal strings `async redirects()`,
      `source: "/ledgeriq"`, `destination: "/projects/ledgeriq"`, and `permanent: true`
    - The redirect entry contains no `:path*` wildcard and no `http://` or `https://`
      anywhere in its source or destination — verify by reading the block back after the edit
    - `git diff --stat middleware.ts vercel.json lib/data/projects.ts app/projects/echo/echo-client.tsx` produces no output
    - `node -e "require('./next.config.js')"` exits 0 (the config still parses)
    - The SUMMARY contains the D-12 accessibility-contract carry-forward note
  </acceptance_criteria>
  <done>The orphan /ledgeriq route, the abandoned components/case-study/ set, and echo-client-final.tsx are gone; /ledgeriq 301s to /projects/ledgeriq; D-12 is recorded for Phase 6.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate onto lib/data and delete the /archive route (FND-01, D-09, D-10)</name>
  <files>app/projects/[slug]/project-detail-client.tsx, app/about/about-client.tsx, __tests__/about-professional-experience.test.tsx, app/footer.tsx, app/archive/page.tsx, app/archive/archive-client.tsx, components/ui/archive-thumbnail.tsx, lib/data/types.ts, lib/data/static-data.ts, lib/data/index.ts, app/data.ts</files>
  <read_first>
    - app/projects/[slug]/project-detail-client.tsx lines 35-50 — the import block
      containing line 42, `import type { Project } from "../../data";` (THE FND-01 BLOCKER)
    - app/about/about-client.tsx lines 15-30 — the import block containing line 22,
      `import { getEmail } from "../data";`
    - __tests__/about-professional-experience.test.tsx lines 40-55 — the
      `jest.mock("@/app/data", ...)` block at line 46
    - app/footer.tsx lines 14-24 — the /archive anchor; it and its sibling
      FooterThemeToggle live inside a `flex items-center space-x-4 text-xs text-zinc-400` div
    - lib/data/types.ts lines 81-96 — the ArchiveItem type to delete
    - lib/data/static-data.ts line 1 (the ArchiveItem import), line 85
      (ARCHIVE_CATEGORIES), line 93 (ARCHIVE_ITEMS)
    - lib/data/index.ts lines 1-19 — the barrel; ARCHIVE_ITEMS sits at line 17 of the
      explicit re-export list
    - .planning/phases/05-foundation-cleanup/05-PATTERNS.md — the lib/data import-surface
      section and the ARCHIVE removal reference table
    - .planning/phases/05-foundation-cleanup/05-CONTEXT.md — D-09, D-10
  </read_first>
  <action>
    Order matters: repoint importers FIRST, then delete app/data.ts. Deleting first
    produces a broken intermediate state.

    Step A — repoint the three surviving importers. Change the module specifier only; do
    not change what is imported and do not reorder the import block.

    1. app/projects/[slug]/project-detail-client.tsx line 42 — the specifier "../../data"
       becomes "@/lib/data/types", keeping it a type-only `import type { Project }`.
       Direct-module style, matching components/ui/enhanced-metrics-grid.tsx line 4.
    2. app/about/about-client.tsx line 22 — the specifier "../data" becomes "@/lib/data",
       keeping `import { getEmail }`. Barrel style, matching app/page.tsx lines 27-32.
       Leave the unrelated testimonials import from @/lib/data/testimonials exactly as is.
    3. __tests__/about-professional-experience.test.tsx line 46 — the mocked module
       "@/app/data" becomes "@/lib/data". Keep the factory body
       (getEmail returning "test@example.com") unchanged. It must mock the module the
       component now actually imports from, or the mock silently stops applying and the
       test passes for the wrong reason.

    Step B — delete the /archive route (D-09). Remove app/archive/page.tsx and
    app/archive/archive-client.tsx (the whole app/archive/ directory), and remove the
    /archive anchor element from app/footer.tsx. Keep the surrounding
    `flex items-center space-x-4 text-xs text-zinc-400` div and FooterThemeToggle —
    only the anchor goes.

    Also delete components/ui/archive-thumbnail.tsx. Its only importer is
    app/archive/archive-client.tsx (lines 17 and 119), so it becomes zero-importer dead
    code the moment the route is deleted — the same class as components/case-study/.
    This is the addition 05-PATTERNS.md found that CONTEXT.md's file list missed.

    Step C — drop the archive data exports. D-09 left this to Claude's discretion; the
    decision is DELETE, not leave-as-unused-exports.
    - lib/data/types.ts — delete the `export type ArchiveItem` block at lines 81-96
    - lib/data/static-data.ts line 1 — remove ArchiveItem from the
      `import { WorkExperience, BlogPost, SocialLink, ArchiveItem } from "./types";`
      list, leaving the other three
    - lib/data/static-data.ts — delete `export const ARCHIVE_CATEGORIES` (line 85) and
      `export const ARCHIVE_ITEMS` (line 93) together with their array literals
    - lib/data/index.ts line 17 — remove ARCHIVE_ITEMS from the explicit re-export list,
      leaving WORK_EXPERIENCE, BLOG_POSTS, SOCIAL_LINKS
    Leave WORK_EXPERIENCE, BLOG_POSTS, and SOCIAL_LINKS untouched.

    Step D — delete app/data.ts (775 lines). Only after Steps A through C. Do not migrate
    anything out of it first: lib/data/projects.ts is a strict superset (8 slugs vs 4)
    and WORK_EXPERIENCE is identical between the two files.

    Do NOT touch components/blog/blog-archive-accordion.tsx, lib/utils/blog-data.ts,
    app/blog/**, or the "View archive" link at app/page.tsx line 783 (it points at
    /blog). Those are the blog's own unrelated archive naming and are confirmed false
    positives in 05-PATTERNS.md.
  </action>
  <verify>
    <automated>test ! -f app/data.ts && test ! -d app/archive && test ! -f components/ui/archive-thumbnail.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f app/data.ts` exits 0
    - `test ! -d app/archive` exits 0
    - `test ! -f components/ui/archive-thumbnail.tsx` exits 0
    - `app/projects/[slug]/project-detail-client.tsx` contains the string `from "@/lib/data/types"`
      and does NOT contain `from "../../data"`
    - `app/about/about-client.tsx` contains `import { getEmail } from "@/lib/data"` and
      does NOT contain `from "../data"`; its `@/lib/data/testimonials` import is unchanged
    - `__tests__/about-professional-experience.test.tsx` contains `jest.mock("@/lib/data"`
      and does NOT contain `"@/app/data"`
    - `grep -rn "ARCHIVE_ITEMS\|ARCHIVE_CATEGORIES\|ArchiveItem" app/ components/ lib/ __tests__/`
      returns no matches
    - `grep -n "/archive" app/footer.tsx` returns no matches, while
      `grep -c "FooterThemeToggle" app/footer.tsx` is unchanged from before the edit
    - `npx tsc --noEmit` exits 0
    - `git diff --stat lib/data/projects.ts app/blog lib/utils/blog-data.ts components/blog`
      produces no output
  </acceptance_criteria>
  <done>Project resolves from lib/data/types, app/data.ts and the fabricated /archive route are gone, and the whole tree typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Hero proof chips and Live Product badge click-through (DEBT-01, DEBT-02)</name>
  <files>app/page.tsx, app/projects/projects-client.tsx</files>
  <read_first>
    - app/page.tsx lines 1-45 (the import block; Badge is NOT yet imported) and lines
      534-575 (the hero: ScrambleSectionTitle, the h2-scale p, the TextGradientScroll
      subhead, the CTA div, then the AnimatedNumberBasic section)
    - app/projects/[slug]/project-detail-client.tsx lines 226-232 — the canonical chip
      row: Badge inside `flex flex-wrap gap-1.5`, variant outline, text-xs
    - app/about/about-client.tsx lines 630-643 — the same shape at gap-2 with
      variant secondary and text-sm
    - components/ui/badge.tsx — the cva variant list (default, secondary, destructive, outline)
    - app/projects/projects-client.tsx lines 232-246 — the Card, the Link wrapping only
      ProjectThumbnail, and the absolutely-positioned badge wrapper div at line 239
    - app/projects/waffle/waffle-client.tsx lines 113-126 — the CONTRAST case; this badge
      is in normal flow with no overlay and must NOT be changed
    - .planning/phases/05-foundation-cleanup/05-CONTEXT.md — D-14, D-15, D-16
    - .planning/phases/05-foundation-cleanup/05-PATTERNS.md — the app/page.tsx hero-chip
      section and the projects-client badge-wrapper section
  </read_first>
  <action>
    DEBT-01 (D-14). Root cause: the current 43-word TextGradientScroll subhead restates
    two of the four stats rendered about 200px below it in AnimatedNumberBasic (2.5M
    Users Impacted, 4 Design Awards, $50M in product value, 800 Designers Mentored). The
    fix is dividing labor between the two blocks, not adding a third block.

    1. Shorten the TextGradientScroll `text` prop to exactly:
       `I turn startups into design-led organizations — and write the code to prove it.`
       Everything after "prove it." is deleted — the "20 years / 8+ years / 2.5M+ /
       800+ / React, Next.js, TypeScript" tail moves into the chips (below) and the
       counter block. Keep the component's other props (type="letter",
       textOpacity="soft", and its existing className) exactly as they are.

    2. Add `import { Badge } from "@/components/ui/badge";` to the app/page.tsx import
       block, matching the path used in project-detail-client.tsx and about-client.tsx.

    3. Insert a chip row BETWEEN the TextGradientScroll element and the existing
       `mt-6 flex flex-wrap gap-3` CTA div. Container class string:
       `mt-6 flex flex-wrap gap-2`. Inside it, exactly three Badge elements, all with
       `variant="secondary"` and `className="text-sm"` — do not mix variants across the
       three chips (no existing row in the codebase mixes outline and secondary). Chip
       labels, in order:
         - `20 years in design`
         - `8+ yrs leading teams`
         - `Ships React / Next.js / TypeScript`

    4. Leave the CTA div ("Book a 30-min call" and "View work") and the
       AnimatedNumberBasic section entirely unchanged. Do not edit
       components/core/animated-number-basic.tsx.

    D-16 bans that apply here: no lucide-react icon inside a chip (that is the banned
    icon+heading+body card pattern), no Card wrapper around a chip (Card is for richer
    allowlisted treatments like EnhancedMetricsGrid), no new color or type or spacing
    tokens, and specifically do NOT reuse the amber accent
    (`bg-amber-600 text-sm font-bold text-zinc-950 dark:bg-amber-500`) — that class
    string is reserved for the "Live Product" status badge, and reusing it on
    informational chips blurs the status-vs-fact semantic.

    DEBT-02 (D-15). In app/projects/projects-client.tsx, the badge wrapper div at line
    239 currently reads `absolute top-3 right-3 z-10`. Change that class string to
    `pointer-events-none absolute top-3 right-3 z-10`. That is the entire fix: one
    class. Do NOT touch the Badge element itself, the Link, the Card, or any other JSX.
    Do NOT wrap the card in a Link. Do NOT touch
    app/projects/waffle/waffle-client.tsx — its badge is in normal flow with no overlay
    and needs no change.
  </action>
  <verify>
    <automated>grep -q "pointer-events-none absolute top-3 right-3 z-10" app/projects/projects-client.tsx && grep -q "@/components/ui/badge" app/page.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` contains the string `import { Badge } from "@/components/ui/badge"`
    - `app/page.tsx` contains a container with the class string `mt-6 flex flex-wrap gap-2`
      and, inside it, the three literal chip labels `20 years in design`,
      `8+ yrs leading teams`, and `Ships React / Next.js / TypeScript`
    - All three chips use `variant="secondary"` and `className="text-sm"`; the strings
      `variant="outline"` and `variant="default"` do not appear in the new chip row
    - `grep -c "bg-amber" app/page.tsx` returns 0 (the amber status accent is not reused)
    - `grep -c "lucide-react" app/page.tsx` is unchanged from before the edit, and no
      icon element appears inside the chip row
    - The TextGradientScroll `text` prop in `app/page.tsx` no longer contains any of
      `2.5M`, `800`, `20 years in design and`, or `now focused on AI`
    - `git diff --stat components/core/animated-number-basic.tsx app/projects/waffle/waffle-client.tsx`
      produces no output
    - `app/projects/projects-client.tsx` contains the exact string
      `pointer-events-none absolute top-3 right-3 z-10`
    - `git diff --numstat app/projects/projects-client.tsx` shows 1 line added and 1 line
      removed (a single-class change, not a JSX restructure)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>The hero divides labor between a short positioning line, three proof chips, and the untouched counter block; the Live Product badge no longer blocks clicks to the project link.</done>
</task>

<task type="auto">
  <name>Task 4: D-18 verification — link-integrity sweep, test-suite audit, standard gate</name>
  <files>__tests__/integration/route-coverage.test.tsx</files>
  <read_first>
    - __tests__/integration/route-coverage.test.tsx — stale entries at line 15
      (`{ path: "/archive", name: "Archive" }`), line 17
      (`{ path: "/ledgeriq", name: "LedgerIQ" }`) in EXPECTED_ROUTES, and around lines
      129 and 132 (`"/archive/page"` and `"/ledgeriq/page"`) in expectedManifestRoutes
    - CLAUDE.md, the Verifying Changes section — the lint then tsc then test order, and
      the warning that `npm run build` is NOT a validation gate
    - .planning/PROJECT.md, the Key Decisions table — the v1.0 lesson to grep ALL
      surfaces including OG generators, app/sitemap.ts, and app/robots.ts
    - .planning/phases/05-foundation-cleanup/05-PATTERNS.md, the Deletion-Safety Grep
      Results section — the confirmed false-positive list so the sweep is not
      over-corrected
  </read_first>
  <action>
    Method 3 first (test-suite audit for deleted surfaces) — fix these deliberately
    rather than discovering them red.

    In __tests__/integration/route-coverage.test.tsx remove all four stale entries: the
    `/archive` and `/ledgeriq` objects in EXPECTED_ROUTES (lines 15 and 17) and the
    `"/archive/page"` and `"/ledgeriq/page"` strings in expectedManifestRoutes (around
    lines 129 and 132). Do NOT remove `/projects/ledgeriq` or any `/projects/*` entry —
    that page survives and is now the single canonical LedgerIQ URL. If removing entries
    leaves a hardcoded expected-count assertion stale, update the count to match.

    Then grep the whole test tree for any other assertion against a deleted surface:
    search __tests__/ for `/archive`, `app/data`, `components/case-study`, and
    `echo-client-final`. Excluded false positives per 05-PATTERNS.md — do not touch
    these: __tests__/app/blog-page.test.tsx and
    __tests__/components/blog-archive-accordion.test.tsx (the blog's own unrelated
    "archive" naming).

    Method 2 (link-integrity sweep). Run a grep across app/, components/, lib/, and
    __tests__/ for each of these five tokens, filtering comment lines so header prose
    cannot self-invalidate the gate: `/archive`, `/ledgeriq`, `app/data`,
    `components/case-study`, `echo-client-final`. Explicitly include app/sitemap.ts,
    app/robots.ts, app/manifest.ts, and every opengraph-image.tsx generator in the sweep
    scope — PROJECT.md's v1.0 lesson is that OG generators and metadata surfaces are the
    ones that get missed. Expected legitimate survivors, which must NOT be removed:
      - `/projects/ledgeriq` references in lib/data/projects.ts,
        components/ui/search-interface.tsx, and the project data tests
      - `source: "/ledgeriq"` / `destination: "/projects/ledgeriq"` in next.config.js
        (that is the redirect added in Task 1)
      - the blog's own archive naming in app/blog/**, lib/utils/blog-data.ts,
        components/blog/blog-archive-accordion.tsx, and the "View archive" link at
        app/page.tsx line 783 which points at /blog
    Any hit outside that survivor list is a dead reference and must be removed.

    Method 1 (standard gate). Run, in this exact order, and do not substitute
    `npm run build` for any of them (next.config.js sets ignoreDuringBuilds and
    ignoreBuildErrors, so the build passes with lint and type errors):
      npm run lint
      npx tsc --noEmit
      npm test

    Known baseline, NOT a regression — do not treat these as failures and do not "fix"
    them: __tests__/performance/animation-load-testing.test.tsx is flaky because it
    measures FPS inside jsdom and fails on slower runs, and some suites are
    intentionally describe.skip. Record any such result in the SUMMARY as a known
    baseline with the suite name, rather than reporting the phase red.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\"/archive\"\|\"/ledgeriq\"\|/archive/page\|/ledgeriq/page" __tests__/integration/route-coverage.test.tsx`
      returns no matches
    - `grep -rn "components/case-study\|echo-client-final" app/ components/ lib/ __tests__/`
      returns no matches
    - `grep -rn "from \"@/app/data\"\|from \"../data\"\|from \"../../data\"" app/ components/ lib/ __tests__/`
      returns no matches
    - `grep -rn "href=\"/archive\"\|href='/archive'" app/ components/` returns no matches
    - `grep -rn "\"/ledgeriq\"" app/ components/ lib/` returns matches ONLY inside
      next.config.js (the Task 1 redirect source)
    - `grep -rn "archive\|ledgeriq" app/sitemap.ts app/robots.ts app/manifest.ts` returns
      no matches
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm test` passes, with any failure limited to the known-flaky
      __tests__/performance/animation-load-testing.test.tsx suite, named explicitly in
      the SUMMARY as a pre-existing baseline
    - `npm run build` was NOT used as a verification step
  </acceptance_criteria>
  <done>No dead reference to any deleted surface survives anywhere in the tree, the test suite is deliberately corrected rather than discovered red, and lint, tsc, and test all pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual browser check (D-18 method 4)</name>
  <files>none (read-only runtime verification against a running dev server)</files>
  <read_first>
    - .planning/phases/05-foundation-cleanup/05-CONTEXT.md — D-14, D-15, D-18 method 4
    - CLAUDE.md, the Development Commands section — use `npm run dev:auto` or
      `npm run dev:direct`; plain `npm run dev` waits for interactive confirmation and
      will hang in a non-interactive shell
  </read_first>
  <what-built>
    The orphan /ledgeriq route, the /archive route, components/case-study/, and
    echo-client-final.tsx are deleted. app/data.ts is gone and its three importers now
    resolve from @/lib/data. next.config.js issues a 301 from /ledgeriq to
    /projects/ledgeriq. The hero renders three proof chips under a shortened positioning
    line. The Live Product grid badge no longer intercepts clicks.

    Automated gates already passed in Task 4: npm run lint, npx tsc --noEmit, npm test,
    plus the link-integrity grep sweep. This checkpoint covers only the runtime behavior
    those gates structurally cannot see.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev:auto` (NOT `npm run dev` — it prompts and
    hangs). Then:

    1. Visit http://localhost:3000/ — confirm the hero reads: "Head of Product &
       Fractional CDO", then "Design leader who ships AI products.", then the single
       short line "I turn startups into design-led organizations — and write the code to
       prove it.", then a row of three chips reading "20 years in design",
       "8+ yrs leading teams", "Ships React / Next.js / TypeScript", then the
       "Book a 30-min call" and "View work" buttons. The chips must render as visually
       distinct pills, not as a run-on sentence.
    2. Scroll down on / — the four-stat counter block (2.5M Users Impacted, 4 Design
       Awards, $50M in product value, 800 Designers Mentored) still animates and is
       unchanged.
    3. Scroll to the footer on any page — there is no "Archive" link. The theme toggle
       is still there.
    4. Visit http://localhost:3000/projects — find a card showing the amber "Live
       Product" badge. Click DIRECTLY ON THE BADGE ITSELF. It must navigate to that
       project's page. Before this fix it did nothing.
    5. Visit http://localhost:3000/ledgeriq — the browser must land on
       /projects/ledgeriq. Confirm the URL bar shows /projects/ledgeriq and the page
       renders the data-driven LedgerIQ case study (not the old bespoke page with
       broken images).
    6. Visit http://localhost:3000/archive — must return a 404.
    7. Visit http://localhost:3000/projects/echo — still renders normally (echo-client.tsx
       is the live file and was not touched).
  </how-to-verify>
  <action>
    Do not write any code in this task. Start the dev server with `npm run dev:auto`
    (NOT `npm run dev` — it waits for interactive confirmation and hangs in a
    non-interactive shell; `npm run clean-port` frees port 3000 if needed). Present the
    7-step checklist below to the operator, pause for their verdict, and record the
    result in the SUMMARY. If the operator reports a failure, stop and report which
    numbered step failed rather than attempting a fix inside this task — a runtime
    failure here means Task 1, 2, or 3 was implemented incorrectly and must be
    revisited at its source.
  </action>
  <verify>
    <human-check>All 7 steps above behave as described.</human-check>
  </verify>
  <acceptance_criteria>
    - Three chips render as visually separate pills on / (behavior assertion, step 1)
    - The AnimatedNumberBasic counter block still animates all four stats (step 2)
    - No "Archive" link is present in the footer and the theme toggle still renders (step 3)
    - Clicking the amber "Live Product" badge on /projects navigates to that project's
      detail page (step 4 — this is the DEBT-02 behavior fix)
    - Navigating to /ledgeriq results in the URL /projects/ledgeriq (step 5)
    - /archive returns 404 (step 6)
    - /projects/echo renders without error (step 7)
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which numbered step failed and what you saw.</resume-signal>
  <done>All 7 runtime behaviors confirmed by a human in a browser.</done>
</task>

</tasks>

<threat_model>
**Reviewed against ASVS L1** — applicable control families: V5 (input validation — redirect destination is a hardcoded internal path; no wildcard source, no absolute-URL destination), V4 (access control — removal of crawlable routes). Block on: high severity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet to Next.js routing layer | The new `redirects()` entry in next.config.js is evaluated on every inbound request to /ledgeriq before any application code runs. |
| external inbound links to deleted routes | /ledgeriq and /archive were crawlable (app/robots.ts allows /). Deleting them changes what an external visitor receives. |

This phase adds no new user input handling, no new API route, no new data persistence,
and no new dependency. The realistic surface is the redirect and the correctness of the
removals.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | `async redirects()` in next.config.js | mitigate | The destination is the hardcoded internal literal `/projects/ledgeriq`. Task 1's action explicitly forbids a `:path*` wildcard in `source`, `has`/`missing` conditions, request-derived or env-derived destinations, and absolute URLs with a host — each of which would turn a fixed internal redirect into an open-redirect primitive. Task 1's acceptance criteria assert the absence of `:path*`, `http://`, and `https://` in the block. |
| T-05-02-02 | Denial of Service (availability, not attack) | deleted routes /ledgeriq and /archive | mitigate | /ledgeriq gets a permanent 301 rather than a 404, preserving external inbound links (D-01). /archive is deleted outright with no redirect because 100% of its content is fabricated placeholder data (D-09) — a redirect would preserve a link to nothing. Task 4's link-integrity sweep, scoped to include app/sitemap.ts, app/robots.ts, app/manifest.ts, and every opengraph-image.tsx generator, guarantees no internal surface still links to either route. |
| T-05-02-03 | Information Disclosure | fabricated /archive content and unverified LedgerIQ metrics on the deleted bespoke page | mitigate | Deletion removes four fabricated articles with example.com links and 404ing thumbnails, and removes a metric set that contradicts the surviving page (D-03). This is a net reduction in misleading public claims. D-02 forbids salvaging those metrics forward. |
| T-05-02-04 | Tampering | `pointer-events-none` on the Live Product badge wrapper | accept | The badge is informational, not interactive. Making it click-transparent routes clicks to the already-present `<Link>` to that project's own detail page — a same-origin internal navigation the user was already trying to make. No privilege or destination changes. |
| T-05-02-05 | Repudiation | deletion of 12 files across the tree | mitigate | All deletions land as git commits with the phase and plan scope, so every removal is attributable and revertible. 05-PATTERNS.md's zero-importer greps are the recorded justification for each. |
| T-05-02-SC | Tampering | npm / pip / cargo installs | accept | This plan runs zero package installs and adds zero dependencies. `Badge` is an existing local component at components/ui/badge.tsx. No Package Legitimacy Audit table is required because no package is introduced. |

Spoofing and Elevation of Privilege have no applicable component: this phase touches no
authentication, authorization, session, or trust-elevating code path.
</threat_model>

<verification>
All four D-18 methods are represented in this plan:

1. **Standard gate** (Task 4) — `npm run lint`, then `npx tsc --noEmit`, then
   `npm test`, in that order. `npm run build` is explicitly NOT a gate because
   next.config.js sets `eslint.ignoreDuringBuilds` and `typescript.ignoreBuildErrors`.
   Note tsconfig.json excludes `__tests__/`, so test files are gated only by `npm test`.
2. **Link-integrity sweep** (Task 4) — grep app/, components/, lib/, __tests__/ for
   `/archive`, `/ledgeriq`, `app/data`, `components/case-study`, `echo-client-final`,
   with app/sitemap.ts, app/robots.ts, app/manifest.ts, and the opengraph-image.tsx
   generators explicitly in scope per PROJECT.md's v1.0 lesson.
3. **Test-suite audit for deleted surfaces** (Task 4) — the four stale entries in
   `__tests__/integration/route-coverage.test.tsx` are removed deliberately, plus a
   sweep of the rest of `__tests__/`.
4. **Manual browser check** (Task 5, `autonomous: false`) — the 7-step runtime check on
   /, /projects, /projects/ledgeriq, /ledgeriq, /archive, /projects/echo.

Known baseline, NOT a regression: `__tests__/performance/animation-load-testing.test.tsx`
is flaky (it measures FPS inside jsdom and fails on slower runs), and some suites are
intentionally `describe.skip`. Neither counts as a Phase 5 failure.
</verification>

<success_criteria>
Maps to ROADMAP Phase 5 success criteria 1, 2, 4, and 5:

- `app/projects/[slug]/project-detail-client.tsx` imports `Project` from
  `@/lib/data/types`, `app/data.ts` does not exist, and `npx tsc --noEmit` exits 0 (SC 1)
- Only `/projects/ledgeriq` exists as a LedgerIQ URL; `/ledgeriq` 301s to it and no page
  links to the old route (SC 2)
- `echo-client-final.tsx` and all 6 `components/case-study/*` files are deleted; every
  remaining file in the rewrite path is live and imported (SC 4)
- The hero subhead renders as three chips plus a short positioning line, and clicking the
  Waffle-style "Live Product" badge on /projects navigates instead of dead-ending (SC 5)
- `/archive` and its fabricated content are gone, including
  `components/ui/archive-thumbnail.tsx` and the `ARCHIVE_*` data exports
- All four D-18 verification methods pass
- `git diff` shows no change to `lib/data/projects.ts`,
  `components/core/animated-number-basic.tsx`, `app/projects/echo/echo-client.tsx`,
  `app/projects/waffle/waffle-client.tsx`, `middleware.ts`, or `vercel.json`

ROADMAP SC 3 (the deck-coverage table) is owned by plan 05-01 and is deliberately not
in this plan's scope.
</success_criteria>

<output>
Create `.planning/phases/05-foundation-cleanup/05-02-SUMMARY.md` when done.

The SUMMARY MUST carry forward, for Phase 6's consumption:
- **D-12 accessibility contract** (from the deleted `case-study-section.tsx`): section
  `id`, `role="region"`, `aria-labelledby` pointing at the `{id}-heading` element. Phase
  6's section wrapper must preserve this contract while rendering its heading via
  `ScrambleSectionTitle`.
- **D-16 no-AI-slop allowlist and bans**, which Phase 6's `UI-SPEC.md` must encode so
  `gsd-ui-checker` can issue BLOCK verdicts.
- Confirmation that FND-01 is closed, i.e. that Phase 6 may now add `decisions[]` and
  `roleNarrative` to `lib/data/types.ts` and expect them to typecheck.
</output>
